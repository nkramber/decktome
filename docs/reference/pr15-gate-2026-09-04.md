# PR-15 eval harness gate (2026-09-04)

Run date: 2026-09-04. Branch: `pr-15`, from `main` at f299289 (#64). Card snapshot: 2026-09-03. The gate is free: every line is a Go test, a free command over the committed run files, or a dry run. No model call ran.

## The gate lines of the roadmap

| Line | Check | Result |
|---|---|---|
| Corpus fingerprint per run: model, effort, snapshot date, prompt version | `evalrun.TestRoundTrip`, `evalrun.TestMarkdownIsTheSameBlockEverywhere`, and the "## Run" block of every writer | PASS: one header shape, one block, eight writers |
| Long-format results table | `evalrun.Row` per item and metric, JSONL beside every document | PASS: 288 rows of 24 items for deck gate run 14 |
| Suffix rows for informational metrics | `Row.Kind` gate or info, and the compare folds the information rows into one count | PASS: `TestCheckNamesTheFlip` |
| Observe-only is not pass | `evalrun.TestObserveOnlyIsNotPass` | PASS: no gate row, or no verdict of its own, reads NOT EVALUATED |
| The harness runs on PR-8's output and reports named regressions | `eval compare` of the imported runs 13b and 14 | PASS: one gate flip, deck 17, `blocks` 0 to 1 with `deck_size`, which is F-34 |
| Golden prompts with expected slot sets | `questions-gate.TestExpectationsAreTheFourthBar`, `TestEveryCountedConversationNamesItsExpectations` | PASS: 27 counted conversations carry expectations, a miss fails the gate |
| Expected validation outcomes | The block rows of the deck gate, one gate row per bar per deck | PASS: `deck-gate.TestRunRowsMirrorTheVerdict` |
| A judge role scores plan quality and usefulness on a fixed rubric | `generate.TestJudgePlanReadsTheFourGrades`, `deck-gate.TestPlanJudgeRowsAreInformation` | PASS: four fields, three-point scale, information rows |
| Tier 0 in CI, $0 | `make eval-check` and the job `verify:eval` | PASS: the baseline of 14 with 14b stands alone, no newer run |
| Cost cap per run | `eval.TestSweepPlansUnderTheCap` | PASS: the sweep stops before the step that crosses the cap, and on a FAIL |

## The compare that names F-34

`go run ./cmd/eval compare -base pr8-deck-gate-run13b.jsonl -next pr8-deck-gate-run14.jsonl` reads:

```
## Suite `decks`: FAIL

Baseline `pr8-deck-gate-run13b` of 2026-09-03 against run `pr8-deck-gate-run14` of 2026-09-04. the run fails its own bars.

The epoch moved:

- the card snapshot moved from 2026-09-02 to 2026-09-03
- quality_model moved from none to 20260903T210658Z

| Item | Metric | Kind | Before | After | Read |
|---|---|---|---|---|---|
| 17 | `blocks` | gate | 0 | 1 | WORSE, deck_size |

59 information rows moved, 27 of them worse. Pass -info to list them.
```

## The dry sweep

`go run ./cmd/eval sweep -cap 5 -dry` plans five steps for $4.37: question gate run 34 at $0.18, question eval run 34 at $0.10, deck gate run 15 at $2.54, tier judge run 4 at $0.32, and revise gate run 9 at $1.23. The estimate of the deck gate reads run 14, the newest full run, and not the one-prompt rerun 14b. Nothing ran.

## The free quality gate

`make quality-gate` ran into the scratchpad with the new flags. The document carries the "## Run" block, the run file holds the header and 16 rows, and the verdict reads FAIL on the two precon bars of run 12, as before: Commander 0.78 and Modern 0.88 against 0.95.

## The tree

Go build, vet, the `-race` suite, golangci-lint, and `make ste-check` pass. The harness packages hold 122 passing tests. `.gitignore` and `.dockerignore` carry `eval`.

## What the gate does not prove

- That a paid run through the new flags writes a complete fingerprint. The imported runs name no model, because a document never printed one. The paid gate is one sweep through `eval sweep`, about $4.37, on the owner's word.
- That the 27 expectations of the question gate match the words the classifier settles on. Question gate run 34 calibrates them, and a miss there is a wrong expectation or a wrong slot.
- That the plan judge's grades are stable. They are information rows until a baseline exists, and OQ-58 asks the owner to confirm the fields.

Verdict: PASS on the free gate. Time: the Go suite, about three minutes. No provider call.
