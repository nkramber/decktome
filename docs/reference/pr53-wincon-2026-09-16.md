# PR-53, the win-condition target, 2026-09-16

The roadmap gate of PR-53 reads:

> Gate: a test fails on the old roles for a card tagged drain-life. The prompt version rises, and a whole deck gate run reads PASS against run 19. The bracket gate reads the judge at brackets 4 and 5 again.

This document holds every free number of the item. The paid runs of D-740 come last, and the section at the end holds them.

Every count here reads the local card snapshot of 2026-09-04 and the local quality model. The deployed app reads a newer snapshot, so a deployed number can differ by a little.

## What the code does

- **The count.** `profile.FinisherSet` reads the nine parent finisher tags of M-17 with no child tag, and the child tag `blood-artist-ability`. `profile.FinisherIDs` adds the evasive creatures of power 5 or more. The snapshot holds 1,857 such cards.
- **The role.** A Commander shortlist gives the role wincon to the best finishers, up to the target of the bracket. Every other finisher keeps the role it earned (D-741).
- **The order.** An owned mode promotes the owned finishers first. An unowned finisher takes the role only when the collection holds too few (D-742).
- **The pin.** Each promoted finisher pins, so no role cap and no total cut drops the win route (D-726).
- **The target.** The job targets ask for 3 finishers at brackets 1 to 4, and 1 at bracket 5. The places come out of the synergy pieces, so the deck still counts 99 cards.
- **The floor.** Every Commander bracket carries a finisher floor: 2 at brackets 1 to 4, and 1 at bracket 5. A deck under it takes an off-band finding, and the gap note names the cards that close it.
- **The prompt.** The deck shape block names the finisher floor, and each shortlist line marks a finisher. The prompt version rises from 13 to 14.

The count, the role, the floor, and the target read Commander alone. A 60-card format holds no finisher floor, so the role there would move cards between the role caps and buy nothing.

## The first reading, and why it was wrong

PR-53 first gave the role wincon to every card of the curated count. The shortlist caps the wincon role at 15, against 50 for threats and 80 for synergy pieces. So 1,857 cards met a cap of 15, and the shortlists lost win routes.

| Prompt | Finishers on `main` | Finishers on the first reading |
|---|---|---|
| 1, lifegain, any card | 49 | 20 |
| 11, a locked card | 41 | 21 |
| 14, a delegated commander | 57 | 27 |

9 of the 25 pools held fewer finishers than `main`. The owner chose the target-count promotion over a wincon cap of 45, and over a mark with no role change (D-741).

## The second reading, and why it was wrong

The target-count promotion read the whole scored pool. An owned mode drops the unowned cards after that step, so a promotion landed on a card the mode drops. The shortlist then read under the target of its bracket.

The deck gate holds 7 owned-first Commander prompts and no owned-only prompt. The count reads the wincon rows of the shortlist, and the pinned rows of them.

| Prompt | Wincon rows, the second reading | Wincon rows, the fix |
|---|---|---|
| 2, aristocrats, owned first | 0 of 3 | 3 of 3 |
| 5, blink, owned first | 0 of 3 | 1 of 3 |
| 13, the commander is not owned | 1 of 3 | 4 of 3 |
| 15, a delegated commander, owned first | 1 of 3 | 4 of 3 |
| 16, a tight budget, owned first | 0 of 3 | 3 of 3 |
| 17, upgrade a precon, owned first | 1 of 3 | 3 of 3 |
| 25, no card of an owned precon | 3 of 3 | 3 of 3 |

The pool of prompt 5 holds one finisher, so 1 is every finisher it has. The owned-only replay of the Gríma request read 2 wincon rows of a target of 3, and it reads 3 of 3 with the fix. The owner chose the owned pass over the code as built, and over a fix of the owned-only mode alone (D-742).

## The shortlists after the fix

The comparison builds the pool of every deck gate prompt on `main` and on the branch, over the local snapshot. It calls no provider.

- No prompt holds fewer finishers than `main`.
- 11 of the 25 pools hold the same cards as `main`.
- The 14 pools that move grow by 1 to 3 cards, because a pinned finisher adds to the total and takes no place from another card (F-132, D-712).
- Every moved pool is a Commander pool. Every Modern and Standard pool reads as `main` reads it, and so does every set-family pool.
- Prompt 25 adds 3 cards and drops 1, and its finisher count holds at 5. Prompt 14 rises from 57 finishers to 58.

## The tests

- A card tagged `drain-life` takes the role wincon, and so does an evasive creature of power 5. The test fails on the old code: Bloodletting Artist read synergy, Exsanguinate read removal, and Grave Dragon read threat.
- A surplus finisher keeps the role it earned, and no finisher leaves the list.
- The pin holds the target of the bracket: 3 at bracket 3, and 1 at bracket 5.
- The curated set holds no card of a child tag, so Ragavan, Nimble Pilferer stays out.
- An owned mode pins an owned finisher, and not the better unowned one. The test fails on the second reading: owned-only pinned no card, and owned-first pinned the unowned card.
- The finisher feature counts a deck, and a snapshot with no tag holds no row.
- Every Commander bracket reads its finisher floor, and the prompt line names the mark.
- The job targets read 3 finishers at brackets 1 to 4 and 1 at bracket 5, and each deck still counts 99 cards.

## The smoke fixture

The fake provider builds one deck for the smoke flow, and every name of it must sit in the shortlist of the smoke slots. The new shortlist drops Sneering Shadewriter, so the fixture names Victory's Herald, a white Angel that the test collection holds. The deck still counts 99 cards with no repeated name.

## Limits

- Tagger tags are community data (F-5). A tag can miss a finisher, or hold a card that wins no game. The curated count of M-17 answers the second case, and no count answers the first.
- The evasive rule reads the power of the front face. A creature with a power of `*` counts as no finisher.
- The floors come from M-17, and no real list carries a bracket. No group of real lists stands for bracket 4.

## Deck gate run 24, and the three defects it found (D-743)

Run 24 reads **FAIL**. It cost $2.7876 over 2,320 seconds on commit `6ff0244`, at prompt version 14.

| Measure | Run 23 | Run 24 |
|---|---|---|
| Verdict | PASS | FAIL |
| Decks with no block finding | 25 | 23 |
| Summaries that state a FALSE rule | 0 | 1 |
| Decks that needed the repair turn | 1 | 4 |
| `profile_off_band` findings | 12 | 15 |
| Mean plan score | 0.72 | 0.72 |
| Calls | 76 | 79 |

`eval-check` moves three gate rows worse against baseline run 19: the blocks of prompt 5 and prompt 18, and the false rule of prompt 23.

**F-150: the finding holds no subject.** The off-band finding read " is 0, and bracket 3 wants 2 or more". `featureWords` named no finisher.

**F-151: the gap note states a false rule.** Deck 23 read "Bracket 3 wants 2 or more finishers, the cards that win the game, and the deck holds 1." The judge marked it false. The bracket system counts Game Changers, mass land denial, extra turns, banned cards, and combos, and it counts no finisher.

**F-152: an unreachable floor can empty the deck.** Prompt 5 builds from an owned-first pool that holds one finisher against a floor of two. The repair answered "No legal repaired deck can be returned" and returned 0 cards.

Both decks that took a finisher gap note in run 24 had pools that could not close the gap. Prompt 5 holds 1 finisher of a floor of 2, and prompt 23 holds 1 of 2.

## The fixes of D-743

- The feature word reads "the finisher count".
- The finisher note and the finisher finding name the deck plan. The three power floors of D-704 keep the bracket.
- The shortfall carries the code `finisher_short`, and no repair input holds it.
- A repair that answers an empty deck never replaces a deck that holds cards. The guard read the earlier pass alone, and prompt 5 carried a block of its own.

Each fix has a test that fails on the code of run 24.

## Deck gate run 25, and the defect it found (D-744)

Run 25 reads **FAIL** on one bar. It cost $2.5900 over 2,050 seconds.

| Measure | Run 23 | Run 24 | Run 25 |
|---|---|---|---|
| Verdict | PASS | FAIL | FAIL |
| Decks with no block finding | 25 | 23 | 25 |
| Summaries that state a FALSE rule | 0 | 1 | 1 |
| Decks that needed the repair turn | 1 | 4 | 1 |
| Cost | $2.6509 | $2.7876 | $2.5900 |

The three fixes of D-743 hold on the gate. Prompt 5 built 99 cards with no repair, against 0 cards on run 24. The finding reads `finisher_short`: "the finisher count is 1, and this deck plan wants 2 or more (Angel of Serenity)". The reworded gap note reached a summary, and no judge flagged it. `eval-check` moves one gate row worse against run 19, against three on run 24.

**F-153: a summary called a non-legendary creature the leader.** Prompt 5 wrote "its dedicated closing cards, led by Angel of Serenity". A rejudge of the 25 summaries held the verdict at $0.1045, so it is no judge noise. Run 19 and run 23 wrote the same prompt with Gilraen or with no leader, so the finisher target moved the words.

## The prompt guard, and the small runs (D-744)

The owner asked for a small run of the one prompt before a whole one.

| Run | Prompts | Verdict | Rule claims | False rules | Cost |
|---|---|---|---|---|---|
| 26, the first guard | 5 | PASS | 1 | 0 | $0.1292 |
| 27, the reworded guard | 5 | PASS | 0 | 0 | $0.1018 |

Run 26 read PASS, and its summary echoed the wording of the guard as "No other card leads it." The guard asked the model to say it. The reworded guard forbids the words and asks for none. Run 27 wrote "Gilraen leads a white blink shell" and "chief among them Angel of Serenity".

## Deck gate run 28: PASS

Run 28 measures every fix. It cost $2.7527 over 2,375 seconds at prompt version 15.

| Measure | Run 23 | Run 24 | Run 25 | Run 28 |
|---|---|---|---|---|
| Verdict | PASS | FAIL | FAIL | **PASS** |
| Decks with no block finding | 25 | 23 | 25 | 25 |
| Invented names | 0 | 0 | 0 | 0 |
| Summaries that state a FALSE rule | 0 | 1 | 1 | **0** |
| Summaries that state a rule | 0 | 1 | 1 | 0 |
| Decks that needed the repair turn | 1 | 4 | 1 | 2 |
| Mean plan score | 0.72 | 0.72 | 0.70 | 0.69 |
| Cost | $2.6509 | $2.7876 | $2.5900 | $2.7527 |

`eval-check` reads PASS against baseline run 19, and no gate row moved against its sense. Run 24 moved three rows worse, and run 25 moved one.

## Bracket gate run 8

Run 8 built the six decks of brackets 4 and 5, prompts 10 to 15. It cost $0.5318 over 489 seconds.

| Measure | Run 6 | Run 7 | Run 8 |
|---|---|---|---|
| Judge agreed with the bracket | 4 of 6 | 5 of 6 | **5 of 6** |
| Decks in every band | 5 of 6 | 5 of 6 | 4 of 6 |
| Decks with no content violation | 6 of 6 | 6 of 6 | 6 of 6 |
| Decks with no block finding | 6 of 6 | 6 of 6 | 6 of 6 |

The judge bar is 80 percent, and run 8 reads 83 percent. That is the bar the roadmap gate of PR-53 names, and run 8 holds it.

**Every deck meets its finisher floor.** The six decks hold 3, 5, 4, 2, 1, and 2 finishers, against floors of 2, 2, 2, 1, 1, and 1. No `finisher` row sits off band.

The verdict reads FAIL on the band bar, as runs 6 and 7 do. Najeela sits off band on `color_sources`, as it did on both earlier runs. Run 8 adds one deck: Kinnan reads `avg_mana_value` 2.53 against a band of 1.2 to 2.4, and `hands_two_to_four_lands` 0.64 against 0.65. Both misses are small, and the two other bracket 5 decks read 2.07 and 2.35, inside the band. Each run builds new decks, so one deck of six is one sample.

## The review of #182

`gitar-bot` found one defect, and the session fixed it. `promoteUpTo` stepped over a card that already read wincon and pinned, and it counted none of it. `roles.go` names a wincon from the tag alternate-win-condition, and `pinPower` pins a finisher that reaches the keep rate. So a card can carry both before the promotion. The promotion then added the whole target on top of it, and each extra pinned card skips its role cap and adds to the total.

A pre-pinned wincon finisher counts toward the target now. A test fails on the code of run 28: at bracket 4 the list held 4 pinned wincon rows against a target of 3.

The fix moves one deck gate pool. Prompt 3, an artifacts Commander deck at bracket 4, falls from 320 cards to 319. Its finisher count holds at 10, the count `main` reads. No prompt holds fewer finishers than `main`, and 11 of the 25 pools still hold the same cards as `main`. So deck gate run 28 measured a shortlist one card wider on one prompt of the 25.
