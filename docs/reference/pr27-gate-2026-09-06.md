# PR-27 gate, the feedback harvest (2026-09-06)

PR-27 is the feedback harvest (F-49, D-557 to D-559). Its gate is five lines in the roadmap: every placement has a unit test, the dialog passes axe, the store passes its emulator test, the smoke flow rates the built deck and reads the toast, and every thumbs button is 44 pixels or more on a touch screen. This document holds the parts, the checks, and the read of 2026-09-06.

Verdict: PASS. Every check passed on 2026-09-06 on the owner's Mac, on branch `pr-27` from `main` at 9e91959. No step called a model, and the gate cost nothing.

## 1. What the branch holds

| Part | Where | Decision |
|---|---|---|
| `FeedbackService.SubmitFeedback`, the `Feedback` message with the kind, the verdict, the four ids, the reason keys, and the text | `proto/mtg/v1/feedback_service.proto` | D-558 |
| One document per verdict under `users/<uid>/feedback/<id>`, with the short names, the prompt versions, and the time | `go/internal/feedback` | D-558, D-561 |
| The reason keys per kind, in Go and in TypeScript, pinned by a test on each side | `go/internal/feedback/feedback.go`, `web/.../features/feedback/reasons.ts` | D-561 |
| The handler: the object must be the caller's, the ids of the kind alone, the text under the message cap, the reasons of the kind alone | `go/internal/feedbacksvc` | D-558, D-561 |
| The service behind the auth interceptor, and the store in `make store-check` | `go/cmd/api/main.go`, `Makefile` | D-558 |
| `Thumbs`, the pair of buttons, and `FeedbackDialog`, the thumbs-down form with the check boxes and the "Other" box | `web/.../features/feedback` | D-557 |
| The import boundary: `feedback` is a leaf, and the chat, the deck, and the workspace may import it | `web/apps/web/eslint.config.js` | D-558 |
| The four placements: under a question, beside the summary, at the foot of a tile and in the detail sheet, and beside the legality line | `question-card.tsx`, `session-page.tsx`, `deck-view.tsx`, `card-tile.tsx`, `card-detail.tsx` | D-557, D-559 |
| The smoke flow rates the built deck and reads the toast | `web/apps/web/e2e/smoke.spec.ts` | D-313 |

## 2. The placements

| Where | Kind | Accessible name of the pair | Visible when |
|---|---|---|---|
| Under the text of an open question card, and under an answered question in the thread | question | "Rate this question" | Always |
| Beside the summary paragraph of the deck header | summary | "Rate the deck description" | Always |
| At the foot of every card tile | card | "Rate <card name>" | Always on a touch screen. On hover and on focus with a pointer, and after a verdict |
| Under the title of the card detail sheet | card | "Rate this card" | Always |
| Beside the legality line of the deck header, with the caption "Rate this deck" | deck | "Rate this deck" | Always |

The public page of a share link shows no pair: it renders the card groups with no deck id, and the API would refuse the write with no sign-in.

## 3. The free checks

| Check | Result |
|---|---|
| `buf lint`, `buf generate`, `go mod tidy` (`make proto`) | pass. The generated files are new on the branch, so `make proto-check` reads them as untracked until the commit. |
| `go build ./...`, `go vet ./...` | pass |
| `go test -race` of `internal/feedback`, `internal/feedbacksvc`, `internal/decksvc`, `cmd/api` | pass. `TestSubmitRefuses` holds 25 refusals, and `TestSubmitStoresTheVerdict` six writes. |
| `go test -race ./...`, the whole suite | pass, after the removal of a stray `go/api` binary (section 4) |
| golangci-lint | 0 issues |
| `make store-check`, six stores on the emulator | pass. `TestFeedbackRoundTrip` reads a verdict back whole and refuses another user's read. |
| The web lint, the typecheck with the spec included | pass |
| The web tests | pass, 272 tests, 10 new. The five of `Thumbs` cover the thumbs up, the dialog, the forgotten draft, the failed write, and the ids per kind. |
| The dialog under axe | no violation, in `thumbs.test.tsx` |
| Every thumbs button 44 pixels or more on a touch screen | `pointer-coarse:size-11` on both buttons, asserted in `thumbs.test.tsx` |
| The API refuses another user's session, deck, and card | `PermissionDenied`, three cases of `TestSubmitRefuses` |
| `make smoke` | PASS, 1 of 1, the flow in 5.2 seconds and the whole target in 15.8 seconds. The flow clicks "This helped" under "Rate this deck" and reads "Thank you for your feedback!". |
| `make ste-check` | 0 findings |

## 4. Read

Four things a reader should know. First, the thumbs put two more buttons in every card tile, and two existing tests read the one button of a tile or the one group with the question's text. Both now name what they want, the title of the name button and the prefix "Question:". The product did not change under them. Second, the accessible name of a question's pair is "Rate this question" and not the question text, because the pair sits inside the group that carries the text. Third, a build of the API from the `go` directory left a 99 MB binary at `go/api`, and `TestNoCommandBinaryIsPresent` refused it. The binary is gone, and the suite passes. Fourth, the roadmap still read PR-23 as built on its branch. The branch holds the correction: PR-23 merged as #73.

## 5. What waits

The owner triggers the workflow `smoke` on GitHub before the merge, because the branch touches the user path (D-313). PR-28, the loop, starts after the merge and the first real feedback (D-557).
