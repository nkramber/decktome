# PR-21 share link and print view gate (2026-09-03)

Run date: 2026-09-03. Branch: `pr-21`, from `main` at #61. The gate is free: every line is a test, and no model call runs.

## The gate lines of the roadmap and the plan

| Line | Test | Result |
|---|---|---|
| A revoked link answers `NotFound` | `decksvc.TestShareRevokeAndRead` | PASS: the revoked token, the replaced token, a token of the wrong shape, and an unknown token all read NotFound |
| A test reads the shared message and finds no user field (guardrail 13) | `decksvc.TestSharedDeckHoldsNoUserField` | PASS: it reads `SharedDeck` and `SharedCard` from the proto text, 15 fields, and finds no session, collection, owned, user, uid, token, favorite, or revised field. The public export copy clears every user field too |
| The rate limit refuses the 61st call in a minute | `ratelimit.TestSixtyPerMinute` | PASS, and the next minute starts fresh |
| It reads the client address from `X-Forwarded-For`, and two forwarded addresses get two buckets | `ratelimit.TestClientAddressReadsTheForwardedHeader`, `ratelimit.TestInterceptorLimitsTheNamedProcedures` | PASS: the proxy's own address is never the key, and a procedure outside the list never counts |
| The public read needs no sign-in, and every other procedure keeps the check | `auth.TestPublicProcedurePassesWithNoToken` | PASS |
| The store keeps the hash and drops the old link | `decks.TestEmulatorShareRevokeLookup` | PASS against the Firestore emulator on 2026-09-03, through `firebase emulators:exec`, beside every other store test. CI's `verify:emulator` job runs it too |
| The public page shows the deck and nothing about the owner | `shared-deck-page.test.tsx` | PASS: the cards by role with art, the reason lines, the export, no owned mark, no price, no account, no navigation, and the NotFound message |
| Share, the link shown once, and Revoke | `deck-screen.test.tsx` "makes a share link, shows it once, and revokes it" | PASS |

## The print view

The deck page prints as the list by role in black on white, with no image: the print variant hides the header, the chat, the images, the stats, the filters, the sample hand, and the export, and `index.css` drops the dark ground, the shadows, and the frame. No test renders a print, and the owner reads it in a browser's print preview.

## The tree

Go build, vet, the `-race` suite, golangci-lint, `make ste-check`, the web lint, the web typecheck, and the web tests pass. `make proto` regenerated the Go and the TypeScript code for the four RPCs and the two messages.

## What the gate does not prove

- The limiter runs behind Firebase Hosting and Cloud Run only after PR-22. Until then the local stack sends no `X-Forwarded-For`, and the remote host is the key.
- The look of the public page and the print in a browser.

Verdict: PASS. Time: the two suites, about three minutes. No provider call.
