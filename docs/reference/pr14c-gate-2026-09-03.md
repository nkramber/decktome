# PR-14C gate: the two lanes, the fit, and quality gate run 12 (2026-09-03)

Run date: 2026-09-03. Branch: `pr-14c`, from `main` at #59. Card snapshot: 20260903T090227. The gate of PR-14C reads: each lane adds its lists to the store, the fit reads them, and the free quality gate shows the pair bars with them. No band and no bar moves for a source.

## The refresh

`META_ARGS="-meta-months 1 -meta-pages 5" make meta-refresh` ran from 15:56 to 16:07 local, eleven minutes, and stored model `20260903T210658Z`. The report per source:

| Source | Pages | Parse failures | Fetch errors | New lists | Note |
|---|---|---|---|---|---|
| MTGTop8 | 300 | 0 | 0 | 259 | 35 paper events, and the cap held with more pages waiting |
| MTGGoldfish | 200 | 0 | 0 | 158 | 193 deck pages, 35 of them tournament decks or other formats, and the cap held |
| MTGO | 1 | 0 | 0 | 32 | The short read of five pages |
| Topdeck.gg | 1 | 0 | 0 | 8 | |
| MTGJSON, cEDH database, EDHREC | 0 | 0 | 0 | 0 | Read earlier the same day |

The M-6 failure rate of both new lanes is zero over 500 pages. The whole cap of the MTGGoldfish lane went to the newest Modern decks, so the Standard typical rung stayed empty. The cap is per format since (D-506).

## Quality gate run 12

`docs/reference/pr14b-quality-gate-run12.md`, free, 56 seconds. The verdict reads FAIL on the same two bars as run 11.

| Format | Typical lists | Great over precon | Precon over bad | Run 11 |
|---|---|---|---|---|
| Commander | 1,037 | 0.98 | 0.78 | 0.78 |
| Standard | 0 | 0.92 | 1.00 | 1.00 |
| Modern | 149 | 0.99 | 0.88 | 0.9465 |

Modern has a typical rung now, 121 train and 28 holdout lists. The rung reads itself right in 9 of 28, and the other 19 spread over every rung, as user decks of every quality do. The precon bar fell from 0.9465 to 0.88, as it fell in Commander when the typical rung grew (D-488). The weak axes are colors at 0.53 and copies at 0.88. Four weights moved by more than 0.3: `unseen_share` from -1.68 to -0.80, `playset_share` from -0.95 to -0.51, `land` from +0.32 to -0.00, and `card_rate` from +0.71 to +0.44. The user decks hold unseen cards and odd playsets, and the model reads them as less of a defect since. No weight changed by hand (D-486).

## The built decks

The free explain mode over deck gate 13b, with the run 12 model, grades 19 of 24 built decks bad and 5 typical, and the detector flags 10. Run 11 read 19 bad, 4 typical, 1 good, and 13 flagged. The four Modern decks moved: the tournament tempo deck from good to typical, and the three casual ones from a flagged 0.06 to an unflagged 0.26 to 0.33, still under the baseline. The two Standard decks did not move, because the Standard typical rung is empty. The Commander decks did not move.

## Verdict

FAIL on record, as PR-14A and PR-14B merged (D-466, D-491). Both lanes work: zero failures over 500 pages, and the Modern typical rung exists. The bars did not move toward the pass, and the judge bar of PR-14B stays open. The next refresh fills the Standard rung, and the daily runs fill both rungs to the 4,000 the fit keeps (D-483).
