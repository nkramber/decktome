# F-49: the first harvest and the judge lane, 2026-09-23

This document holds each measurement of PR-68 (F-49, D-840, D-841). The harvest files stay on the Mac of the owner, under `.local`, and never enter git (D-841). This document names no user id.

## The deploy of the base

- Cloud Build `d1183bd` of `deploy-api` built `e755f65`, from #217. It started at 21:10:56 UTC and ended SUCCESS at 21:14:25 UTC on 2026-09-23.
- The `mtg-meta` job reads the image `worker:e755f65` after that build, read at 21:20 UTC.
- The first meta job that runs the fit of D-839 starts at 06:00 UTC on 2026-09-24. The result is in the section "The commander rates of the deployed model".

## The harvest

`make feedback-harvest` read `decktome-prod` at 21:25 UTC on 2026-09-23. No watermark existed, so it read every verdict. It found 4 verdicts, and no file holds an email address.

| Created (UTC) | Kind and verdict | Reason | Snapshot of D-635 | Route of the reason keys |
|---|---|---|---|---|
| 2026-09-08 02:44 | question, up | none | none | keep case |
| 2026-09-08 03:45 | question, down | `already_answered` | none | Q1, asked again |
| 2026-09-08 03:50 | card, down | the words of the reader | none | the judge |
| 2026-09-09 22:07 | card, down | `wrong_printing` | the deck and the session | C4, a defect |

Each verdict comes from one user. One verdict of four carries a snapshot, and its class writes a defect and no gate case.

## The dry cycle

`make feedback-loop-dry` on `e755f65` read "the triage wrote 2 case(s) a gate measures", and it planned `questions gate, -only 111,112`. Both cases held `"messages":null`, because neither verdict carries a session. The test `TestConversationsFile` of `go/cmd/questions-gate/main_test.go` refuses a conversation with no message. So a live cycle had nothing to measure.

After the fix of D-841, the same dry cycle reads "the triage wrote 0 case(s) a gate measures", and the cycle stops before any gate run.

## The live judge lane

`make feedback-triage` ran at 21:27 UTC with the keys of `.env` and the card snapshot `20260904T210157`. It cost $0.0122. The document reads PASS: 3 verdicts reached a class, 1 is a keep case, and none still wants the judge.

The judge put the verdict of 2026-09-08 03:50 in C4, a wrong printing. The reader wrote that the app never asked which Aragorn, and then chose a completely different card. The card of that verdict is Thranduil, Sindarin Liege, and no printing of Aragorn.

The snapshot holds five Commander cards with the name Aragorn:

- Aragorn, Company Leader
- Aragorn, Hornburg Hero
- Aragorn, King of Gondor
- Aragorn, the Uniter
- Aragorn and Arwen, Wed

F-75 is the same fault, and #99 fixed it at 14:14:40 UTC on 2026-09-08. The verdict came at 03:50:49 UTC on that day, before the fix.

So the class of the judge names the wrong fix. No card class names a card that the build used in place of a named card. This rests on one judge read, and no second read ran.

## The commander rates of the deployed model

Pending: the meta job of 06:00 UTC on 2026-09-24.
