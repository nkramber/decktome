# PR-15 paid gate, 2026-09-04

The paid gate of PR-15 is one sweep through `eval sweep` that reproduces the verdicts of 2026-09-03 and names no flip but F-34 (D-513). This document records the three steps of the procedure, what each one found, and what the session changed on the branch. The owner confirmed the go on 2026-09-04 (D-514). Every command ran on the owner's machine from the repo root.

## The verdict

The deck lane passes and reproduces. Deck gate run 16 reads PASS on 25 of 25, the compare with the baseline of run 14 with 14b names no gate flip, and the run file carries the first complete fingerprint. The tier judge reads FAIL at 7 of 25 against the open bar of D-488, as run 3 did at 8 of 24. The question lane reads FAIL on the fourth bar, and the read names two defects of the harness and three wrong expectations, all fixed on the branch. The revise lane did not run, because the sweep stopped on the tier judge as the owner's rule says. Two paid runs complete the gate, and they wait on the owner (OQ-62, OQ-63).

Verdict: PASS on the deck lane. The question lane and the revise lane wait on OQ-62 and OQ-63.

## The cost

| Step | Document | Calls | Cost |
|---|---|---|---|
| Question gate run 34 | `pr7-question-gate-run34.md` | 664 | $0.18 |
| Question eval run 34 | `pr7-question-eval-run34.md` | 103 | $0.09 |
| Deck gate run 15, prompt 25 alone | `pr8-deck-gate-run15.md` | 3 | $0.11 |
| Deck gate run 16 | `pr8-deck-gate-run16.md` | 93 | $3.79 |
| Tier judge run 4 | `pr14b-quality-judge-run4.md` | 25 | $0.36 |
| Total | | 888 | $4.53 |

The estimate of the deck gate read $2.54, the cost of run 14. Run 16 cost $3.79: the plan judge added 25 calls, and 12 decks needed the repair turn against 9 in run 14. The sweep of step 9 ran under a cap of $5.00 and not the $4.50 of the procedure, because the estimate of the plan judge sat outside the cap. The next estimate reads $3.79.

## Step 2, the question gate and its eval

`EVAL_SWEEP=1 go -C go run ./cmd/eval sweep -cap 0.50 -suites questions,question-eval -continue` ran both steps. Run 34 reads FAIL. The three old bars pass: 26 of 27 counted conversations used catalog questions only, no conversation was premature, and the linter found nothing. The fourth bar failed on 21 misses over 15 conversations. The eval of run 34 reads 6.8 percent bad on the holdout, against 10.3 percent in run 33, and the sweep printed its verdict as unnamed. The eval has no verdict of its own, so its suite reads NOT EVALUATED, as D-512 says.

The 21 misses, sorted by cause:

- 17 misses came from the renderer of the gate, a defect of the harness. The commander and the locked cards are names on the state, `CommanderNames` and `LockedNames`, and the classifier never fills the id fields of the proto. The renderer read the id fields, so every commander and every locked card read none: 9 commander misses, of which one was the `*` of "the user says none, then picks", and 2 locked misses. A reader with no collection never gets the pool question, and the build reads any-card for them (D-37). The renderer read the unset slot as none: 6 pool rule misses on the six conversations with no collection.
- 3 misses were wrong expectations. "blink with a thin library" and "a commander the library does not hold" name a budget in their fourth message, and that message never goes out: the net of D-351 closes the budget question after two turns of silence, and the slots are full after turn 3. "mill for a playgroup" names the scope of its budget in the fourth message, which never goes out either. The two budgets left the expectations, and the third reads `100` with any scope.
- 1 miss is a wrong slot, F-36. "poison in a tournament" opens with "An infect deck for a Modern tournament", and the second message says "Modern. The best deck under budget." The classifier replaced the theme `infect` with `the best deck under budget`. The expectation stays, and the miss names the defect on the next run.

The gate wrote no run file for run 34. The question gate returned the FAIL error before it wrote the run file, and every other gate writes the file first. `writeRunThen` fixes the order, and a test pins it. So the questions suite has no run file from this gate, and run 35 writes the first one (OQ-62).

## Step 7, the deck gate on prompt 25

`DECK_GATE_OUT=docs/reference/pr8-deck-gate-run15.md DECK_GATE_ARGS="-only 25" make deck-gate` built the Avengers prompt alone. The plan judge schema went through the Anthropic API with no error, and the run block names every role: generate and repair on `gpt-5.6-terra`, the judge on `claude-opus-5`. The verdict reads PASS, the exclusion removed 57 cards of Avengers Assemble with 27 spare, and no excluded card reached the deck.

The plan judge read the deck as a different deck: theme_fit no, summary_honest no. The request asks for Avengers superheroes from the library first, with no card of the Avengers precon. The Avengers cards of that library live in the precon, so the pool held 152 names and few superheroes. The build answered with a mono-black Syr Konrad list, and the summary never said that the theme could not be served. Run 16 read the same prompt the same way. That is F-37.

## Step 9, the deck gate, the tier judge, and the revise gate

`EVAL_SWEEP=1 go -C go run ./cmd/eval sweep -cap 5.00 -suites decks,tier-judge,revise` ran two of the three steps and stopped on the tier judge.

Deck gate run 16 reads PASS: 25 of 25 decks passed every block check, no invented name reached the user, and no summary stated a false rule. The plan judge read all 25 decks with no error, and the mean plan score is 0.69. The compare with the baseline of run 14 with 14b names no gate flip. 76 information rows moved, 41 of them worse, and the pass is on record with them: `buy_cost` rose on 11 decks and fell on 8, `grade` fell on 14 decks and rose on 9, and 3 decks needed a repair turn that run 14 did not. The one deck run 14 blocked, deck 17, passed with a warning. `eval baseline -suite decks -run pr8-deck-gate-run16.jsonl` records run 16 as the decks baseline, the first with a complete fingerprint.

Tier judge run 4 reads FAIL: the judge agreed with the model on 7 of 25 decks, 28 percent against the bar of 80, 6 off by one rung. Run 3 read 8 of 24. The judge reads most decks one or two rungs above the model, as before. The bar stays open on the corpus (D-488, D-491), and no baseline was recorded for the suite.

The revise gate did not run. The procedure of D-513 gave step 9 no `-continue`, and the tier judge was FAIL on record before the sweep started. Revise gate run 9 costs about $1.23 and waits on the owner (OQ-63).

## What the plan judge wrote, read past the grades

The grades are information rows until a baseline exists, and a bar reads a number and never the prose (lesson 11). Two reads of the prose stand out.

- The judge wrote the one word "placeholder" as its reason on `summary_honest` in 10 of 25 decks, and never on another field. That is F-38. The schema now asks for the reason before the grade in each field, the document counts the empty reasons as a number, and `plan_reasons_empty` is a row per deck. `PlanRubricVersion` reads 2.
- The judge graded legality from its own card knowledge and got every fact wrong. On deck 2 it read Denethor, Ruling Steward as mono-black, and Scryfall reads the card white-black on 2026-09-04. On deck 10 it read Boros Charm, Inspiring Vantage, and Sacred Foundry as outside Standard, and it read a four-of as a black card. Scryfall reads all three legal in Standard on 2026-09-04, and the four-of, Redcap Gutter-Dweller, costs {2}{R}{R}. The deterministic checks passed both decks against the snapshot of 2026-09-03. That is F-39. The instructions now tell the judge that code checked the legality, the color identity, and the counts, and that its knowledge of the card pool can be older than the data. The deck text the judge reads holds no mana cost and no color, and the fix that grounds it waits on the owner (F-39).

## What else the gate changed on the branch

- `eval check` read PASS on a baseline with no newer run. D-512 says a suite with nothing evaluated never reads PASS, so it reads NOT EVALUATED now, with the same green exit code.
- The sweep prints the eval's verdict as "unnamed". The suite reads NOT EVALUATED in the check, and the word in the sweep stays.
- A one-prompt run against a full baseline reads PASS on the one item, and the check names the 24 items the run lacks. Run 15 read that way against run 14 with 14b, and it is on record here.

## The tree

Go build, vet, the `-race` suite, golangci-lint, the web lint, the web typecheck, and `make ste-check` pass on the branch after the changes.

## What the gate does not prove

- That the 27 expectations hold under the fixed renderer. No live run has read them through it. Run 35 proves it, or names the next wrong expectation (OQ-62).
- That the revise gate writes its run file and its fingerprint through the new flags. Run 9 proves it (OQ-63).
- That the reason before the grade stops the placeholder. The next deck gate run proves it, and the count says how far.
- That the legality line stops the judge from grading legality. The prose of the next run says.
