# Pull request 217 review

Date: 2026-09-23

## Identity

- PR: 217
- Roadmap item: PR-67
- Target: `main`
- Base: `f00a2af0d4e40dff6cc0a4414928bd492d905c5a`
- Merge base: `f00a2af0d4e40dff6cc0a4414928bd492d905c5a`
- Head: `d7f103ef8bc33c28e84572ac99babd8703772cdf`
- Branch: `f166-typal-bracket-5`

## Provider gate

The session provider is Codex, from the active environment. The hand-off names Claude Code as the author provider. The providers differ, so the gate passes.

## Intended behavior and scope

F-166 asks the bracket 5 Commander shortlist to favor cards in the TopDeck lists of its commander. D-839 sets a minimum of 10 lists and a theme boost of 0.05. Other brackets and formats keep their current ranking.

The review read every changed path: `docs/SESSION-HANDOFF.md`, `docs/decisions.md`, `docs/design-roadmap.md`, `docs/reference/eval/pr14a-bracket-gate-run11-judge2.jsonl`, `docs/reference/eval/pr14a-bracket-gate-run11.jsonl`, `docs/reference/f166-commander-rate-2026-09-23.md`, `docs/reference/paid-targets.md`, `docs/reference/pr14a-bracket-gate-run11-judge2.md`, `docs/reference/pr14a-bracket-gate-run11.md`, `docs/reference/session-handoff-archive.md`, `go/cmd/bracket-gate/main.go`, `go/cmd/deck-gate/main.go`, `go/cmd/revise-gate/main.go`, `go/internal/agentsvc/build.go`, `go/internal/candidates/candidates.go`, `go/internal/candidates/commander_rate_test.go`, `go/internal/candidates/power.go`, `go/internal/quality/commander_rates_test.go`, `go/internal/quality/corpus.go`, `go/internal/quality/fit.go`, `go/internal/quality/model.go`, and `go/internal/quality/score.go`.

The fit captures TopDeck rates before the tier cap. The scorer uses the exact commander or partner pair row. The production builder and all three gate tools pass those rates to the shortlist. The candidate builder applies them only to bracket 5 Commander requests. Its tests cover a played off-theme card, a theme card, the boost, and unchanged brackets and formats.

## Findings

No finding.

## Out of scope

None.

## PR comments

No review threads exist. The Gitar dashboard comment contains no Code Review finding. D-838 pauses the Gitar review requirement.

## Description edits

None.

## Verification

- `git diff --stat f00a2af...d7f103e`: 22 paths, 1,090 insertions, and 31 deletions. The effective head is `d7f103e` because each newer commit changes paths outside the metadata set.
- `python3 docs/tools/review_gate.py --effective-head 217`: passed and returned `d7f103ef8bc33c28e84572ac99babd8703772cdf`.
- `make verify` with Node 20.17.0: failed at web test worker startup with `ERR_REQUIRE_ESM`. The hand-off names Node 22.23.2 as required.
- `PATH=/Users/nate/.nvm/versions/node/v22.23.2/bin:$PATH make verify`: passed on `d7f103e`, exit 0. This ran Go race tests, 344 web tests, lint, build, and the repository checks. `eval-check` reported the decks and questions baselines as PASS and revise as NOT EVALUATED because no newer whole run exists.
- `gh pr checks 217`: all verify jobs and `pr-contract` passed. `verify:changes` skipped. `review-gate` failed because the review record was absent at that time. The record will reach the branch with this push.
- PR comments and review threads: `gh pr view 217 --comments` and the pull request comments and reviews API returned no review threads or findings.
- Push: the review commit and hand-off must reach the branch. Verify the remote head with `gh pr view` and `git rev-parse HEAD`.

## Open questions and accepted risks

- The deployed model has no commander rates until the next meta job runs the new fit. Read one deployed bracket 5 session after that job (D-839).
- The partial bracket gate run covers prompts 13 to 15. A whole run of prompt version 16 and the fixing floor remains a paid follow-up. The owner must authorize it.
- The Najeela replay retained 87 of 91 cards used by at least half of its TopDeck lists. The existing land cap accounts for four omitted lands. This pull request does not change that cap (D-839).

## Verdict

**Ready for owner merge.** This review covers the effective head. The provider gate passes, focused regression tests pass, and the local verify gate passes. The targeted bracket gate evidence also passes. No blocking finding remains.
