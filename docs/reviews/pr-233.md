# Pull request 233 review

Date: 2026-09-25

## Identity

- PR: 233
- Roadmap item: PR-82
- Target: `main`
- Base: `8d4b01708f30751b7cac2ffcfa3310e5462a5c44`
- Merge base: `8d4b01708f30751b7cac2ffcfa3310e5462a5c44`
- Head: `c1ca25bfb00259e1f66f9c73be6d1263ef9b8190`
- Branch: `fix/f175-web-guard-serial-builds`

## Provider gate

The hand-off names Claude Code as the author provider. The owner’s session statement agrees. This session runs on Codex. The providers differ, so the gate passes.

## Intended behavior and scope

PR-82 lowers the web build to 2 CPUs so the API and web builds of one merge fit the regional 10-CPU pool. F-175 and D-948 set the scope. The test must fail when their total exceeds 10 CPUs. The next API and web deploy must confirm the builds start together and the guard passes.

I inspected `cloudbuild/api.yaml`, `cloudbuild/web.yaml`, `docs/tools/deploy_order.py`, `docs/tools/test_deploy_order.py`, `docs/deploy-and-rollback.md`, `docs/decisions.md`, `docs/design-roadmap.md`, `docs/SESSION-HANDOFF.md`, and `docs/reference/session-handoff-archive.md`. I read each changed path in context. I did not inspect unrelated code, documents, or assets.

## Findings

No finding.

## Out of scope

None.

## PR comments

- Gitar approved the current head with no finding. The GraphQL review-thread list has no threads. The Gitar check passed after the body correction.

## Description edits

- Replaced the stale review sentence, “Gitar and Codex wait for the first push,” with “The current review is approved. The review record is in progress.”

## Verification

- `python3 docs/tools/review_gate.py --effective-head 233`: `c1ca25bfb00259e1f66f9c73be6d1263ef9b8190`.
- `python3 -m unittest discover -s docs/tools -p 'test_*.py'`: 296 tests passed. `BuildMachinesTest` passed.
- `make ste-check`: 0 findings.
- `make ref-check`: 0 findings.
- `git diff --check 8d4b01708f30751b7cac2ffcfa3310e5462a5c44..c1ca25bfb00259e1f66f9c73be6d1263ef9b8190`: passed.
- `make verify` with Node 20.17.0: failed at web tests with `ERR_REQUIRE_ESM`. The hand-off documents that this check needs Node 22.23.2.
- `PATH=/Users/nate/.nvm/versions/node/v22.23.2/bin:$PATH make verify`: passed. This includes Go race tests, 409 web tests, the web build, lint, typecheck, and document checks.
- GitHub checks after the first metadata push: Gitar, `pr-contract`, and `review-gate` passed. The required `verify` jobs passed or skipped for the documents-only commit.
- The Gitar check passed after the pull request body edit. Its dashboard approved the current head. No review thread remains open.
- No paid target ran. This pull request changes no UI screen.
- Push: `62fe85fb9e2e3c4a91e612f01b8696c2a24ddfc9` is the head of `origin/fix/f175-web-guard-serial-builds`, verified with `gh pr view`.

## Open questions and accepted risks

- D-948 accepts that overlapping merges can still queue builds when their combined CPU requests exceed the pool quota. The owner requires the next API and web deploy to confirm this fix.

## Verdict

**Ready for owner merge.** This verdict applies to head `c1ca25bfb00259e1f66f9c73be6d1263ef9b8190`.
The provider gate passes. The review found no defect, Gitar and local verification pass, and the only failed check needs this record on the branch.
