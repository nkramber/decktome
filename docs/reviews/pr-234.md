# Pull request 234 review

Date: 2026-09-25

## Identity

- PR: 234
- Roadmap item: M-19
- Target: `main`
- Base: `5ac3873dbd3fdfba3c565dc208c8ff7feeaf004f`
- Merge base: `5ac3873dbd3fdfba3c565dc208c8ff7feeaf004f`
- Head: `36dd4e9749fa2184ee48f1f0bfe2330f18b1200b`
- Branch: docs/m19-replay-owned-shortlist

## Provider gate

The hand-off names Claude Code as the author provider. The pull request changes Go code and documents in commit `36dd4e9`. This session is Codex. The providers differ, so the gate passes.

## Intended behavior and scope

M-19 replays the owned-only land shortlist from the thumbs down and corrects the basic land split. The gate requires a replay, a regression test, current Gitar review, this review record, and `make verify`.

The change touches the basic-land balance rule in `go/internal/generate/manapass.go`. It compares capped sources-to-need ratios first. When those ratios tie, it compares uncapped ratios, sorted from the smallest margin. The phase still rejects any trade that raises the band score. The tests cover a trade toward the lower margin and a stop at equal margins.

The reviewed diff contains seven paths. I inspected all seven: `docs/SESSION-HANDOFF.md`, `docs/decisions.md`, `docs/design-roadmap.md`, `docs/reference/m19-owned-shortlist-2026-09-25.md`, `docs/reference/session-handoff-archive.md`, `go/internal/generate/manapass.go`, and `go/internal/generate/manapass_balance_test.go`.

The provider gate, money, Go code, and document contracts apply. No protobuf, model, legality, user data, web screen, CI configuration, or deploy contract changed. No paid target ran.

## Findings

No finding.

## Out of scope

None.

## PR comments

- Gitar's dashboard approves the current head, and no review thread is open. Its top-level comment reports the missing review record as the only CI failure. The review-gate job failed before this record existed. No Gitar code finding remains unanswered.

## Description edits

None.

## Verification

- `git fetch origin main` and `python3 docs/tools/review_gate.py --effective-head 234`: effective head is `36dd4e9749fa2184ee48f1f0bfe2330f18b1200b`.
- `go test ./internal/generate/ -run 'TestTheBalancePhase' -count=1` in `go/`: passed at the reviewed head.
- `make verify` with Node 20: failed because the web test workers raised `ERR_REQUIRE_ESM`. The hand-off says this machine needs Node 22.23.2.
- `env PATH=/Users/nate/.nvm/versions/node/v22.23.2/bin:$PATH make verify`: passed at the reviewed head. All checks passed. The emulator lane needs `make store-check` and `make gcs-check`, and govulncheck runs weekly.
- `MANAPASS_IN=docs/reference/pr8-deck-gate-run29.md MANAPASS_OUT=/tmp/pr234-manapass-review.md make manapass-check`: did not run because this worktree has no `.local/gcs/mtg-local-cards/scryfall` snapshot.
- GitHub checks at the reviewed head: `Gitar`, `pr-contract`, all `verify` jobs passed. `review-gate` failed because this record did not yet exist. The workflow skipped `verify:changes`. Refresh `review-gate` after this record reaches the branch.
- `make ste-check` and `make ref-check`: passed before this record. Rerun them before commit.
- Push: `36dd4e9749fa2184ee48f1f0bfe2330f18b1200b` is the effective head of origin/docs/m19-replay-owned-shortlist, verified with gh pr view.

## Open questions and accepted risks

- The free `make manapass-check` replay did not run. This worktree lacks its local card snapshot. The roadmap gate does not require this lane. The PR regression tests, full local verification, and CI provide the required evidence.

## Verdict

**Ready for owner merge.** This verdict applies to head `36dd4e9749fa2184ee48f1f0bfe2330f18b1200b`.
The provider gate passes, the review found no defect, the required checks pass, and the current Gitar review has no open thread. The review-gate check must pass after this record reaches the branch.
