# Pull request 235 review

Date: 2026-09-26

## Identity

- PR: 235
- Roadmap item: PR-83
- Target: `main`
- Base: `52d21a78bd3f0cf5075463a00586ca9a0ea15084`
- Merge base: `52d21a78bd3f0cf5075463a00586ca9a0ea15084`
- Head: `222b9bb389b5d0e5e9a6f8bc9cb61a56dbe589e9`
- Branch: `fix/cold-start-index-wait`

## Provider gate

The hand-off names Claude Code as the author provider. The PR has one substantive commit, and its hand-off identifies the author session. This session is Codex. The providers differ, so the gate passes.

## Intended behavior and scope

PR-83 addresses F-176 under D-952 to D-954. Reads that need the first card index wait up to five seconds. The chat retries only an Unavailable refusal that carries `Deck-Tome-Refusal: index-loading`. This keeps a retry before any chat state change. It prevents a second paid call after another failure.

The merge base is `52d21a78bd3f0cf5075463a00586ca9a0ea15084`. The diff has 21 paths. I inspected all paths: the hand-off, decisions, deployment and setup notes, roadmap, archive, Go services and tests, and web chat and tests. I traced the shared refusal header through the API CORS exposure and the Connect client metadata read. The change touches no protobuf, model default, legality rule, user record, paid target, CI action, or deploy definition.

## Findings

No finding.

## Out of scope

None.

## PR comments

- Gitar's current dashboard comment reports that `review-gate` failed because this record was absent. Its code review approves with no finding. Gitar updated the dashboard after the PR head push. The Gitar check passes on head `222b9bb389b5d0e5e9a6f8bc9cb61a56dbe589e9`, and GraphQL returned no review threads.

## Description edits

None.

## Verification

- `git fetch origin` and `python3 docs/tools/review_gate.py --effective-head 235`: effective head `222b9bb389b5d0e5e9a6f8bc9cb61a56dbe589e9`.
- `PATH=/Users/nate/.nvm/versions/node/v22.23.2/bin:$PATH make verify` at `222b9bb389b5d0e5e9a6f8bc9cb61a56dbe589e9`: passed, exit 0. This included Go race tests, 414 web tests, lint, typecheck, build, and container checks. The eval check passed its decks and questions suites. The eval check did not evaluate revise because no newer whole run exists.
- The first `make verify` used Node 20.17.0 and failed to start web tests with `ERR_REQUIRE_ESM`. The required Node 22.23.2 run passed.
- `gh pr checks 235` at head `222b9bb389b5d0e5e9a6f8bc9cb61a56dbe589e9`: `Gitar`, `pr-contract`, and every `verify` job passed. `review-gate` failed because this record was absent. Policy skipped `verify:changes`.
- This review did not inspect the screen at phone or desktop widths. The deployed web bundle predates this PR. This review did not deploy the branch. The change adds one paragraph inside the existing vertical status column. Normal text wrapping remains in effect. The web component tests and production build passed.
- No paid target ran. The post-merge deployment and cold-start checks remain for the owner under the hand-off.
- Push: `222b9bb389b5d0e5e9a6f8bc9cb61a56dbe589e9` is the reviewed head of `origin/fix/cold-start-index-wait`, verified with `gh pr view` before the review metadata commit.

## Open questions and accepted risks

- Responsive visual inspection remains unverified. The added paragraph uses the existing vertical layout and normal text wrapping. No deployment or live web check ran in this review.

## Verdict

**Ready for owner merge.** This verdict applies to head `222b9bb389b5d0e5e9a6f8bc9cb61a56dbe589e9`.
The provider gate passes. The changed behavior and tests hold. Required PR checks pass except `review-gate`, which awaits this record on the branch.
