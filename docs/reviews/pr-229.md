# Pull request 229 review

Date: 2026-09-25

## Identity

- PR: 229
- Roadmap item: PR-79
- Target: `main`
- Base: `8aec3798320035c703ec9e30e84d1b7a6f03c3ba`
- Merge base: `8aec3798320035c703ec9e30e84d1b7a6f03c3ba`
- Head: `950a757213413a654e15c75a2e37f4dbb66d232c`
- Branch: `review-fixes-p5`

## Provider gate

The hand-off states `Author provider: Claude Code`. The PR author and hand-off agree. This session runs on Codex. The providers differ, so the gate passes.

## Intended behavior and scope

PR-79 corrects three P2 and eighteen P3 findings. D-920 approves this scope. Each regression test must fail on the base and pass here. The gate also requires a current Gitar review, this review, and `make verify`.

The diff changes 63 paths across review gating, scripts, Go services and tests, hosting headers, the web app, protobuf checks, and documentation. I inspected each changed path and its relevant callers, tests, and contracts. I did not run a paid target or inspect a deployed preview channel.

## Findings

### P2-1: Delete can remove a session after a build acquires its lease

Status: fixed in `a8efdf3206159d9866843bb792e6a202f4b1d0e6`.

Open at: `4d51d4c58daad5ec68de1d7916bb95a5b3c9da98`.

File: `go/internal/agentsvc/sessions.go:155-170`.

Trigger: A delete reads no lease. Before its later delete transaction, a ready chat acquires the lease and stores its turn. The delete transaction then removes the session and lease while the build runs.

Expected: D-922 requires a delete on any instance to refuse while a build holds the lease. The check and delete must act on one consistent store state.

Actual: `DeleteSession` calls `Leased` before `Get` and `Delete`. `Repo.Delete` then deletes the session and lease in a transaction with no lease check. A lease acquired after the first read does not stop deletion.

Consequence: The build can finish after the session and lease disappear. `storeDeck` can keep the new deck, then fail to record its id because the session no longer exists. The user can see a deck from a session they deleted during its build.

Correction: Check the lease in the delete transaction, and reject a live lease. Add a test that pauses deletion after its lease read. Start a build on another server. Prove deletion leaves the session and lease intact.

Regression check: `TestADeleteRefusesALeaseTakenAfterItsRead` now takes a lease after the earlier reads and checks that deletion keeps the session and lease. `TestEmulatorBuildLease` checks that a live lease blocks the transactional delete. Both pass at `a8efdf3`.

### P2-2: Failed triage marks its harvest as processed

Status: fixed in `c4275833ae517578e44e39c094d10076913ae21e`.

Open at: `da6392363817d5d91540de2e4c6ef36ffb562166`.

File: `go/cmd/feedback-triage/main.go:142-144`.

Trigger: A live `-apply` run reads multiple pending harvests, and one judge call fails after other verdicts route successfully.

Expected: D-924 says a live run records harvests it triaged. The PR-28b gate needs a class or owner question for every verdict, with no failed item.

Actual: The old code called `MarkTriaged` for every input harvest before `Report` returned the failed verdict.

Consequence: The run exits with FAIL, but a later triage skips the harvest containing the failed verdict. That complaint never becomes a case or an owner question.

Correction: Mark a harvest only after each verdict succeeds and the run applies each case. Keep a failed harvest pending for a later live run.

Regression check: `TestSettleKeepsAFailedVerdictPending` records the successful verdict, leaves the harvest with the failed verdict pending, and filters the applied verdict from the next read. `go test ./internal/triage ./cmd/feedback-triage` passes.

### P2-3: A failed case write can duplicate earlier cases on retry

Status: fixed in `950a757213413a654e15c75a2e37f4dbb66d232c`.

Open at: `c4275833ae517578e44e39c094d10076913ae21e`.

File: `go/cmd/feedback-triage/main.go:148-155`.

Trigger: A live `-apply` run writes one case, then a later case append or owner-question write fails.

Expected: D-924 requires a later read to write no case twice. A failed run must retain enough progress for a retry. Otherwise it can duplicate an earlier write.

Actual: `applyCases` wrote cases one at a time and returned on the first error. `run` called `triage.Settle` only after `applyCases` succeeded. The first case remained on disk, but its verdict had no `verdict <id>` line. The next run read that verdict again and appended the case a second time.

Consequence: A transient filesystem error or an unwritable later target can duplicate a case in a gate document. The duplicate can change the gate input and cause repeated owner work.

Correction: `applyCases` now marks each case and owner question after its write succeeds. `run` calls `triage.Settle` after a failed apply too. The settle step records each landed verdict and leaves the failed verdict pending.

Regression check: `TestAFailedWriteKeepsTheWrittenCases` makes the second write fail after the first succeeds. It proves that the retry reads only the failed verdict and keeps the harvest pending. The focused test passes on this head.

## Out of scope

None.

## PR comments

- Gitar found that `takeLease` used the client context. The author moved the lease call to a detached context in `4d51d4c`. Gitar confirmed the correction at `a8efdf3`, and the thread is resolved.

## Description edits

None.

## Verification

- `make where`: returned no PR for the detached worktree. `gh pr view 229`, branch refs, and the hand-off identify `review-fixes-p5` and author provider Claude Code.
- `git fetch origin main` and `python3 docs/tools/review_gate.py --effective-head 229`: returned `da6392363817d5d91540de2e4c6ef36ffb562166`.
- `git diff --stat 8aec379..da63923`: 63 paths, 2,248 insertions, 299 deletions. I inspected the changed paths, their contracts, callers, and tests.
- Gitar thread review: the lease finding is resolved. Its answer and regression test match the correction. Gitar check on `da63923` passed, and no review thread remains open.
- `PATH=/Users/nate/.nvm/versions/node/v22.23.2/bin:$PATH make verify` on `da63923`: passed. Proto generation, Go race tests, lint, web tests, build, shell checks, eval check, and Docker builds passed. The target excludes emulator and GCS lanes, and govulncheck runs weekly.
- `gh pr checks 229` on `da63923`: Gitar, `pr-contract`, and all substantive verify jobs passed. `verify:changes` skipped. `review-gate` failed on the old `Blocked` record. This record now says `Changes required` for P2-2.
- Checks on metadata tip `1c538c1`: Gitar, `pr-contract`, `verify:eval`, `verify:proto`, `verify:shell`, `verify:skip`, `verify:vuln`, and `verify:web` passed. `verify:changes` skipped. `review-gate` failed because the verdict is `Changes required`. `verify:go`, `verify:docker`, and `verify:emulator` remain pending. Those lanes passed at effective head `da63923`, and local `make verify` passed there.
- The triage failure path at `go/cmd/feedback-triage/main.go:142-144` marks all harvests before `triage.Report` computes the failed verdict. The current tests cover pending files, but not a partial judge failure.
- On `c427583`, `go test ./internal/triage ./cmd/feedback-triage`: passed. This includes `TestSettleKeepsAFailedVerdictPending`.
- `PATH=/Users/nate/.nvm/versions/node/v22.23.2/bin:$PATH make verify` on `c427583`: passed. Proto, Go race tests, lint, STE, reference, lifecycle, web tests (44 files, 391 tests), eval check, and Docker builds passed. The emulator and fake GCS lanes are separate targets, and govulncheck runs weekly.
- Gitar on `c427583`: check passed and the dashboard says approved. The only review thread is resolved. Its lease finding and author answer match the regression test.
- `gh pr checks 229` at metadata tip `0654d3c`: `Gitar`, `pr-contract`, `verify:eval`, `verify:proto`, `verify:shell`, `verify:skip`, `verify:vuln`, and `verify:web` passed. `verify:changes` skipped. `review-gate` failed RG 4 because this record correctly says `Changes required`. `verify:docker`, `verify:emulator`, and `verify:go` were still running when I stopped the check wait. Local `make verify` passed on the effective head.
- `python3 docs/tools/review_gate.py --effective-head 229`: returned `c4275833ae517578e44e39c094d10076913ae21e` before this metadata update.
- `git diff --stat da63923..c427583`: eight paths, 190 insertions, 30 deletions. I inspected each changed path and its caller and test context.
- The current Gitar dashboard reports an earlier `review-gate` failure. At that time, the record held `Changes required` and an older head. Rerun the check after you push this record.
- P2-2's test passes. Adjacent fault trace: `applyCases` writes each case, then it returns. `run` calls `Settle` after all writes succeed. A later write error leaves earlier cases unmarked, so a retry appends them again.
- No paid target ran. No production data was read or written.
- `make ste-check`, `make ref-check`, and `git diff --check`: passed after the record and hand-off update.
- Push: `c28dc5c` was the head of `origin/review-fixes-p5`, verified with `gh pr view`.
- `git fetch origin` and `python3 docs/tools/review_gate.py --effective-head 229`: returned `950a757213413a654e15c75a2e37f4dbb66d232c`. The effective head matches the supplied worktree and PR head.
- The diff from `c427583` to `950a757` changes seven paths. I inspected each changed path and its caller, test, and document context.
- `go test ./cmd/feedback-triage/ ./internal/triage/ ./internal/agentsvc/ ./internal/sessions/ ./internal/decksvc/`: passed on `950a757`. This covers the three review findings and adjacent lease behavior.
- `PATH=/Users/nate/.nvm/versions/node/v22.23.2/bin:$PATH make verify`: passed on `950a757`. It ran proto checks, lint, STE, reference and lifecycle checks, Go race tests, 391 web tests, web build, eval check, shell checks, and Docker builds. The target excludes emulator and GCS checks. CI passed the emulator lane. Govulncheck runs weekly.
- `gh pr checks 229`: Gitar, `pr-contract`, and all verify lanes passed. `verify:changes` skipped. `review-gate` failed because the prior record had a stale head and a `Changes required` verdict. Rerun it after this push.
- Gitar's dashboard updated after `950a757` and reports approval. Its only thread is resolved. The saved issue comments and review threads are in `/tmp/decktome-pr-229-comments-and-threads.json`.
- `make ste-check`, `make ref-check`, `git diff --check`, and `python3 docs/tools/review_gate.py --effective-head 229`: passed after this record update. No paid target ran. No production data was read or written.
- Push: `950a757213413a654e15c75a2e37f4dbb66d232c` is the effective head of `origin/review-fixes-p5`, verified with `gh pr view`.

## Open questions and accepted risks

- None.

## Earlier verdicts

**Changes required.** P2-3 duplicated a case after a later write failed. This verdict applied to head `c4275833ae517578e44e39c094d10076913ae21e`.

**Changes required.** P2-2 let a later run skip a failed harvest verdict. This verdict applied to head `da6392363817d5d91540de2e4c6ef36ffb562166`.

**Changes required.** The lease check and session deletion were not atomic, so a concurrent delete removed a live build lease. This verdict applied to head `4d51d4c58daad5ec68de1d7916bb95a5b3c9da98`.

**Blocked.** The repeat review found P2-1 fixed, but treated D-921 as requiring whole paid gate runs before merge. This verdict applied to head `a8efdf3206159d9866843bb792e6a202f4b1d0e6`.

## Verdict

**Ready for owner merge.** The three earlier findings are fixed. The focused regressions, full local verification, current Gitar review, and required CI lanes pass for this effective head.
