# Pull request 229 review

Date: 2026-09-25

## Identity

- PR: 229
- Roadmap item: PR-79
- Target: `main`
- Base: `8aec3798320035c703ec9e30e84d1b7a6f03c3ba`
- Merge base: `8aec3798320035c703ec9e30e84d1b7a6f03c3ba`
- Head: `4d51d4c58daad5ec68de1d7916bb95a5b3c9da98`
- Branch: `review-fixes-p5`

## Provider gate

The hand-off states `Author provider: Claude Code`. The PR author and hand-off agree. This session runs on Codex. The providers differ, so the gate passes.

## Intended behavior and scope

PR-79 corrects three P2 and eleven P3 findings. D-920 approves this scope. Each regression test must fail on the base and pass here. The gate also requires a current Gitar review, this review, and `make verify`.

The diff changes 39 paths across review gating, scripts, Go services and tests, hosting headers, the web app, protobuf checks, and documentation. I inspected each changed path and its relevant callers, tests, and contracts. I did not run a paid target or inspect a deployed preview channel.

## Findings

### P2-1: Delete can remove a session after a build acquires its lease

Status: open.

Open at: `4d51d4c58daad5ec68de1d7916bb95a5b3c9da98`.

File: `go/internal/agentsvc/sessions.go:155-170`.

Trigger: A delete reads no lease. Before its later delete transaction, a ready chat acquires the lease and stores its turn. The delete transaction then removes the session and lease while the build runs.

Expected: D-922 requires a delete on any instance to refuse while a build holds the lease. The check and delete must act on one consistent store state.

Actual: `DeleteSession` calls `Leased` before `Get` and `Delete`. `Repo.Delete` then deletes the session and lease in a transaction with no lease check. A lease acquired after the first read does not stop deletion.

Consequence: The build can finish after the session and lease disappear. `storeDeck` can keep the new deck, then fail to record its id because the session no longer exists. The user can see a deck from a session they deleted during its build.

Correction: Check the lease in the delete transaction, and reject a live lease. Add a test that pauses deletion after its lease read. Start a build on another server. Prove deletion leaves the session and lease intact.

Regression check: No test covers this interleaving at this head. `go test -race ./internal/agentsvc/ ./internal/sessions/` passes, but does not refute the store-level race.

## Out of scope

None.

## PR comments

- Gitar found that `takeLease` used the client context. The author moved the lease call to a detached context in `4d51d4c`. Gitar confirmed the correction, and the thread is resolved.
- The Gitar CI dashboard reported the missing `docs/reviews/pr-229.md` record. This record supplies it. The dashboard had no other code finding.

## Description edits

None.

## Verification

- `git fetch origin`, `git status --short --branch`, and `gh pr view 229`: passed. The tree was clean at start, and the local head matched the open PR head `4d51d4c58daad5ec68de1d7916bb95a5b3c9da98`.
- `python3 docs/tools/review_gate.py --effective-head 229`: returned `4d51d4c58daad5ec68de1d7916bb95a5b3c9da98`.
- `gh pr checks 229` after the record push: Gitar, `pr-contract`, `verify:eval`, `verify:shell`, and `verify:skip` passed. The heavy jobs and `verify:changes` skipped for the metadata-only push. `review-gate` failed because P2-1 keeps the verdict at `Changes required`.
- The Gitar check for `4d51d4c58daad5ec68de1d7916bb95a5b3c9da98` completed successfully after the commit. Its one thread is resolved.
- `python3 -m unittest discover -s docs/tools -p 'test_*.py'`: passed, 253 tests.
- `make verify` with Node 20.17.0: failed when the web tests failed to start because Node 20 did not load an ES module. The hand-off names this runtime mismatch.
- `PATH=/Users/nate/.nvm/versions/node/v22.23.2/bin:$PATH make verify`: passed. All checks passed. The target notes that the emulator lane needs `make store-check` and `make gcs-check`, and govulncheck runs weekly.
- No paid target ran. No production data was read or written.
- `make ste-check`, `make ref-check`, `make context-budget`, and `git diff --check`: passed after this record and hand-off update.
- Push: review commit `03641148952a1f22db67b6dc7693d273e8e95363` was the head of `origin/review-fixes-p5`, verified with `gh pr view`. This final verification update follows as a metadata commit.

## Open questions and accepted risks

- No frame-rule or script-policy check ran on a Hosting preview channel (D-923). The hand-off says the owner must approve a deploy for this check.
- The whole paid gate runs named in the PR body did not run. D-921 says the owner approves those runs.

## Verdict

**Changes required.** The lease check and session deletion are not atomic, so a concurrent delete can remove a live build lease. This verdict applies to head `4d51d4c58daad5ec68de1d7916bb95a5b3c9da98`.
