# Pull request 230 review

Date: 2026-09-25

## Identity

- PR: 230
- Roadmap item: PR-80
- Target: `main`
- Base: `d9faa00ec2f8deb289cddfc925ef5aabb14d3b10`
- Merge base: `d9faa00ec2f8deb289cddfc925ef5aabb14d3b10`
- Head: `a782dfa76427ba64b28d57e2b612af825015fdd5`
- Branch: `review-fixes-p6`

## Provider gate

The hand-off at `docs/SESSION-HANDOFF.md` names Claude Code as the author provider. This session runs Codex. The providers differ, so the gate passes.

## Intended behavior and scope

PR-80 corrects the findings named in D-925 to D-941. The roadmap gate requires the regression tests, a current Gitar pass, this Codex review, and `make verify`. I read the complete diff, plus the callers and consumers of each changed contract.

I inspected all 78 changed paths: `.dockerignore`, `.github/rulesets/review-gate.json`, `.github/workflows/review-gate.yml`, `.gitignore`, `AGENTS.md`, `Makefile`, `README.md`, `cloudbuild/api.yaml`, `cloudbuild/web.yaml`, `docs/SESSION-HANDOFF.md`, `docs/decisions.md`, `docs/deploy-and-rollback.md`, `docs/design-roadmap.md`, `docs/open-questions.md`, `docs/reference/feedback-fixer-prompt.md`, `docs/reference/merge-rules.md`, `docs/reference/paid-targets.md`, `docs/reference/session-handoff-archive.md`, `docs/setup-gcp.md`, `docs/setup-second-mac.md`, `docs/tools/codex_review.py`, `docs/tools/context_budget.py`, `docs/tools/test_codex_review.py`, `docs/tools/test_context_budget.py`, `docs/tools/test_deploy_workflow.py`, `docs/tools/test_feedback_review_guard.py`, `docs/tools/test_review_gate.py`, `firestore.indexes.json`, `go/cmd/api/main.go`, `go/cmd/api/main_test.go`, `go/cmd/bracket-gate/main.go`, `go/cmd/deactivate-user/main.go`, `go/cmd/deactivate-user/main_test.go`, `go/cmd/deck-gate/main.go`, `go/cmd/feedback-triage/main.go`, `go/cmd/quality-gate/main.go`, `go/cmd/questions-gate/main.go`, `go/cmd/questions-gate/rerun_cap_test.go`, `go/cmd/tune-check/main.go`, `go/cmd/tune-check/main_test.go`, `go/cmd/worker/meta.go`, `go/internal/agentsvc/import.go`, `go/internal/agentsvc/service.go`, `go/internal/agentsvc/service_test.go`, `go/internal/agentsvc/spend_test.go`, `go/internal/auth/auth.go`, `go/internal/auth/auth_test.go`, `go/internal/decksvc/service.go`, `go/internal/decksvc/share.go`, `go/internal/decksvc/share_test.go`, `go/internal/evalrun/evalrun.go`, `go/internal/feedback/indexes_test.go`, `go/internal/gatekit/gatekit.go`, `go/internal/gatekit/spendcap_test.go`, `go/internal/meta/store.go`, `go/internal/meta/store_test.go`, `go/internal/quality/bars.go`, `go/internal/quality/bars_test.go`, `go/internal/quality/refit.go`, `go/internal/triage/loop_test.go`, `go/internal/triage/manifest.go`, `go/internal/users/closed_test.go`, `go/internal/users/emulator_test.go`, `go/internal/users/users.go`, `scripts/feedback-loop.sh`, `scripts/feedback-review.sh`, `web/apps/web/src/features/chat/build-stepper.tsx`, `web/apps/web/src/features/export/buy-list.test.ts`, `web/apps/web/src/features/export/buy-list.ts`, `web/apps/web/src/features/export/export-panel.test.tsx`, `web/apps/web/src/features/export/export-panel.tsx`, `web/apps/web/src/features/share/shared-deck-page.tsx`, `web/apps/web/src/features/workspace/deck-screen.test.tsx`, `web/apps/web/src/features/workspace/deck-screen.tsx`, `web/apps/web/src/lib/clipboard.test.ts`, `web/apps/web/src/lib/clipboard.ts`, `web/apps/web/src/styles/tokens.css`, and `web/apps/web/src/styles/tokens.test.ts`.

The protobuf contract and generated clients do not change. The model roles and prices do not change. Money guards apply to the new per-gate cap, and no paid target ran. I checked the API, Firestore, CI, Cloud Build, web, and documentation contracts. The changed `AGENTS.md` paid-target list matches the Makefile, `README.md`, `CLAUDE.md`, and `docs/reference/paid-targets.md`.

## Findings

No finding.

## Out of scope

REV-046 and REV-072 stay in later work, as the hand-off states. The owner must grant the custom rules-test role before the `deploy-web` trigger can use `web-deployer` (D-931).

## PR comments

- Gitar claimed that a spent cap marked a fully measured run stopped. The author fixed this in `a782dfa`. I checked the resolved thread and `TestASpentCapAfterTheLastConversationKeepsTheRunWhole`. The test confirms that the run stays whole and reruns stop.
- The latest Gitar dashboard reported the missing review record at `docs/reviews/pr-230.md`. That claim was correct before this push. The Gitar review of `a782dfa` is current, its check passed, and its only finding is resolved.

## Description edits

None.

## Verification

- `git diff --check d9faa00...a782dfa`: passed. No whitespace errors.
- `python3 docs/tools/review_gate.py --effective-head 230`: passed. Effective head is `a782dfa76427ba64b28d57e2b612af825015fdd5`.
- `make verify` on `a782dfa76427ba64b28d57e2b612af825015fdd5`: passed, exit 0. The log is `/tmp/decktome-pr230-verify.log`. The verify target notes that the emulator lane needs `make store-check` and `make gcs-check`, and govulncheck runs weekly.
- `gh pr checks 230` on `a782dfa76427ba64b28d57e2b612af825015fdd5`: all checks passed except `review-gate`, which reported the missing review record. The record is this review deliverable.
- Gitar check on `a782dfa76427ba64b28d57e2b612af825015fdd5`: completed with success. The latest dashboard edit is after the push. The only review thread is resolved.
- `make pr-check`: local invocation reports one branch mismatch because this review worktree uses detached HEAD. GitHub `pr-contract` passed for the same head.
- `make ste-check` and `make ref-check` on this record: passed, with zero findings.
- Paid targets: not run.

## Open questions and accepted risks

- D-931: the owner must grant the custom role before the web trigger switches to `web-deployer`.
- D-935: the clipboard path remains unverified on iPhone or WebKit.
- Paid gate evidence is absent by protocol. The roadmap gate does not require a paid run.

## Verdict

**Ready for owner merge.** This verdict applies to head `a782dfa76427ba64b28d57e2b612af825015fdd5`. The provider gate passes. I reviewed the changed contracts and paths, confirmed that the current Gitar finding is resolved, and ran `make verify` with a pass.
