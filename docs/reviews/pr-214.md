# Pull request 214 review

Date: 2026-09-23

## Identity

- PR: 214
- Roadmap item: PR-45b, F-165
- Target: `main`
- Base: `755b593e5edf7a84e33be6cebf37379eadbb7a3b`
- Merge base: `755b593e5edf7a84e33be6cebf37379eadbb7a3b`
- Head: `1df2e1eb6977b6a5aae2ac6f765434d11e04e58d`
- Branch: `f165-najeela-fixing`

## Provider gate

The hand-off states `Author provider: Claude Code`. The pull request author and the hand-off agree. This session runs on Codex. The providers differ, so the gate passes.

## Intended behavior and scope

The pull request keeps the mana half of the land cap through the total shortlist cut. It also lets the fixing fill trade a tapped nonbasic land when no basic land is spare (F-165, D-835). The gate is the fixing floor and the land count of the stored replay, plus the regression tests.

The diff has these paths. I inspected each path and its callers or contract:

- `docs/SESSION-HANDOFF.md`: cause, implementation, measurements, checks, and review state.
- `docs/decisions.md`: D-835 and the owner's choices for the total cut and land trade.
- `docs/design-roadmap.md`: F-165 correction, closure, and run 10 result. F-166 remains open.
- `docs/reference/eval/pr14a-bracket-gate-run10.jsonl`: stored machine-readable run data.
- `docs/reference/pr14a-bracket-gate-run10.md`: run metadata, deck, feature bands, judge result, and cost.
- `docs/reference/session-handoff-archive.md`: the prior resume section moved into the archive.
- `go/internal/candidates/candidates.go`: `capLands` marks the mana half, and `capByRole` keeps those candidates before its score cut.
- `go/internal/candidates/manahalf_test.go`: tests the mana-half mark, priority, and low total.
- `go/internal/candidates/power.go`: preserves marked candidates while it caps unpinned lands.
- `go/internal/generate/manapass.go`: swap callers, basic-land drops, tapped-utility drops, band-score guard, color guard, and fixing fill.
- `go/internal/generate/manapass_swap_test.go`: existing swap tests use the new explicit drop selector.
- `go/internal/generate/manapass_tapped_test.go`: tests tapped-land trade, spare-basic priority, colorless land retention, and deck size.

The protobuf contract, generated files, model layer, user data, provider calls, CI definitions, and deploy configuration do not change. The paid run in the record is author-provided evidence. I did not run a paid target.

## Findings

No finding.

## Out of scope

F-166, the bracket judge's reading of typal Commander decks, remains a later roadmap item.

## PR comments

Gitar's dashboard claims approval with no issues. The hand-off names approval of `1df2e1e`, and the Gitar check passed on that head. The review threads query returns no threads. An earlier CI dashboard reported the absent review record. That claim was accurate before this record existed.

## Description edits

None.

## Verification

- `git fetch origin`, `git status --short --branch`, and `gh pr view 214`: the PR is open, its branch points to `3f0c639f8c43fa7b4527b4cd760f85fb0fc2c563`, and the tree was clean before this record.
- `python3 docs/tools/review_gate.py --effective-head 214`: effective head `1df2e1eb6977b6a5aae2ac6f765434d11e04e58d`.
- `git diff --stat 755b593e5edf7a84e33be6cebf37379eadbb7a3b...HEAD` and each path diff: all 12 changed paths inspected.
- Gitar check and review threads on `1df2e1e`: pass, no finding, no open thread.
- CI on `1df2e1e`: `verify:go`, `verify:web`, `verify:proto`, `verify:docker`, `verify:emulator`, `verify:vuln`, `verify:eval`, `verify:shell`, `verify:skip`, and `pr-contract` passed. `verify:changes` skipped. `review-gate` failed only because this record was absent.
- `make verify` with the default Node 20: failed at web test startup with `ERR_REQUIRE_ESM`, as the hand-off predicts.
- `PATH=/Users/nate/.nvm/versions/node/v22.23.2/bin:$PATH make verify`: passed. Go race tests, web tests (39 files, 344 tests), lint, typecheck, build, STE, reference, lifecycle, context-budget, eval, and Docker checks passed.
- No paid target ran in this review.
- Push: pending.

## Open questions and accepted risks

- D-835 accepts that the total shortlist stays at 300 and now cuts 11 other cards. Run 10 has one finisher, against three in run 9. Its floor is one, and this one paid run does not show a trend.
- F-166 remains open for a later roadmap item.

## Verdict

**Ready for owner merge.** This verdict applies to head `1df2e1eb6977b6a5aae2ac6f765434d11e04e58d`.
The provider gate passes, the full diff and contracts hold, and the required local and CI checks pass. The `review-gate` check must rerun after this record reaches the branch.
