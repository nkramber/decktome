# Pull request 213 review

Date: 2026-09-23

## Identity

- PR: 213
- Roadmap item: PR-64
- Target: `main`
- Base: `ab60c43fbe552b0276444b754a7f96a721cfd514`
- Merge base: `ab60c43fbe552b0276444b754a7f96a721cfd514`
- Head: `7ecd1a0d5639afd5a0aa09885a0aa5a8b48d54dd`
- Branch: `codex-review`

## Provider gate

The hand-off names Claude Code as the author provider. The substantive pull request changes come from the Claude Code author session. The correction comes from the same session, and no other provider made a substantive change. This review uses Codex. The providers differ, so the gate passes.

## Intended behavior and scope

PR-64 adds the Codex review target, a review worktree, the three-strike stop, ruleset files, and a live ruleset check. The author session must run a current Gitar pass first. The review record must approve the effective head before merge (D-823 to D-834).

The merge base is `ab60c43fbe552b0276444b754a7f96a721cfd514`. The effective head is `7ecd1a0d5639afd5a0aa09885a0aa5a8b48d54dd`. I inspected all 28 changed paths. I traced the review runner through `review_gate.py`, its Make target, the Gitar skill, and the ruleset files. I checked the changed hand-off, decisions, roadmap entry, owner questions, and documentation contracts. The protobuf, model, legality, user data, web app, and deploy contracts do not apply because this change does not touch them.

## Findings

### P2-1: The Gitar gate reads only the first 100 review threads

Status: fixed in `7ecd1a0d5639afd5a0aa09885a0aa5a8b48d54dd`.

Open at: `a3b6c9904169b103dea790f21b27b7e651f09bb8`.

File: `docs/tools/codex_review.py:205-207`.

Trigger: a pull request has more than 100 review threads, and an unresolved thread appears after the first 100.

Expected: the target must read every review thread before it accepts the Gitar pass (D-832 and the `gitar-review` skill).

Actual: the GraphQL query requests `reviewThreads(first: 100)` and does not read `pageInfo` or request another page. `check_gitar` tests only those returned threads.

Consequence: the target can start a Codex review while an unanswered Gitar thread remains open. The author loop can then treat an incomplete Gitar pass as complete.

Correction: the query now uses `--paginate`, reads `pageInfo`, and checks every returned thread. The regression test puts an unresolved thread on page two.

Regression check: `test_an_open_thread_on_a_later_page_fails_the_pass` passed in the 198-test lifecycle suite. The test has 100 resolved threads on page one and one unresolved thread on page two.

## Earlier verdicts

- **Changes required.** The verdict applied to head `a3b6c9904169b103dea790f21b27b7e651f09bb8`. P2-1 blocked approval because the Gitar gate read one thread page.

## Out of scope

None.

## PR comments

- Gitar thread on the merge-head push walk: the author fixed it in `a3b6c99`. The thread is resolved. The first-parent walk and its merge test support the correction.
- Gitar thread on worktree cleanup after install failure: the author fixed it in `a3b6c99`. The thread is resolved. Cleanup runs after an install failure, and its regression test covers that path.
- P2-1: the author added pagination and a later-page regression test in `7ecd1a0`. The test and live query confirm that all review threads reach the gate.
- The current Gitar check passes. The saved thread query returns two threads, and both are resolved.
- The earlier Gitar CI comment reported a stale review record. That report matched the record before this update.

## Description edits

- Replaced the stale body status “No Gitar pass and no Codex review yet” with the current Gitar result and finding P2-1.

## Verification

- `git diff --stat ab60c43fbe552b0276444b754a7f96a721cfd514..7ecd1a0d5639afd5a0aa09885a0aa5a8b48d54dd`: 28 changed paths, 1,664 insertions, 82 deletions.
- `python3 docs/tools/review_gate.py --effective-head 213`: effective head `7ecd1a0d5639afd5a0aa09885a0aa5a8b48d54dd`.
- `make verify`: passed on `7ecd1a0d5639afd5a0aa09885a0aa5a8b48d54dd`, exit 0. The suite included 198 documentation tool tests, 344 web tests, Go race tests, lint, builds, and eval checks.
- `make ruleset-check`, `make ste-check`, `make ref-check`, `make context-budget`, and `git diff --check`: passed on `7ecd1a0d5639afd5a0aa09885a0aa5a8b48d54dd`.
- `gh pr checks 213` on `7ecd1a0`: Gitar, `pr-contract`, and every `verify` job passed. `review-gate` failed because the previous record had an old head and a `Changes required` verdict. It needs a new run after this metadata push.
- Push: pending the commit of this review record.
- No paid target ran.

## Open questions and accepted risks

- The review-gate check must pass on the metadata commit that holds this approval.

## Verdict

**Ready for owner merge.** This verdict applies to head `7ecd1a0d5639afd5a0aa09885a0aa5a8b48d54dd`.
The pagination correction and its regression test close the earlier finding. Local verification passes, and the current Gitar check has no open thread.
