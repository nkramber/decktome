# Pull request 212 review

Date: 2026-09-23

## Identity

- PR: 212
- Roadmap item: PR-63
- Target: `main`
- Base: `503dffd1fd56cea401e0d679946cf72c4174cd18`
- Merge base: `503dffd1fd56cea401e0d679946cf72c4174cd18`
- Head: `f5007982f95e6f61c9fa5753b3d3e28f441b258d`
- Branch: `review-gate`

## Provider gate

The hand-off names Claude Code as the author provider. This Codex session is the reviewer provider. The providers differ, so the gate passes. The pull request body and the hand-off contain no conflicting provider claim.

## Intended behavior and scope

PR-63 adds a required review record gate and skips six heavy CI jobs for eligible document-only changes. D-810 to D-822 and the PR-63 roadmap gate define the scope. The live `review-gate` workflow cannot run on this pull request because `pull_request_target` reads its workflow from `main`.

This repeat review covers the changes since `568ed53`. D-822 moves the roadmap mark before Gitar and adds a `pr-contract` rule for that mark. The effective head is `f5007982f95e6f61c9fa5753b3d3e28f441b258d`. The later commit `8afc6e8` changes only hand-off metadata.

I inspected every changed path from the merge base to the effective head:

- `.claude/skills/one-pr-one-session/SKILL.md`
- `.claude/skills/pr-review/SKILL.md`
- `.claude/skills/pr-review/references/answer-review.md`
- `.claude/skills/pr-review/references/commit-and-end.md`
- `.claude/skills/pr-review/references/project-contracts.md`
- `.claude/skills/pr-review/references/repeat-review.md`
- `.claude/skills/pr-review/references/review-record.md`
- `.github/pull_request_template.md`
- `.github/workflows/review-gate.yml`
- `.github/workflows/verify.yml`
- `AGENTS.md`
- `CLAUDE.md`
- `README.md`
- `docs/SESSION-HANDOFF.md`
- `docs/decisions.md`
- `docs/deploy-and-rollback.md`
- `docs/design-roadmap.md`
- `docs/reference/session-handoff-archive.md`
- `docs/reviews/pr-212-response.md`
- `docs/reviews/pr-212.md`
- `docs/tools/ci_skip.py`
- `docs/tools/review_gate.py`
- `docs/tools/test_ci_skip.py`
- `docs/tools/pr_check.py`
- `docs/tools/test_pr_check.py`

The list above names every path in the merge-base diff. The repeat diff since `568ed53` changes `.claude/skills/one-pr-one-session/SKILL.md`, `docs/decisions.md`, `docs/design-roadmap.md`, `docs/tools/pr_check.py`, and `docs/tools/test_pr_check.py`, plus the review record, response, and hand-off.
- `docs/tools/test_review_gate.py`

The review gate reads the changed paths, commit authors, review record, and effective head. A merge commit moves the effective head.

The CI skip uses the documentation allowlist. It reads its rule from the base commit. It runs heavy jobs when facts are absent or fail. The workflow passes event values through environment variables. It does not run a file from the pull request head.

The skill instructions agree with repository rules. They define reviewer and author flows. The decisions, roadmap gate, hand-off, and pull request documentation agree.

## Findings

### P2-1: The required record title fails the reference check

Status: fixed in `568ed5358bc09d953fb0fe149bb9fe0fcf3b506f`.

File: `.claude/skills/pr-review/references/review-record.md:41-44`.

Trigger: A reviewer follows the required title and places the GitHub number after `PR-`.

Expected: `make ref-check` accepts the review record. D-753 permits only register-defined `PR-` identifiers.

Actual: `make ref-check` reports the GitHub number as an undefined roadmap identifier.

Consequence: A reviewer who follows the template cannot pass `make verify` after the record exists.

Correction: The template now uses `# Pull request <number> review`. It keeps REF 1 unchanged.

Regression check: `test_a_filled_skeleton_passes_the_reference_check` fills the corrected template and checks it against the real registers. It passes. `make ref-check` also passes.

### P2-2: The roadmap mark accepts a longer pull request number

Status: open.

File: `docs/tools/pr_check.py:229-231`.

Trigger: PR #212 changes the roadmap and adds `✅ merged as #2120`.

Expected: D-822 requires the roadmap diff to add the mark for the exact pull request number.

Actual: `check_pr` searches for `✅ merged as #212` as a substring, so the `#2120` mark passes.

Consequence: A roadmap change can pass `pr-contract` with a mark for a different pull request.

Correction: Match the full pull request number, including a boundary after its final digit.

Regression check: A focused probe with number `212` and roadmap text `✅ merged as #2120` returned no errors. The current `test_pr_check.py` suite passes but has no prefix case.

## Out of scope

None.

## PR comments

- Gitar's merge-head finding was fixed in `46d47ca`. The author added parent handling and a regression test. The review thread remains resolved.
- Gitar's missing-`gh` finding was fixed in `46d47ca`. The author catches `OSError` and stubs the API call. The review thread remains resolved.
- Gitar's label-path finding was fixed in `cc386b4`. The author preserves the full changed-path list and adds a regression test. The review thread remains resolved.
- Gitar's newest dashboard update is `2026-09-23T05:29:48Z`. It approves with three closed findings. The current `Gitar` check passes, and no open review thread remains. The effective head is `f5007982f95e6f61c9fa5753b3d3e28f441b258d`.

## Description edits

None.

## Verification

- `python3 docs/tools/review_gate.py --effective-head 212` returned `f5007982f95e6f61c9fa5753b3d3e28f441b258d`.
- `python3 docs/tools/test_review_gate.py` passed, 30 tests. `python3 docs/tools/test_ci_skip.py` passed, 15 tests in the prior review.
- `python3 docs/tools/test_pr_check.py` passed, 26 tests. The suite does not cover a longer number prefix.
- `make pr-check` passed with 26 changed files and zero contract errors.
- `make ste-check` and `make ref-check` passed with zero findings.
- GitHub checks on branch tip `8afc6e8`: `pr-contract`, `verify:docker`, `verify:emulator`, `verify:eval`, `verify:go`, `verify:proto`, `verify:shell`, `verify:skip`, `verify:vuln`, `verify:web`, and Gitar passed. `verify:changes` skipped as expected.
- Gitar's dashboard update at `2026-09-23T05:29:48Z` follows effective head `f500798`. All three threads are resolved. Commit `8afc6e8` changes only hand-off metadata.
- The live `review-gate` check did not run. GitHub loads that workflow from `main`, which does not yet contain it. The focused gate tests passed.
- The full local `make verify` command did not run in this repeat review. The GitHub verify checks passed on branch tip `8afc6e8`.
- Push: pending.

## Open questions and accepted risks

- D-815: the `review-gate` check cannot run on this pull request. The author must disable the ruleset for this merge and enable it again after the merge. The next pull request gives the first live run of the check.

## Earlier verdicts

- **Changes required.** The review of `cc386b4950677bd09425f5a1febabce64e81fea6` found that the required record title failed REF 1.
- **Ready for owner merge.** The review of `568ed5358bc09d953fb0fe149bb9fe0fcf3b506f` found the earlier finding fixed and the required checks green.

## Verdict

**Changes required.** This review covers effective head `f5007982f95e6f61c9fa5753b3d3e28f441b258d`. The earlier findings are fixed. The roadmap mark check accepts a longer pull request number, which violates D-822.
