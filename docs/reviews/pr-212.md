# Pull request 212 review

Date: 2026-09-23

## Identity

- PR: 212
- Roadmap item: PR-63
- Target: `main`
- Base: `503dffd1fd56cea401e0d679946cf72c4174cd18`
- Merge base: `503dffd1fd56cea401e0d679946cf72c4174cd18`
- Head: `fdf73be58dde4dfb8e2166c35228fe259fce58ce`
- Branch: `review-gate`

## Provider gate

The hand-off names Claude Code as the author provider. This Codex session is the reviewer provider. The providers differ, so the gate passes. The pull request body and the hand-off contain no conflicting provider claim.

## Intended behavior and scope

PR-63 adds a required review record gate and skips six heavy CI jobs for eligible document-only changes. D-810 to D-822 and the PR-63 roadmap gate define the scope. The live `review-gate` workflow cannot run on this pull request because `pull_request_target` reads its workflow from `main`.

This repeat review covers the changes since `f500798`. The check now requires a digit boundary after the roadmap mark. The effective head is `fdf73be58dde4dfb8e2166c35228fe259fce58ce`. Later commits change only the hand-off and this review record.

I inspected every changed path from the merge base to the effective head:

- `.claude/skills/one-pr-one-session/SKILL.md`
- `.claude/skills/pr-review/SKILL.md`
- `.claude/skills/pr-review/references/answer-review.md`
- `.claude/skills/pr-review/references/commit-and-end.md`
- `.claude/skills/pr-review/references/project-contracts.md`
- `.claude/skills/pr-review/references/repeat-review.md`
- `.claude/skills/pr-review/references/review-record.md`
- `.github/pull_request_template.md`
- `.github/workflows/review-gate.yml`
- `.github/workflows/verify.yml`
- `AGENTS.md`
- `CLAUDE.md`
- `README.md`
- `docs/SESSION-HANDOFF.md`
- `docs/decisions.md`
- `docs/deploy-and-rollback.md`
- `docs/design-roadmap.md`
- `docs/reference/session-handoff-archive.md`
- `docs/reviews/pr-212-response.md`
- `docs/reviews/pr-212.md`
- `docs/tools/ci_skip.py`
- `docs/tools/pr_check.py`
- `docs/tools/review_gate.py`
- `docs/tools/test_ci_skip.py`
- `docs/tools/test_pr_check.py`
- `docs/tools/test_review_gate.py`

The list above names every path in the merge-base diff. The repeat diff since `f500798` changes `docs/SESSION-HANDOFF.md`, `docs/reviews/pr-212-response.md`, `docs/tools/pr_check.py`, and `docs/tools/test_pr_check.py`.

The review gate reads the changed paths, commit authors, review record, and effective head. A merge commit moves the effective head.

The CI skip uses the documentation allowlist. It reads its rule from the base commit. It runs heavy jobs when facts are absent or fail. The workflow passes event values through environment variables. It does not run a file from the pull request head.

The skill instructions agree with repository rules. They define reviewer and author flows. The decisions, roadmap gate, hand-off, and pull request documentation agree.

## Findings

### P2-1: The required record title fails the reference check

Status: fixed in `568ed5358bc09d953fb0fe149bb9fe0fcf3b506f`.

File: `.claude/skills/pr-review/references/review-record.md:41-44`.

Trigger: A reviewer follows the required title and places the GitHub number after `PR-`.

Expected: `make ref-check` accepts the review record. D-753 permits only register-defined `PR-` identifiers.

Actual: `make ref-check` reports the GitHub number as an undefined roadmap identifier.

Consequence: A reviewer who follows the template cannot pass `make verify` after the record exists.

Correction: The template now uses `# Pull request <number> review`. It keeps REF 1 unchanged.

Regression check: `test_a_filled_skeleton_passes_the_reference_check` fills the corrected template and checks it against the real registers. It passes. `make ref-check` also passes.

### P2-2: The roadmap mark accepts a longer pull request number

Status: fixed in `fdf73be58dde4dfb8e2166c35228fe259fce58ce`.

File: `docs/tools/pr_check.py:230-231`.

Trigger: PR #212 changes the roadmap and adds `✅ merged as #2120`.

Expected: D-822 requires the roadmap diff to add the mark for the exact pull request number.

Actual: Before the fix, `check_pr` searched for `✅ merged as #212` as a substring, so the `#2120` mark passed.

Consequence: A roadmap change passed `pr-contract` with a mark for a different pull request.

Correction: `check_pr` now requires a non-digit after the pull request number, or the end of the text.

Regression check: `test_the_mark_of_a_longer_number_fails` and `test_the_mark_at_the_end_of_the_text_passes` pass in the 28-test suite. Against the old checker from `f500798`, both `#2120` cases fail the regression test, and the end-of-text case passes.

## Out of scope

None.

## PR comments

- Gitar's merge-head finding was fixed in `46d47ca`. The author added parent handling and a regression test. The thread is resolved.
- Gitar's missing-`gh` finding was fixed in `46d47ca`. The author catches `OSError` and stubs the API call. The thread is resolved.
- Gitar's label-path finding was fixed in `cc386b4`. The author preserves the full changed-path list and adds a regression test. The thread is resolved.
- Gitar said the path list omitted `docs/tools/test_review_gate.py` and was out of order. The owner said the reviewer will correct the record. I verified the defect, restored the path in order, and moved the summary after the list. The thread is resolved.
- Gitar's review passed after the first review-record push at `787aa9e`, with four findings closed. GitHub reports all four threads resolved.

## Description edits

None.

## Verification

- `python3 docs/tools/review_gate.py --effective-head 212` returned `fdf73be58dde4dfb8e2166c35228fe259fce58ce`.
- `python3 docs/tools/test_pr_check.py` passed, 28 tests. Both `#2120` regression cases failed against the old checker from `f500798`.
- `python3 docs/tools/test_review_gate.py` passed, 30 tests. `python3 docs/tools/test_ci_skip.py` passed, 15 tests.
- `make verify` passed in the original checkout at branch tip `9731d24`. It passed proto checks, build, lint, 133 tool tests, Go race tests, 344 web tests, eval check, vulnerability scan, and Docker builds.
- The first `make verify` attempt in the worktree stopped at `ref-check`. The `.git` pointer file made `.git/decktome-session-bind` appear as an unresolved path. The same check passed in the original checkout.
- GitHub checks on branch tip `787aa9e` passed: `pr-contract`, all eight verify jobs, and Gitar. `verify:changes` skipped. `verify:skip` ran every job because base `503dffd` does not contain `ci_skip.py` yet.
- The live `review-gate` check did not run. GitHub loads that workflow from `main`, which does not yet contain it. Focused gate tests passed.
- Push: review-record commit `787aa9e` is on `origin/review-gate`, verified with `gh pr view`. The effective head remains `fdf73be58dde4dfb8e2166c35228fe259fce58ce`.

## Open questions and accepted risks

- D-815: the `review-gate` check cannot run on this pull request. The author must disable the ruleset for this merge and enable it again after the merge. The next pull request gives the first live run of the check.

## Earlier verdicts

- **Changes required.** The review of `cc386b4950677bd09425f5a1febabce64e81fea6` found that the required record title failed REF 1.
- **Ready for owner merge.** The review of `568ed5358bc09d953fb0fe149bb9fe0fcf3b506f` found P2-1 fixed and the required checks green.
- **Changes required.** The review of `f5007982f95e6f61c9fa5753b3d3e28f441b258d` found P2-2 open because the roadmap mark accepted a longer number.

## Verdict

**Ready for owner merge.** This review covers effective head `fdf73be58dde4dfb8e2166c35228fe259fce58ce`. The earlier findings are fixed. The regression tests reject a longer pull request number, and the required checks pass.
