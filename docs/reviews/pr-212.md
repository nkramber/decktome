# Pull request 212 review

Date: 2026-09-23

## Identity

- PR: 212
- Roadmap item: PR-63
- Target: `main`
- Base: `503dffd1fd56cea401e0d679946cf72c4174cd18`
- Merge base: `503dffd1fd56cea401e0d679946cf72c4174cd18`
- Head: `568ed5358bc09d953fb0fe149bb9fe0fcf3b506f`
- Branch: `review-gate`

## Provider gate

The hand-off names Claude Code as the author provider. This Codex session is the reviewer provider. The providers differ, so the gate passes. The pull request body and the hand-off contain no conflicting provider claim.

## Intended behavior and scope

PR-63 adds a required review record gate and skips six heavy CI jobs for eligible document-only changes. D-810 to D-821 and the PR-63 roadmap gate define the scope. The live `review-gate` workflow cannot run on this pull request because `pull_request_target` reads its workflow from `main`.

The repeat review covers the correction since `cc386b4`. It changes the review-record template, adds a regression test for a filled template, and records the response. The effective head is `568ed5358bc09d953fb0fe149bb9fe0fcf3b506f`. Later commits change only the review record, response, and hand-off metadata.

I inspected every changed path from the merge base to the effective head:

- `.claude/skills/one-pr-one-session/SKILL.md`
- `.claude/skills/pr-review/SKILL.md`
- `.claude/skills/pr-review/references/answer-review.md`
- `.claude/skills/pr-review/references/commit-and-end.md`
- `.claude/skills/pr-review/references/project-contracts.md`
- `.claude/skills/pr-review/references/repeat-review.md`
- `.claude/skills/pr-review/references/review-record.md`
- `.github/pull_request_template.md`
- `.github/workflows/review-gate.yml`
- `.github/workflows/verify.yml`
- `AGENTS.md`
- `CLAUDE.md`
- `README.md`
- `docs/SESSION-HANDOFF.md`
- `docs/decisions.md`
- `docs/deploy-and-rollback.md`
- `docs/design-roadmap.md`
- `docs/reference/session-handoff-archive.md`
- `docs/tools/ci_skip.py`
- `docs/tools/review_gate.py`
- `docs/tools/test_ci_skip.py`
- `docs/tools/test_review_gate.py`

The review gate reads the changed paths, commit authors, review record, and effective head. A merge commit moves the effective head.

The CI skip uses the documentation allowlist. It reads its rule from the base commit. It runs heavy jobs when facts are absent or fail. The workflow passes event values through environment variables. It does not run a file from the pull request head.

The skill instructions agree with repository rules. They define reviewer and author flows. The decisions, roadmap gate, hand-off, and pull request documentation agree.

## Findings

### P2-1: The required record title fails the reference check

Status: fixed in `568ed53`.

File: `.claude/skills/pr-review/references/review-record.md:41-44`.

Trigger: A reviewer follows the required title and places the GitHub number after `PR-`.

Expected: `make ref-check` accepts the review record. D-753 permits only register-defined `PR-` identifiers.

Actual: `make ref-check` reports the GitHub number as an undefined roadmap identifier.

Consequence: A reviewer who follows the template cannot pass `make verify` after the record exists.

Correction: The template now uses `# Pull request <number> review`. It keeps REF 1 unchanged.

Regression check: `test_a_filled_skeleton_passes_the_reference_check` fills the corrected template and checks it against the real registers. It passes. `make ref-check` also passes.

## Out of scope

None.

## PR comments

- Gitar finding: the old code left the recorded head stale after a merge. The author added merge-parent handling and a regression test in `46d47ca`. I verified the code and test.
- Gitar finding: the old CI-skip test called the real `gh` binary. The author catches `OSError`, stubs the API call, and tests the unknown-fact path in `46d47ca`. I verified the code and test.
- Gitar finding: the old label check read only the newest commit paths. The author uses a separate `commit_files` variable and adds a code-then-docs regression test in `cc386b4`. I verified the code and test.

Gitar updated its dashboard at 04:24:15 UTC. Its check on branch tip `ff2fd26` passed at 04:24:16 UTC. All three review threads show resolved. The dashboard approves the pull request with no open finding. The effective head is `568ed5358bc09d953fb0fe149bb9fe0fcf3b506f`.

## Description edits

None.

## Verification

- `python3 docs/tools/review_gate.py --effective-head 212` returned `568ed5358bc09d953fb0fe149bb9fe0fcf3b506f`.
- `python3 docs/tools/test_review_gate.py` passed, 30 tests. `python3 docs/tools/test_ci_skip.py` passed, 15 tests.
- `make ste-check` and `make ref-check` passed with zero findings.
- `make verify` failed with Node 20.17.0. Vitest workers returned `ERR_REQUIRE_ESM`.
- `PATH=/Users/nate/.nvm/versions/node/v22.23.2/bin:$PATH make verify` passed on `ff2fd26`, exit 0. It passed the Go checks, 128 lifecycle tests, 344 web tests, web build, eval check, and Docker builds.
- GitHub checks on branch tip `ff2fd26`: `pr-contract`, `verify:docker`, `verify:emulator`, `verify:eval`, `verify:go`, `verify:proto`, `verify:shell`, `verify:skip`, `verify:vuln`, `verify:web`, and Gitar passed. `verify:changes` skipped as expected.
- Gitar's check on branch tip `ff2fd26` passed at 04:24:16 UTC. Its dashboard edit time is 04:24:15 UTC, and its three review threads are resolved. Branch tip `ff2fd26` changes only metadata after effective head `568ed53`.
- The live `review-gate` check did not run. GitHub loads that workflow from `main`, which does not yet contain it. The local effective-head command and gate tests passed.
- Push: `ff2fd26` is the head of `origin/review-gate`, verified with `gh pr view`.

## Open questions and accepted risks

- D-815: the `review-gate` check cannot run on this pull request. The author must disable the ruleset for this merge and enable it again after the merge. The next pull request gives the first live run of the check.

## Earlier verdicts

- **Changes required.** The review of `cc386b4950677bd09425f5a1febabce64e81fea6` found that the required record title failed REF 1.

## Verdict

**Ready for owner merge.** This review covers effective head `568ed5358bc09d953fb0fe149bb9fe0fcf3b506f`. The earlier finding is fixed, the focused checks pass, and Gitar approves with no open thread. D-815 records the expected bootstrap exception for the live `review-gate` check.
