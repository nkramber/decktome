# Pull request 224 review

Date: 2026-09-24

## Identity

- PR: 224
- Roadmap item: PR-74
- Target: `main`
- Base: `d4b8daf4120669ba763c1fa7b5202768d91c7c76`
- Merge base: `d4b8daf4120669ba763c1fa7b5202768d91c7c76`
- Head: `00a330fd63fb112e40b14a29e179985a15d3d69f`
- Branch: `plain-upload-page`

## Provider gate

The hand-off names Claude Code as the author provider. This session runs Codex. The providers differ, so the gate passes.

## Intended behavior and scope

PR-74 removes service names from upload surfaces and files an IMPORT thumbs down when a file does not read or has parse faults. D-882 to D-891 set its contract. I inspected all 49 changed paths in the full diff.

The paths cover the README, hand-off, decisions, roadmap, feedback prompt, archive, Go code and tests, protobuf files, web files, loop script, and lint config. The generated files match the proto change. No changed path was left unread.

The provider gate passes. The upload and deck import flows keep the file, report recognized parse faults, and store the server's re-read fault. The report stores no whole file. The triage writes a parser fixture that fails its parser test until the parser reads it. No paid target ran.

## Findings

### P2-1: The service name loses its entered whitespace

Status: open.

Open at: `00a330fd63fb112e40b14a29e179985a15d3d69f`.

File: `web/apps/web/src/features/feedback/report-import.tsx:55`, `go/internal/feedbacksvc/service.go:173`.

Trigger: A reader enters a service name with leading or trailing whitespace and sends the report.

Expected: D-883 says to keep the reader's words as entered, with no correction.

Actual: The web form calls `service.trim()`. The server also calls `strings.TrimSpace` before storage.

Consequence: The report does not retain the exact free-text answer that the owner chose to preserve.

Correction: Send and store the entered value as is. Add a test for leading and trailing whitespace.

Regression check: No exact-text test covers boundary whitespace. `make verify` passes, but does not prove this contract.

## Out of scope

None.

## PR comments

No review threads or submitted reviews exist. The Gitar pause applies, and no Gitar claim exists to verify.

## Description edits

None.

## Verification

- `git diff --check origin/main...HEAD`: passed.
- `make pr-check`: passed.
- `PATH=/Users/nate/.nvm/versions/node/v22.23.2/bin:$PATH make verify`: passed. Go race tests, 41 web test files and 363 web tests, lint, type checks, builds, proto checks, and document gates passed. The eval check marked the revise suite not evaluated because it has no newer whole run.
- `gh pr checks 224`: code, shell, proto, emulator, Docker, eval, web, vulnerability, and PR contract checks passed. `verify:changes` skipped. `review-gate` failed because the review record was absent before this push.
- Gitar review: not required during the D-838 pause. No Gitar threads or claims exist.
- Paid targets: not run.
- Push: pending.

## Open questions and accepted risks

None.

## Verdict

**Changes required.** This verdict applies to head `00a330fd63fb112e40b14a29e179985a15d3d69f`.
The report changes the free-text answer that D-883 says to preserve. Keep the entered text and test that behavior before approval.
