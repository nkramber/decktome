# Decisions

Every owner decision, with date. This file is the source of truth. The design doc cites it by D-number. Do not re-ask a question that has a row here. When a later decision changes an earlier one, keep both rows and mark the old one "superseded".

Note on the numbers (2026-08-26): the fixer of the loop wrote D-172 to D-176 on branch `auto-tune/20260826-164931`. The owner dropped that branch (D-204). Those five numbers are retired, and nothing reuses them.
| # | Date | Question | Decision |
|---|---|---|---|
| D-1 | 2026-08-23 | LLM provider | Provider-agnostic role-to-model layer. The provider is a config choice. Model: connector-syncer `llm_roles.py`. |
| D-2 (amended by D-37) | 2026-08-23 | Card pool | Build from the user's owned cards first. Suggest upgrades and acquisitions separately. |
| D-3 (extended by D-265) | 2026-08-23 | "60-card anything goes" | Not a fixed format. The agent asks the user what it means. Flexibility is the principle. The owner personally means "any card, no ban list". |
| D-4 | 2026-08-23 | Audience and deployment | Multi-user web app on GCP. Mirror the Wallabee stack: Cloud Run, Firestore, GCS. |
| D-5 | 2026-08-23 | Meta data sources | Allowed: official MTGO decklists, MTGGoldfish, MTGTop8, Aetherhub, EDHREC. **2026-08-23 update (OQ-10): the legal check passed. Deck data from any of these sites may be used.** |
| D-6 | 2026-08-23 | Card images | Hotlink Scryfall image URIs. Show artist and copyright. Do not crop, skew, or watermark. |
| D-7 | 2026-08-23 | RPC style | Connect-RPC with buf. One `.proto` contract. Generated Go and TypeScript clients. Server streaming for chat. |
| D-8 | 2026-08-23 | Power scale | Commander: official brackets 1-5. 60-card: three steps, casual / FNM / tournament-meta. |
| D-9 (amended by D-267) | 2026-08-23 | Local test environment | The full stack, UI included, must run locally. Firestore emulator confirmed feasible. Local testing has priority over hosting. |
| D-10 | 2026-08-23 | Docker | The owner will install Docker. The install is a roadmap item. |
| D-11 | 2026-08-23 | Auth | Firebase Auth, with the Auth emulator in local mode. |
| D-12 | 2026-08-23 | Output language | All docs, skills, and agent files in ASD-STE100. |
| D-13 (superseded by D-32) | 2026-08-23 | Write scope | This session writes only in `docs/` and `.claude/` of this repo. All other repos are read-only. |
| D-14 | 2026-08-23 | Question cadence | Ask questions as they come up, in small batches. Never batch them for the end. Never hesitate to ask, or to call out an inconsistency. |
| D-15 | 2026-08-23 | Deck export targets (OQ-1) | ManaBox text first. Other formats later. |
| D-16 | 2026-08-23 | Collection storage (OQ-2) | Store the full collection (name, printing, quantity, finish, condition, rarity, set, collector number). A hash can not recover any of this. The hash is kept only to detect an identical re-upload. |
| D-17 | 2026-08-23 | Prices (OQ-3) | USD. Source: Scryfall daily near-mint market estimate, 7-day rolling average, outliers rejected. Show the lowest price across all legal printings and finishes. Label it "NM market estimate". Scryfall has no lightly-played tier. A condition-tiered source (TCGplayer API) is a later option, not planned. |
| D-18 (amended by D-256) | 2026-08-23 | Determinism and variance (OQ-4) | First answer: identical collection plus identical prompt should ideally give the same deck, and divergence through follow-up questions is acceptable. **Second answer, same day, supersedes it: keep random variance. Identical-input determinism is not a requirement.** The seed is still stored with each deck so a build can be reproduced on request. |
| D-19 | 2026-08-23 | Card explanations (OQ-5) | Yes. One summary paragraph on the deck's style and purpose. One line per card. |
| D-20 | 2026-08-23 | Play simulator (OQ-6) | Later, not at launch. Stays in Phase 5. |
| D-21 (extended by D-38) | 2026-08-23 | Day-one provider (OQ-7) | OpenAI on day one, behind the provider-agnostic role layer. |
| D-22 | 2026-08-23 | Judge model (OQ-8) | A different provider from the generator. The exact provider is chosen at implementation time. |
| D-23 | 2026-08-23 | Languages (OQ-9) | English only. Non-English rows in an import are reported to the user, not silently dropped. |
| D-24 | 2026-08-23 | GCP projects (OQ-11) | `mtg-dev` and `mtg-prod`. No domain name and no hosting yet. Local testing first. |
| D-25 | 2026-08-23 | Question source (OQ-12) | Both. Draw from the fixed catalog whenever possible. Add a gap score that detects when no catalog question fits. Allow model-invented questions when the gap score says one is needed. The catalog is a living artifact: extend, change, or reduce it from user feedback and evals. |
| D-26 | 2026-08-23 | Price outlier rule (OQ-13) | Yes: drop a day whose price is more than 2x the 7-day median. |
| D-27 | 2026-08-23 | Gap-score threshold (OQ-14) | Needs evals, gated on manual testing by the owner. The owner uses the product and scores each model-invented question against the catalog questions that could have been asked instead. The threshold is set from those scores. This is a complex, first-class part of the deliverable. |
| D-28 | 2026-08-23 | Catalog change approval (OQ-15) | The owner approves catalog changes to start. |
| D-29 | 2026-08-23 | Stale decks on a rule change (OQ-16) | Show a banner on a stored deck that now contains a card illegal in its format. Give a "rerun" button. The rerun rebuilds according to the nature of the change: which cards were banned and how deeply they affect the deck. |
| D-30 | 2026-08-23 | Sample ManaBox exports (OQ-17) | The owner will provide them later. |
| D-31 | 2026-08-23 | Roadmap approval | The owner approved `docs/design-roadmap.md` draft 1 (passes 1-3). PR-0a may start. |
| D-32 | 2026-08-23 | Write scope (supersedes D-13) | Writes are allowed across this repo. Other repos stay read-only. |
| D-33 | 2026-08-23 | Go module and layout | Module `github.com/nkramber/mtg-deck-builder`. One Go module at `go/` with `cmd/` and `internal/`. |
| D-34 | 2026-08-23 | CI and PR flow | GitHub Actions. PRs into `main`, squash merge, branch protection. `verify:*` matrix with a fan-in job. |
| D-35 | 2026-08-23 | Scaffold toolchain pins (PR-0a) | Go 1.26.4. buf 1.72.0, protoc-gen-go 1.36.12, protoc-gen-connect-go 1.20.0 as `go tool` directives, with buf built into `.bin/`. Node 20.17.0 (installed version, moved to 22.12 LTS by D-52), pnpm 9.2.0. Vite 7.3.6 (Vite 8 needs rolldown native bindings that pnpm 9.2 did not install). React 19.2.8. Vite dev port 5180 (5173 is used by wallabee-ui). golangci-lint v2.6.2. |
| D-36 | 2026-08-24 | Local port map (PR-0c) | This machine also runs the Wallabee stack (flash :8080, Firestore emulator :8181, emulator UI :4000, vite :5173). Our stack uses its own block: API :8090 under `make dev` (:8080 default in production images), Firestore emulator :8281, Auth emulator :9199, emulator UI :4100, hub :4490, logging :4590, fake-gcs-server :4443, web :5180. |
| D-37 | 2026-08-24 | Collection is optional (amends D-2) | The app must build decks with zero user library. A user with a library can deselect it ("use only cards in my library" off) to get a truly optimized deck. Pool modes: owned-first (default when a library exists), owned-only, any-card (default when no library exists, and the "optimized" mode). When a library exists but the user picks any-card, the deck still marks the cards the user happens to own, as information only. The proto already carries all three modes (`PoolRule`, PR-1). |
| D-38 | 2026-08-24 | Judge provider (implements D-22) | Anthropic serves the `judge` role. OpenAI serves the other four. Config validation refuses a judge on the generator's provider. |
| D-39 | 2026-08-24 | Baseline models (PR-10) | `classify` and `ask` on `gpt-5.6-luna`. `generate` and `repair` on `gpt-5.6-terra`. `judge` on `claude-sonnet-5`. This is the baseline for the PR-15 bake-off. The owner first picked `sol` and `claude-opus-5`, then the same day chose the middle tier as the baseline. |
| D-40 | 2026-08-24 | Provider SDKs | The official Go SDKs: `openai-go/v3` and `anthropic-sdk-go`. Their retries are off. The role layer owns the budget. |
| D-41 | 2026-08-24 | API key storage | Keys live in `.env` at the repo root (gitignored by the `.env.*` rule). `make dev` and `make test-smoke` source it. Never commit a key. Production sets `LLM_REQUIRE_KEYS=1` so a missing key is fatal, not a silent fake. |
| D-42 (amended by D-264) | 2026-08-24 | Audit answers (docs) | The roadmap is an approved, living document. A ✅ means the code is merged on `main`. `CLAUDE.md` is the entry point and leads into `SESSION-HANDOFF.md`. Research notes under `docs/reference/` are frozen history when they are out of date, with a banner. The STE script stays as it is (no passive-voice check). |
| D-43 | 2026-08-24 | Fixture set (OQ-17, closes D-30) | The owner's own export is the PR-4 fixture. Add public anonymized ManaBox exports when found (OQ-20). |
| D-44 | 2026-08-24 | Token rows in an import | A row whose printing is a token, emblem, or art card is reported as `NOT_PLAYABLE`. It is never resolved by name. |
| D-45 | 2026-08-24 | Judge effort | `judge` on `claude-sonnet-5` keeps thinking on at effort medium. Thinking tokens are counted. |
| D-46 | 2026-08-24 | Contract amendment | One PR-1b proto change carries every field PR-6 to PR-9 need. `buf breaking` runs in CI from now on. The PR-6 candidate list stays Go-internal until the UI needs it. |
| D-47 | 2026-08-24 | Announcement coverage (F-23) | A legality diff between snapshots decides coverage. The calendar only sets the poll cadence. |
| D-48 | 2026-08-24 | Worker shape | The worker is a Cloud Run job (`-once`) under Cloud Scheduler: hourly, and every 15 minutes on announcement days. The loop mode is for `make dev` only. |
| D-49 | 2026-08-24 | Partner variants (F-22) | `Card.partner_text` holds the text after "Partner—". Two Partner commanders need equal text. No new enum value. |
| D-50 (amended by D-272) | 2026-08-24 | Vehicle and Spacecraft commanders | Supported now per CR 903.3 (2026-08-07). |
| D-51 | 2026-08-24 | LLM keys | Keys are required by default. `LLM_REQUIRE_KEYS=0` opts out for local dev and tests. A `fake` provider is refused when keys are required. |
| D-52 | 2026-08-24 | Node version (amends D-35) | The Node 22 LTS line. Pinned at 22.23.2 (the current patch on 2026-08-24). Engines `>=22.12.0 <23`. Vite 7.3.6 needs 20.19 or 22.12 and up. jsdom 29 and up need 22.13 and up. |
| D-53 | 2026-08-24 | Golden gate size | At least 30 good and 30 bad decks, enforced by a test. The audit pass landed 41 and 53. |
| D-54 | 2026-08-24 | Corpus ban bullets | Keep the dated list and refresh it after each announcement. |
| D-55 | 2026-08-24 | Owned-only basics | Basic lands stay exempt from the ownership check in owned-only mode. The exception is recorded in the corpus checklist. |
| D-56 | 2026-08-24 | CI path filters | Struck. A skipped required check blocks a merge under branch protection. Every job runs on every PR. |
| D-57 | 2026-08-24 | Re-upload name | An identical re-upload with a new name takes the new name. |
| D-58 | 2026-08-24 | Collection entry fields | `language` and `set_name` are stored per entry. |
| D-59 | 2026-08-24 | Price history | Deferred to I-2. `price_as_of` is set now. The proto comment states today's rule and the D-17 target. |
| D-60 | 2026-08-24 | Small calls | Commander land guide is 34 to 38 (corpus wins, engine aligned). Commanders count toward the Game Changer limit, marked unverified. Checklist items on curve, roles, and plan are PR-8 model-side. ASD-STE100 Issue 8 stays the citation, with an Issue 9 note. A model override with no price row logs a startup warning. A canceled or expired caller context is `ClassBudget`. |
| D-61 | 2026-08-24 | Worker schedule and M-2 record | One Cloud Scheduler cron runs the worker job with `-once` every 15 minutes. The worker exits early when the latest snapshot is under an hour old and no announcement window is open. On other ticks it makes one small catalog GET and downloads only when Scryfall has a newer file. After a legality diff the worker writes `scryfall/<version>/legality_diff.json` so the M-2 lag outlives log retention. |
| D-62 | 2026-08-24 | Candidate scoring (PR-6) | Theme signals split into payoffs and enablers. A payoff tag (`lifegain-matters`) or needle ("whenever you gain life") outweighs an enabler tag (`lifegain`) or keyword (Lifelink). One tag per kind counts. A keyword covers its own name as a text needle. The owned-first gate view merges owned cards and upgrades by score. |
| D-63 | 2026-08-24 | Thin theme (PR-6, PR-7) | When an owned mode finds under 30 on-theme owned cards, the builder sets `ThinTheme`. PR-7 then asks the pool-mode question again: build owned-first with a buy list, or switch to any-card. |
| D-64 | 2026-08-24 | On-theme rule for a conditional line (PR-6 gate) | A card counts as on theme when it holds a real theme line, even behind a condition. Examples: Cosmic Intervention in blink, Grapeshot in burn. The Review block still names each borderline card and gives the total under the strict reading. |
| D-65 | 2026-08-24 | PR-6 gate failed, fix and rerun | The first run scores 12 of 20, and the bar is 18. Fix the three engine defects, repair `themes.json` for the eight failed prompts, then rerun `make candidates-review`. The first run keeps its file `docs/reference/pr6-candidate-review.md` with every score and Review block. The second run writes a new file, `docs/reference/pr6-candidate-review-run2.md`. A rerun never overwrites a scored document (owner directive, 2026-08-24). |
| D-66 | 2026-08-24 | Invented-question rubric (OQ-19, implements D-27) | The owner scores six fields per invented question: `catalog_enough` (yes/no/unsure), `invented_better` (worse/same/better), `right_slot` (yes/no), `filled_slot` (yes/partly/no, auto-filled), `faults` (mandatory on every row that is not clean: duplicate, two questions in one, jargon, assumes an answer, unanswerable, out of scope), and `catalog_action` (none/add/reword). Three-point scales only, because a five-point scale drifts between sessions. An invention is warranted when `catalog_enough` is no and `invented_better` is not worse. `unsure` rows are reported and excluded from the fit. The D-27 threshold is the gap score that best separates warranted from unwarranted, with a precision floor: at least 80% of the inventions the threshold allows must be warranted. Every tenth item repeats an earlier one, to measure self-consistency. The rubric is versioned, and a change to it invalidates the fit. |
| D-67 | 2026-08-24 | Card-pool question timing (PR-7, amends D-63) | The agent does not ask the card-pool question on turn 1. It fills format, colors, and theme first, because PR-6 needs those three before it can count on-theme owned cards. The agent then asks the pool question once, and adds the real count when `ThinTheme` is set. This removes the near-repeat that D-63 caused. Source: the PR-7 dogfood runs of 2026-08-24. |
| D-68 (superseded by D-241) | 2026-08-24 | Slot freeze at the start of a run (PR-7) | A slot stays open through the question phase. A later answer replaces an earlier one, and the agent states the change. When a build run starts, every slot freezes with the deck. The user can not swap the pool mode, the format, the commander, the colors, or the power level inside a run. A change after that point starts a new run, which I-1 covers. |
| D-69 | 2026-08-24 | Gap score is its own call, and its first threshold (PR-7, implements D-27) | The gap score runs as a second `classify` call, not inside the `ask` call. A model that phrases a question must not also rate its own wording. A turn therefore costs three calls: classify, score, ask. The provisional fit threshold is 0.35, which is conservative: the agent keeps the catalog question unless the score is under it. M-5 replaces the number from at least 50 scored questions (D-66). |
| D-70 | 2026-08-24 | Locked-card row (PR-7) | The locked row fires only for a named card that is not the commander. The old row fired on any named card. The live run of 2026-08-24 asked the user to keep or cut a list that held only their commander. The row also drops the list assumption: "Must the deck keep {locked}, or may I cut a card that does not fit the plan?" The state keeps the commander names and the locked names apart. |
| D-71 | 2026-08-24 | Commander row split (PR-7) | The commander row asked two things at once, and it scored 0.05 in the live run. The base row now asks "Do you have a commander in mind, or should I suggest one?" The pick row carries the three names. The classify schema gained `facts.wants_suggestion`, which sets the `Suggested` fact that the pick row needs. A name the user gives as the commander closes all three commander rows. |
| D-72 | 2026-08-24 | Prompt cache key (PR-7) | Every model call of one session carries the session id as `llm.Request.CacheKey`. It saves nothing today, because a call is near 470 tokens and OpenAI caches above 1,024. PR-8 owns the caching lever. Open point: the stable prefix is the role instructions, which every session shares, so PR-8 must measure a role-scoped key against the session key. |
| D-73 (amended by D-147 and D-163) | 2026-08-24 | A "none" answer to the commander pick (PR-7) | The pick row repeats until the user takes a commander. It is the one exception to the no-repeat rule. Every round names three commanders the agent did not offer before. The owner chose this over a reworded row and over a cap of two asks. |
| D-74 | 2026-08-24 | Session state storage (PR-7) | Firestore holds the proto `Session` and a private state document for each session. The proto stays the public contract that `GetSession` returns. The private document holds the asked rows, the user's words, the card names, and the trigger facts. The owner chose this over a rebuild from `Session.turns` and over memory-only state. |
| D-75 | 2026-08-24 | A commander fills the color slot (PR-7) | The color identity of the commander is the color identity of the deck. The agent never asks for colors when a commander is set, so nothing else could close that key. The card-pool question waits for the colors (D-67), and it therefore waited forever. PR-8 reads the identity from the card. |
| D-76 | 2026-08-24 | The classifier may close an open question (PR-7) | The classifier is offered two groups of keys: a key the agent asked and did not close, and a key the planner would ask next. The no-repeat rule drops an asked row from the plan, so the first group was missing and no advisory key could ever close. The live-run fix of 2026-08-24 holds: a key the user never saw still closes nothing. |
| D-77 | 2026-08-24 | The service owns the facts the classifier can not answer (PR-7) | `AfterBuild` comes from the stored deck ids. `ThinTheme` and the on-theme owned count come from one PR-6 build, which runs once per theme. `CommanderNotOwned` compares the named commander against the collection. Each fact gates a catalog row, and each row was dead without it. `WeakCommanderPool` stays open: it needs a score bar that PR-8 sets. |
| D-78 | 2026-08-25 | "Casual" alone is not a house rule (amends D-3) | The word "casual" no longer triggers the house-rules row on its own. It fires next to "anything goes", "kitchen table", "proxy", or "no ban list". Evidence: the gate run of 2026-08-25 asked a parent about house rules for a child's deck, and the parent answered "casual means low power, not a house format". The word now routes to power. |
| D-79 | 2026-08-25 | A competitive answer is a theme (PR-7) | "The best deck under budget" and "a named tier-one deck" fill the theme slot. Before, the competitive theme row never closed its key, so the generic theme row fired one turn later and read as a repeat. It cost two of the nine gate failures of 2026-08-25. |
| D-80 (amended by D-147 and D-163) | 2026-08-25 | The pick row keeps its names (amends D-73) | The commander pick row repeats while the commander slot is empty. The same three names stay on the table until the user asks for others. A retired name never comes back. Evidence: the gate run of 2026-08-25 named three other commanders when the user answered a different question, which read as if the agent had ignored the answer. |
| D-81 | 2026-08-25 | Two rows wait for their context (PR-7) | The locked-cards row requires the format and the theme. The house-format-limits row requires the house-rules answer. Both fired before their context existed in the gate run of 2026-08-25, and the model replaced both. The classify prompt also states that a phrase which only raises a topic closes no key. |
| D-82 | 2026-08-25 | The resolver asks only for what a row names (PR-7) | The placeholder resolver called the PR-6 hint source for every row, and it cached the answer under the theme alone. A commander list computed before the user named their colors therefore survived. In the gate run of 2026-08-25 the agent offered three commanders outside the deck color identity, twice. The resolver now asks only for a placeholder the row holds, and the cache key carries the format, the colors, and the pool rule. |
| D-83 | 2026-08-25 | A slot closes on a value, not on a name (PR-7, settled at the third attempt) | A key closes by name only when its question is out and it carries no typed value. The format, theme, colors, power, pool-rule, budget, and commander slots close on their value alone. Three gate runs set the rule. Run 2 retired power and the pool rule from "Brago blink deck from my library", and that session called itself complete after one question, so the question must be out. Run 3 then blocked every typed slot from closing by name, and the format stayed open after the user answered "Pioneer", so the agent asked twice. Run 6 allowed the by-name close again, and the format value stayed empty: every row that triggers on the format stopped firing, five sessions ended with no power level, and PR-8 would have had no format. The deciding invariant is that a slot the deck generator reads must end with a value. Asking twice is the safe failure. The classify prompt now states that an answer counts only in its own field (D-90). |
| D-84 | 2026-08-25 | The gap score is a fault checklist (PR-7, amends D-69) | The score prompt asked how well a row "asks what this user needs next", which is a matter of taste. The same question scored 0.02 and 0.98 in two turns of one conversation, and 0.98 then 0.02 across two runs. The prompt now names four faults: contradiction, already answered, wrong slot, and unanswerable. The score follows the fault count. D-66 says a prompt change invalidates the M-5 scores. M-5 has not run, so the change costs nothing now. |
| D-85 | 2026-08-25 | `Question.catalog_text` (amends D-46) | The proto carries the catalog question that an invented question replaced. M-5 runs in the UI after PR-12, and the D-66 rubric scores `catalog_enough` and `invented_better`. Neither can be judged without both texts. The field is additive, and `buf breaking` against `main` is clean. It is a second contract change after PR-1b, which D-46 meant to avoid, so the owner may reverse it. |
| D-86 | 2026-08-25 | The color clause needs a real theme (PR-7, corrects D-79) | The color row states a fact about the theme. D-79 made "the best deck under budget" a theme value, and the row then read "the best deck under budget is strongest in white, blue, black, and green". Gate run 3 of 2026-08-25 replaced three color questions for this reason. The clause is now dropped when the theme names no archetype, and when the answer holds more than two colors. The row reads "A {theme} deck is strongest in {colors}". |
| D-87 | 2026-08-25 | The acquisition row is deleted (owner, amends the corpus catalog) | The row asked "Do you buy in person or online, and by what date do you need the cards?" The app can not act on either answer. It holds no store stock and no delivery times, and Scryfall gives a price estimate, not availability (D-17). The answer had no reader. The row also asked two things in one sentence, and every gate run of 2026-08-25 replaced it. The `deadline` fact and the `Deadline` trigger are gone with it. The catalog holds 25 rows. |
| D-88 | 2026-08-25 | A reword is not an invention (PR-7) | The agent refuses a replacement that repeats more than 60% of the words of the row it replaces. Gate run 4 of 2026-08-25 measured a median word overlap of 0.67 over eight replacements. Seven of the eight only added the user's colors, format, or card name, which the ask role adds anyway, and the score prompt already says that is not a fault. A refused reword is counted, so a high count means the scorer flags style rather than faults. An invented question also keeps the options of the row it replaces: two replacements offered two of the three pool modes, so the user lost a mode D-37 requires. |
| D-89 | 2026-08-25 | The M-5 sheet is generated (implements D-66) | `cmd/m5-sheet` reads the gate documents and writes the scoring sheet. Each item pairs the invented question with the catalog question it replaced, because the rubric can not judge `catalog_enough` or `invented_better` from one text. Every tenth item repeats an earlier one, unlabeled. An item with no catalog text is left out, and so is an item whose row the catalog no longer holds. `make m5-sheet` costs nothing and refuses to overwrite a scored sheet. |
| D-90 | 2026-08-25 | The classify prompt makes a field the only record of an answer (PR-7) | An answer to the format, theme, colors, power, pool rule, budget, or commander counts only when the classifier puts it in that field. The `closed_keys` list carries advisory keys alone. The prompt also tells the model to repeat a value the user gave in an earlier message while the field is still empty. Without that rule the model reports "answered" and stores nothing, which D-83 records. |
| D-91 | 2026-08-25 | A premature session fails the gate (owner, PR-7) | A conversation that calls itself complete with a needed slot unanswered fails the gate, next to the catalog-only bar. The needed slots are format, theme, colors, and power, plus the commander in Commander and the pool rule with a library. A conversation that runs out of scripted messages does not fail: the script ended, not the engine. Run 6 of 2026-08-25 scored 30 of 30 on the catalog bar and still failed, because five sessions ended with no power level. The old bar alone called that run perfect. |
| D-92 | 2026-08-25 | A schema enum suppressed the format field (PR-7) | The classify schema constrained `format` and `pool_rule` to an enum that held "unknown". The model then answered "unknown" for a message that named the format outright, while it filled every free-text field around it. Measured over four samples each on 2026-08-25: the enum extracted the format in 1 of 8 cases, an enum with an empty member in 3 of 8, and a free string in 7 of 8. Both fields are free strings now, and Go owns the vocabulary through `slotWord`. This is the root cause under D-83: the model reported the format through `closed_keys` because that channel was not suppressed. `TestLiveFormatExtraction` guards it, because no offline test can: a fake provider answers whatever the test tells it to. |
| D-93 | 2026-08-25 | A user can decline a slot (PR-7) | `State.Skip` was declared and never called, so nothing let a user hand a choice back. Gate run 8 of 2026-08-25 shows the cost: conversation 4 answered "any colors are fine" in turn 3 and still ended with the color slot open. The classify schema gained `declined_keys`. A decline closes any key, typed or not, and it names no value on purpose. The generator applies the default the corpus lists under default answers, and the `SKIPPED` state is what tells it to. The question must be out first, because nobody can decline a question they never saw. A live check of 2026-08-25 measured the precision: "any colors are fine" declines the colors alone, "you decide" declines every open key, and a real answer declines nothing. |
| D-94 | 2026-08-25 | Commanders are ranked as commanders (PR-6 correction) | `Commanders` read the 99-card shortlist and took the first legends it met. That list ends in `capByRole`, which emits one role bucket after another with lands first, so it is not ordered by score. A blink request came back with three Ojer modal double-faced cards, which the theme scorer rates 0.16, while 85 on-theme blink commanders existed and Emiel the Blessed rated 0.56. A lifegain request came back at 0.60 and 0.56 while 388 commanders rated up to 1.00. `CommanderPool` now walks the index itself: legal, inside the color identity, no Game Changer under bracket 2, and a theme signal required. Role caps do not apply, because one card fills no role quota, and the staple-role fallback does not apply, because a useful land says nothing about leading a deck. The 99-card pipeline is untouched, so the PR-6 gate still holds. `make themes-check` runs the snapshot regression. |
| D-95 | 2026-08-25 | The scored-sheet guard reads every field (corrects D-89) | `make m5-sheet` refused to overwrite a scored sheet only when `catalog_enough` held a lowercase word. A capitalised "Yes", or a session that fills `catalog_action` first, passed the guard, and a regenerate would have destroyed the scoring. The guard now reads `catalog_enough`, `invented_better`, `right_slot`, `faults`, and `catalog_action`, in any case, and free text counts. `filled_slot` is left out, because the generator pre-fills it. |
| D-96 | 2026-08-25 | Only an engine-current run feeds M-5 (owner, PR-7) | A gate run made before a defect was fixed measures the defect, not the catalog. Run 3 is the proof: the format enum (D-92) and the first form of D-83 threw the user's answer away, the format row fired again, and the model invented a repair question. Scoring that item reads as "the catalog was not enough and the invention was better", which pushes the D-27 threshold up on evidence about a bug that no longer exists. `M5_RUNS` names the documents the sheet may read. Narrowed the same day, on the owner's call: a run is held back when the defect plausibly produced or distorted the replacements being scored, not whenever any code changes. A literal reading would need a fresh run after every fix, forever. Runs 1 to 9 stay on disk as dated records. |
| D-97 | 2026-08-25 | Probe conversations (owner, PR-7) | The 30 gate conversations all hold a cooperative user who answers what the agent asks. None changes their mind, contradicts themselves, asks a question back, or wants something the app can not serve. Twenty-two probe conversations now hold those shapes. A probe runs beside the gate and its questions reach the M-5 sheet, but it does not count toward the catalog-only bar, because adding conversations to the bar would move the bar. The owner chose this over five reruns of the same 30, which would re-measure the same rows against model noise. The probes are the assistant's wording, which the owner accepted with the risk noted: they may share the assumptions of the first 30. |
| D-98 | 2026-08-25 | A declined format takes the corpus default (amends D-93) | D-93 made a decline name no value. The format is the one slot the planner routes on, because every power row and every Commander row triggers on it. Probe 35 of gate run 11 declined the format, so no power row could fire and the session finished with no power level. A declined format now stores Commander, which the corpus names under default answers, and the slot keeps the `SKIPPED` state so the generator can tell a default from a choice. The other declined slots stay valueless, because nothing routes on them. |
| D-99 | 2026-08-25 | The agent can decline a request (owner, PR-7) | Gate run 11 answered "Can you build me a Yu-Gi-Oh deck?" with "Which Yu-Gi-Oh format would you like?" No catalog row could decline, so the model improvised and followed the user out of scope. A new row on a new `scope` slot says the app builds Magic decks only, and asks whether the user wants one. It fires alone, because asking the format beside it reads as if the agent had not heard the request. The classify schema gained `facts.out_of_scope`, and the fact is not sticky: a user who then asks for a Magic deck gets the usual flow. `slot_states` is a map, so the new key is a proto comment change and not a wire change. `TestLiveOutOfScope` proves the classifier raises the fact. |
| D-100 | 2026-08-25 | The scoring sheet explains itself (implements D-66) | The sheet listed the six field names and nothing else, so it could not be scored without the conversation that produced it. It now carries what the sheet decides, which of the two questions each field reads, the fault vocabulary in plain words, and the free-text convention: keyword first, then a dash, then prose. `catalog_enough` and `invented_better` stay in their listed values, because those two are counted, and free text there would measure the reader rather than the owner. Each item's table names the text each field reads. |
| D-101 | 2026-08-25 | The session store runs against the emulator (PR-7) | Every Firestore path in `internal/sessions` sat at zero coverage. `NewID`, `Get`, `GetState`, and both document paths had never executed, and `Put` ran only far enough to reject a session with no id. The store that persists every conversation had never run. Seven tests now drive it against the local emulator, and coverage went from 31.9% to 78.0%. `make store-check` runs them, and CI skips them without `FIRESTORE_EMULATOR_HOST`. A round trip alone is not enough: `Put` and `GetState` agree with each other whatever path they use, so a rename of the private path passed. Two tests read the literal document path and assert the private state stays out of the public document, which `GetSession` returns (D-74). A path rename now fails, which was checked by mutation. |
| D-102 | 2026-08-25 | The scored sheet is read by a tool (implements D-27, D-66) | `cmd/m5-report` reads a scored sheet and counts. It reports the fit threshold, the reword guard, and what the catalog needs. For each candidate gap score it gives the inventions that threshold allows, how many were warranted, and the share, then names the highest candidate that keeps the 80% floor. It reads only the first word of a field, so free text after a dash survives in the sheet (D-100). A refused reword never reached a user, so it is left out of the fit threshold and counted only against the reword guard. An unsure item is excluded (D-66). A half-scored sheet says so, and every number reads the scored items alone. `make m5-report` costs nothing. |
| D-103 | 2026-08-25 | `vague` is a fault, and a truncation is a reword (owner, amends D-66 and D-88) | The owner scored item 8 of the M-5 sheet and named a fault the six did not hold. The row read "What do people play at your event? I tune the 15 sideboard cards to it." The replacement read "What do people play at your Modern event?" It dropped the sentence that says what the answer is for, so the user can not tell whether to name decks, colors, or archetypes. `vague` is now the seventh fault: the question does not say what kind of answer is useful. The fit is unaffected, because `faults` never enters the threshold calculation, and the rubric version stays 1. The same item exposed the reword guard. Word overlap is symmetric, so a replacement that deletes half the row scores low and passed, although it says strictly less. Item 8 scored 0.44 against a 0.60 bar. The guard now also refuses a truncation: a replacement that borrows 0.8 or more of its words from the row and keeps under 0.6 of it. Measured against all 60 items of the sheet, the rule refuses item 8 and nothing else. |
| D-104 | 2026-08-25 | M-5 scores every question, not only a replaced one (owner, amends D-66 and D-89) | The sheet held only a question that the model offered to replace. That is 60 of 793 questions, or 7.6 percent. A wrong question that the model never challenged could not become an item. The new sheet holds every question the agent asked. An unreplaced question gets one new field, `warranted`, with the values yes, no, and unsure. Evidence: deterministic rules find a defect in 61 of 793 questions, and in 159 when the presumed-table wording counts. |
| D-105 (amended by D-263) | 2026-08-25 | A new conversation set, and the first 30 stay frozen (owner, amends D-97) | Every gate conversation restates the format at turn 2, even when turn 1 gave it. A redundant question therefore always gets an answer, and both gate bars reward it. Conversations 1 to 30 stay byte-identical, so the catalog-only bar stays comparable to runs 1 to 13. A new set never repeats what the user already said. A redundant question then strands the conversation, and the gate sees it. |
| D-106 | 2026-08-25 | Item 20 of the M-5 sheet stands over item 2 (owner) | Items 2 and 20 are the same item, and the owner scored them differently. Item 20 stands: the catalog question was not enough, and the replacement was better. The colors row is therefore a reword candidate. Items 1 and 10 agree. Items 3 and 30 differ on `invented_better` alone. The sheet holds six such pairs, at items 1 and 10, 2 and 20, 3 and 30, 4 and 40, 5 and 50, and 6 and 60. |
| D-107 (amended by D-216) | 2026-08-25 | The agent infers a competitive power level and confirms it (owner, amends D-84) | "Strongest", "money is no object", and "competitive" fill the 60-card power step as tournament. The agent states the inference and asks the user to confirm it. The slot ends with a value, which D-90 requires. Commander keeps the bracket question, because bracket 4 and bracket 5 are too far apart to infer. Evidence: 14 questions across runs 10 to 13 asked for the power level after the user gave it. |
| D-108 | 2026-08-25 | The colors row states no fact (owner, corrects D-86) | The row asserted which colors a theme is strongest in. The value came from a text match over the top 100 theme cards, and it was wrong. Run 13 read "Extra-turns decks are strongest in blue and green", and "Black Lotus decks are strongest in black". Black Lotus is a colorless card, and the match read the word "black" in its name. The row now reads "Any color preference?" and it asserts nothing. |
| D-109 | 2026-08-25 | The power row presumes no table (owner) | The Commander power row said "your table". 123 of 793 questions said "table" to a user who never mentioned one. One of them was a deck as a gift for a brother. The row now reads "Which power bracket should the deck target?" The ask role may still name the table when the user named one. |
| D-110 | 2026-08-25 | The table-tolerance row is deleted (owner, amends the corpus catalog) | The row asked whether the table accepts mill, land destruction, extra turns, or stax. The power level answers the same need, and the owner does not want the agent to ask it. The row fired 20 times across runs 10 to 13. The salt list of corpus section 7 no longer drives a question. The `salty_theme` trigger is gone with the row. |
| D-111 | 2026-08-25 | Proxies fill the budget slot (owner) | The word "proxy" triggered the house-rules row alone. A user who proxies every card has no budget. The word now closes the budget slot, and it no longer triggers the house-rules row. "Anything goes", "kitchen table", and "no ban list" still trigger that row. A negation stops every word trigger. "No proxies" is not a proxy user, and probe 38 proved the need. |
| D-112 | 2026-08-25 | A row for two decks, and a row for an unsupported format (owner) | The agent silently chose one deck when the user asked for two. A new row says the app builds one deck at a time, and it fires alone. The format row also offered Brawl, which the app can not build. A second new row names the format the user asked for, and it offers the nearest supported one. |
| D-113 | 2026-08-25 | The card-pool row names the precon (owner) | "Upgrade my Atraxa, Praetors' Voice precon" got the generic library question. A new row names the precon instead. The three pool modes do not change, and the proto does not change. PR-8 owns the build rule that keeps most of the precon cards. OQ-21 records that open point. |
| D-114 | 2026-08-25 | The rubric gains three faults and a legal n/a (owner, amends D-66 and D-103) | The owner used three faults the rubric did not hold: `vague`, `inaccurate`, and `omits information from the catalog question`. The value `n/a` becomes legal, and it marks a row that must not exist. `m5-report` counted an `n/a` item as an unwarranted invention, which lowered the precision. It now leaves such an item out of the fit. |
| D-115 | 2026-08-25 | A deterministic question linter (owner) | Most defects need no model call. A format question after the user named the format, a presumed table, a word trigger on a negation, a stuttered card name, and an unsupported format are all checkable offline. The linter reads every gate document and the offline conversations. It runs in CI, and a hit fails the build. It can not judge tone or relevance, and the owner's scoring still owns that. |
| D-116 | 2026-08-25 | The ask role adds no clause that repeats a known value (assistant finding, corrects a reading of D-88) | `m5-report` said the reword guard is too tight, because six refusals scored better than the row. In each one the replacement was the resolved catalog row word for word, so the guard refused an exact copy. The owner's "better" verdict was about the clause the ask role added, such as "for your white-black lifegain deck". The ask prompt now forbids a clause that only repeats a value the user gave. A clause that narrows the question stays, which is why items 30 and 31 scored the loss of "red-green" as worse. `m5-report` counts an identical replacement apart, because it says nothing about the guard. |
| D-117 | 2026-08-25 | A row that states an app limit goes out as written (assistant finding, extends D-99 and D-112) | Three rows say what this app does or does not do: `out_of_scope`, `one_deck`, and `format_unsupported`. A smoke run before gate 14 replaced "I build one deck at a time. Which deck do you want first?" with "Which deck would you like to work on first: Commander or Modern?" The user never heard the limit, which is the whole point of the row. Such a row carries `fixed`. The model may not replace it, and the ask role never sees it. A turn whose every row is fixed skips the ask call, which saves one model call. The linter also exempts `format_unsupported` from the unsupported-format rule, because that row must name the format the user asked for. |
| D-118 | 2026-08-25 | A card with no stated role goes in its own list (assistant finding, corrects D-70) | "Build around Grist, the Hunger Tide" names a card and no role. The classify schema held two card lists, and the model put the card in `commander_names`. `SetCommander` then closed the role question, the color question, and both other commander rows. Conversation 3 is named "a named card, role unknown", and the role question never fired in runs 11, 12, or 13. The schema now holds a third list, `named_cards`, for a card whose role the user did not state. The M-5 sheet could never show this defect, because the model offered no replacement for a question that never went out (D-104). Prompt version 3. |
| D-119 | 2026-08-25 | An answer that repeats an option closes the key (assistant finding, nets under D-83) | The agent asks "When you say anything goes, do you mean any card with no ban list, or Vintage rules?" The user answers "Any card, no ban list." The classifier left the key open in gate run 13 and in the batch run of 2026-08-26. The house-format row waits on that key, so it never fired, and the user's next message answered a question nobody had asked. A deterministic net now closes an advisory key when the message repeats an option that question offered. An option under eight characters closes nothing, because "Yes" and "No" answer any question. A typed slot is left alone, because it closes on its value (D-83). |
| D-120 | 2026-08-25 | A refusal never closes the commander pick (assistant finding, restores D-73) | Conversation 14 is named "the user says none, then picks". The user answers "None of those." and the classifier closed the pick row by name, in gate runs 12 and 13 and in the batch run of 2026-08-26. The session then called itself complete, the fourth message never went out, and a deck would have carried a commander nobody chose. Two changes hold the line. `commander_pick` joins the typed slots, so no name can close it, and only a chosen commander does. A word rule reads a refusal while the pick row is out, retires the names on the table, and asks again with three others. The M-5 sheet could never show this, because the model offered no replacement (D-104). |
| D-121 | 2026-08-25 | A commander chosen by its place (assistant finding, completes D-73) | Conversation 14 ends with "The first of the new three is good." The classifier can not map that onto a name, because the classify input never held the names. Two changes fix it. The input now carries `offered_commanders`, and the prompt tells the model to read a pick by name or by place. A word rule also reads an ordinal against the names on the table, while the pick row is out. The rule ignores an ordinal after "library", "collection", "binder", "precon", "deck", "cards", or "pool", because "build from my library first" chooses no commander. An invented pick question now records its names as well, so the next turn can read the answer against them. Prompt version 4. |
| D-122 | 2026-08-25 | A message may answer the question it raises (assistant finding, extends D-119) | Conversation 21 writes "Any card, no ban list. Call it Vintage. An artifact prison deck." The words "no ban list" raise the house-rules row, and the same message answers it. The agent asked it anyway, in the batch run of 2026-08-26. D-119 closed a key only after its question went out, and no question had gone out here. The agent now checks every planned row against the options it offers, closes the key when the message repeats one, and plans again. A closed key can free a row that waited on it. A typed slot is left alone, because it closes on its value (D-83). |
| D-123 (amended by D-147 and D-163) | 2026-08-25 | A request for a suggestion keeps the names on the table (assistant finding, restores D-80) | D-73 retired the offered commanders whenever the classifier set `wants_suggestion`. D-80 says the same three names stay until the user asks for others. Conversation 23 of the batch run of 2026-08-26 swapped all three commanders between turn 2 and turn 3, on a message that refused nothing. Only a refusal retires the names now, which D-120 reads. The fact still fires the pick row. |
| D-124 | 2026-08-25 | The hint source reads the slots of this turn (assistant finding, same class as D-82) | The gate runner and `agentsvc` build the hint source before the turn runs. A value the classifier fills inside that turn was invisible to it. Conversation 23 of the batch run offered Jodah, the Unifier, a five-color commander, for a red-green deck, because the color slot filled in the same turn. A hint source may now implement `SlotAware`, and the agent hands it the format, the colors, and the pool rule before it resolves a row. The cache key already carried all three (D-82), so a stale answer stays keyed to the values that produced it. |
| D-125 | 2026-08-25 | A filled format changes only when the message names the new one (assistant finding, guards D-90) | The classify prompt tells the model to repeat a value the user gave earlier. In probe 31 it repeated the value the user had just replaced. The user asked for Commander, changed to Modern two turns later, and the format went back to Commander on a message that named no format. The agent then asked for a commander in a 60-card format. A filled format now changes only when the current message names the new format, by word or by a commander phrase. A changed format also retires every question still out, because a bracket question means nothing in Modern. |
| D-126 | 2026-08-25 | One row per key while a question is out (assistant finding, extends the no-repeat rule) | The no-repeat rule reads the row id. Two rows that share a state key could therefore ask the same slot in other words. Probe 33 asked the theme through the competitive row, got no answer, and asked it again through the general row one turn later. A row is now skipped while another row holds that key open. The commander rows are unaffected, because each one carries its own key (D-71). A changed format clears the open keys, so the questions that replace them can still go out (D-125). |
| D-127 | 2026-08-25 | A commander row with no names to offer asks nothing (assistant finding) | Probe 35 answers every question with "you pick", so the theme stays empty and PR-6 can name no commander. The pick row fell back to "Which commander would you like, or should I suggest three more?" and asked it twice, with nothing to suggest. A row that names commanders in braces is now dropped when the pool gives none. The key takes the skipped state, so the generator applies the corpus default and picks a commander. The same rule covers the not-owned row and the weak-pool row. |
| D-128 | 2026-08-25 | A row may wait for another row's answer (assistant finding, extends D-126) | The commander rows carry separate keys on purpose, so the pick row can follow the base row (D-71). D-126 therefore did not stop probe 39 from asking "Do you want Karlov of the Ghost Council as your commander, or as one card in the 99?" and then, one turn later, "Do you have a commander in mind, or should I suggest one?" The role answer decides whether a commander question is needed at all. A row may now name the keys it waits for, through `not_outstanding` in the catalog. The commander row waits for the role question. |
| D-129 | 2026-08-25 | A card that can not lead a deck is not a commander (assistant finding) | Probe 41 asks for "Commander deck with Lightning Bolt as my commander". Every run before 2026-08-26 accepted it in silence, closed every commander row, and finished with an illegal commander in the slot. The card index already carries `CanBeCommander`, so the check costs nothing. A hint source may now implement `CommanderChecker`. A named commander that can not lead is recorded as a locked card instead, because the user still wants it in the 99, and a new row says so and offers to suggest one. An unknown card name claims nothing. The base commander row also stops firing once the user has asked for a suggestion, because the pick row is the right question then. |
| D-130 | 2026-08-25 | The commander choice reopens on request (assistant finding) | Probe 49 chooses Karlov of the Ghost Council, then writes "Actually use a different commander, suggest one". A chosen commander closes the commander row, the pick row, and the role row, so nothing could ask. Every run before 2026-08-26 answered with silence, and the user's next message referred to a list that never came. A word rule now reads a swap request, drops the chosen commander, reopens the three rows, and retires the names already offered. The color slot stays closed, because the user or the old commander gave the colors and a new commander comes from inside them. |
| D-131 | 2026-08-25 | The commander pick goes out as written (assistant finding, extends D-117) | The pick row names three commanders, and the names are the question. The ask role rewrote it as "Which commander would you like to use, or should I suggest three more?", which names nobody. The owner scored that same loss as worse than the row in item 31 of the version-1 sheet. The row now carries `fixed`, so it never reaches the ask role. Go builds its options from the names it offered, plus the answer that asks for three others. A turn whose every row is fixed skips the ask call. |
| D-132 | 2026-08-25 | The meta row triggers on the power value (assistant finding, corrects the catalog against the corpus) | Corpus section 11 fires the meta row and the competitive theme row when the power is FNM or tournament-meta. The code fired them on `power_competitive`, a fact the classifier raises for vague words such as "strongest". A user who named the step outright therefore got neither. Conversation 62 asks for sideboard help at tournament level, and the agent never asked what the sideboard must beat. A named FNM or tournament step now sets the fact as well. |
| D-133 (amended by D-141 and D-143) | 2026-08-26 | The eval role, and the automated M-5 lane (owner) | Hand scoring does not scale: 32 items cost the owner hours, and one gate run asks about 250 questions. A new `eval` role scores every question against the rubric the owner applied on 2026-08-25. The owner set it on `gpt-5.6-luna`, the cost tier, so a 66-conversation run costs about eleven cents. On `claude-sonnet-5` the same run costs about $1.19, so the cost tier is the difference between two iterations and fourteen inside a $3 budget. The eval role is not the judge role: D-4 and D-22 give the judge a deck and a separate provider. `cmd/questions-eval` writes a document and a summary, `cmd/tune-check` decides whether an iteration may be kept, and `scripts/autotune.sh` is the loop. The eval shares a model with the classify and ask roles, so it rates work its own model produced. `make eval-calibrate` measures that bias against a stronger judge for about thirty cents, and OQ-26 holds the decision. |
| D-134 | 2026-08-26 | The eval holds a third of the conversations back (assistant finding) | A fixer that reads its own scorecard tunes the scorecard. Every third conversation is now a holdout: the eval scores it, the report never names its failures, and `tune-check` reads the holdout ratio and not the one the fixer saw. A ratio that falls on the tune split while the holdout stands still is a reworded test set, and the loop prints a warning. The loop also keeps four counters that a ratio can not see: questions asked, questions that closed a slot, premature sessions, and linter findings. Gate run 7 of 2026-08-25 is why: it passed both bars with 26 of 30 conversations unanswered. |
| D-135 | 2026-08-26 | The loop may change the catalog inside an approved run (owner, amends D-28, answers OQ-24) | D-28 says the owner approves every catalog change. The owner now approves the loop instead of each change inside it. Every change lands on a branch of its own, carries a test and a decision row, and waits for the owner to read the branch before it merges. Outside a loop run, D-28 stands unchanged: a catalog change needs the owner. |
| D-136 | 2026-08-26 | The eval may share a model with the agent it scores (owner, answers OQ-26) | The eval role runs on `gpt-5.6-luna`, which also writes the questions. A model rates its own wording gently, and D-69 split the score call from the ask call for that reason. The owner accepts the risk for now, because the cost tier is what makes fourteen iterations fit a $3 budget. `make eval-calibrate` measures the bias against `claude-sonnet-5` for about thirty cents. Read every ratio the loop reports as a floor, and not as a measurement. |
| D-137 | 2026-08-26 | The loop stops at a 5 percent bad-question ratio (owner, answers OQ-27) | The target reads the holdout split, which the fixer never sees (D-134). No data sets the number yet, and the first report measures where the agent starts. |
| D-138 | 2026-08-26 | The tuning loop works on a branch and pushes nothing (owner, answers OQ-25) | The owner chose the first of three isolation levels. The loop commits to `auto-tune/<stamp>`, and `--push` is the opt-in. Nothing leaves the machine until the owner reads the branch and pushes it. The guards bound what a bad night does inside the repository. They do not sandbox the agent, and a headless agent still runs shell commands as the owner. A worktree of its own, and a container, stay available and unbuilt. `scripts/autotune-fix.sh` still ships no default agent: the owner names the fixer command. |
| D-139 | 2026-08-26 | The tuning loop starts from `main` (owner, closes OQ-40) | PR-7 merged as #12 on 2026-08-26, and the PR-7B instrument merged with it. A change the loop makes is a PR-7 correction, so it belongs on top of `main`. The loop cuts `auto-tune/<stamp>` at once and never commits to `main` itself: `commit_push` refuses that, whatever branch the run started from. |
| D-140 (extended by D-269) | 2026-08-26 | The illegal-commander row says nothing about a legendary card (assistant finding, corrects D-129) | Gate run 14 told a user "Grist, the Hunger Tide can not lead a deck". The claim is false. Grist is a Legendary Planeswalker by type line, and it is a legal commander: a characteristic-defining ability makes it a creature card everywhere except the battlefield (Scryfall ruling, 2021-06-18). The engine reads a type line, and the hand-off has recorded that limit since 2026-08-24. D-129 turned a silent limitation into a false statement. `CanLead` now answers "not known" for any legendary card it can not confirm, so the row stays quiet. It still answers for a card that is not legendary at all, which is Lightning Bolt in probe 41 and Sol Ring. The run passed the gate and the linter found nothing, so neither guard could catch this. Both eval models did. |
| D-141 | 2026-08-26 | The eval reads a transcript, turn by turn (assistant finding, corrects D-133) | The eval input held a flat list of messages beside a flat list of questions, each tagged with its turn. The prompt told the reader to judge a question against the turns before it, and the cost tier did not. The calibration of 2026-08-26 marked seven questions as duplicates of an answer the user gave one or two turns later. The input is now interleaved: each turn holds the user's message and the questions the agent sent in reply. Nothing below a question in the transcript was known when it went out. Eval prompt version 2, so the calibration of 2026-08-26 does not carry over. |
| D-142 | 2026-08-26 | The loop writes into a container branch (owner, amends D-139) | D-139 started the loop from `main`. That is right for one night and wrong for two: the second night would start from the same commit as the first, see none of its accepted work, and rewrite the same catalog rows. The owner keeps a container branch instead, `pr-7c`, opened from `main` after `pr-7b` merges. Each night cuts `auto-tune/<stamp>` off the container and pushes nothing. The owner reads the night and fast-forwards the container, or deletes the night. Many nights then become one pull request. `scripts/autotune.sh --base` names the container, and the loop warns when the base is `main`. |
| D-143 | 2026-08-26 | The eval reads the session facts, not only the transcript (assistant finding, corrects D-133) | The eval judged the card-pool question as a presumption that the user owns cards. The transcript never says a collection is attached, and the gate document does. The eval input now carries that fact. The calibration of 2026-08-26 showed two such refusals out of six the cost tier made alone. |
| D-144 | 2026-08-26 | A question may not offer an answer the rules forbid (assistant finding) | Gate run 14 asked "Do you want to use any colors beyond Grist's color identity?" In Commander the color identity of the commander is the color identity of the deck, so the rules allow no answer to that question. The colors row reads "Any color preference?", and the ask role added the clause. The ask prompt now states the rule, and the linter refuses the shape. This is the second false rules claim the ask role invented in one run, after D-140. Both were found by the eval and missed by the gate and the linter. |
| D-145 | 2026-08-26 | The conversation set grows to 100 (owner) | The 66 conversations of D-105 covered the shapes the session had already found. Conversations 67 to 100 take the shapes none of them took: bracket 1 and bracket 5, five colors and no colors, a companion, a Background pair, a corrected card name, a corrected color, group hug and voltron and chaos, a zero budget, a partial proxy, a per-card cap beside a total cap, a collection of lands alone, a library that does not exist, Pauper Commander, Oathbreaker, Historic, and a user who answers with a bare digit. All 34 are terse: no user repeats a value they already gave. They count as probes, so the catalog-only bar still reads the first 30 (OQ-37 holds that question). One run now costs about $0.14 and takes about 18 minutes, and the eval about $0.10 and 13 minutes. |
| D-146 | 2026-08-26 | Historic and Timeless name no nearest format (owner, answers OQ-22) | The unsupported-format row named Modern for Historic, and Legacy for Timeless. Neither mapping came from data, and `words.go` marked both unverified. This session measured the card pools against the snapshot of 2026-08-24. Pioneer is nearest to both by Jaccard similarity, at 0.680 for Historic and 0.694 for Timeless. Modern reads 0.568 and 0.581, and Legacy reads 0.454 and 0.465. Portability agrees. 83.3 percent of Pioneer cards are legal in Historic, against 61.5 percent of Modern cards. A Modern deck is therefore about 38 percent unplayable for a Historic player. Historic and Timeless are also 0.968 similar to each other, so no measurement separates them, and the old split between Modern and Legacy had no basis. Their whole difference is 289 Timeless-only cards, such as Reanimate and Entomb, and 216 Historic-only cards that are almost all Alchemy rebalances. Historic bans 77 cards and Timeless restricts 4: Channel, Demonic Tutor, Necropotence, and Tibalt's Trickery. The owner chose to name no substitute over a substitute the data does not support. A new catalog row, `format_unsupported_open`, names the format and asks which format to build. `Context.NoNearFormat` selects it, and the linter exempts it as it exempts the first row (D-112). The catalog holds 31 rows. |
| D-147 | 2026-08-26 | A delegation closes the commander choice (assistant finding, amends D-123) | Eighteen of the 100 gate conversations answer with "You pick the commander", "You decide", or a phrase like them. No rule read one. The classifier set `wants_suggestion`, which fires the pick row, and nothing ever closed the key. The row carries `"repeat": true`, so it asked again every turn until the messages ran out. Eval run 14 refused 18 of its 43 bad questions on that one row, more than the next four rows together. Conversations 22, 24, 27, 51, and 54 all show it. A delegation is a decline (D-93). It closes `commander_pick` and `commander`, it names no value, and the generator takes the best commander of the pool. `DelegatesChoice` holds the phrases, and a scope guard limits it: the delegation closes the commander choice only while a commander question is out, or while the message names a commander. Without the guard "up to you" would close the commander choice after any other question. D-123 is amended, not reversed. Its test asserted that the pick row asks again after "you pick the commander", which is the behavior this decision removes. The test now uses a message that asks for a suggestion and hands over no choice, so D-123 still measures the names on the table and nothing else. |
| D-148 | 2026-08-26 | A commander holds every color the user named (owner, corrects D-82) | `CommanderPool` filtered with `identityFits`, a subset test. A mono-red commander therefore passed a blue-red request, and a colorless commander passed every request. Conversation 22 of gate run 14 asked for a blue-red extra-turns deck and was offered Birgi, God of Storytelling (mono-red), Emrakul, the Promised End (colorless), and Vnwxt, Verbose Host (mono-blue). Not one of the three is blue-red. The eval found it, and the linter and the gate both missed it. The commander's color identity is the deck's color identity, so a subset commander silently narrows the deck the user asked for. A colorless commander is the worst case: that deck can play no colored card at all. `identityCovers` now runs beside `identityFits` in `CommanderPool` alone. The 99 keeps the subset test, because a mono-red card belongs in a blue-red deck. An empty pool is already safe: D-127 drops a commander row with nothing to offer, and the key takes the skipped state. Colorless is skipped on both sides, because it is not a color. |
| D-149 | 2026-08-26 | The eval checks a card name against the snapshot (assistant finding, answers OQ-39 in part) | The eval invents card facts of its own, and a false one changes a verdict. Eval run 14 refused three questions on two such claims. It called Ran and Shaw "not a real Magic card or commander option", and it called Quina, Qu Gourmet "not a valid Magic card option". Both cards are real. Ran and Shaw is a Legendary Creature - Dragon, mono-red, and the user had asked for a red dragon deck. Quina, Qu Gourmet is a Legendary Creature - Qu, mono-green. Both are legal in Commander, and the snapshot of 2026-08-24 holds both. Three of the ten `inaccurate` faults of that run were therefore the eval's own inventions. The owner chose a deterministic check over a second judge. It costs nothing, and the calibration of 2026-08-26 showed that `claude-sonnet-5` invents card facts too, so a second judge does not fix this class. `cmd/questions-eval` now reads the snapshot. A refusal whose reason denies that a card exists is checked against it, and the `inaccurate` fault is dropped when the snapshot holds the card. A verdict that carried another fault keeps that fault and stays bad. The report names every correction, and it says so when the snapshot is absent and the check did not run. The dual-judge proposal stays open under OQ-39 for the faults this check can not reach. |
| D-150 | 2026-08-26 | No question names a format this app does not build (assistant finding, extends D-112 and D-144) | Gate run 15 failed the linter on two questions. Conversation 94 asked "What should the Oathbreaker deck focus on: a creature type, a mechanic, or a play style?" one line under "I do not build Oathbreaker. The nearest format I build is Commander." Conversation 95 did the same with Historic. Both went out in one turn, so the agent declined a format and then named it as the deck it was about to build. The catalog row is innocent. The `theme` row reads "What should the deck do", and the ask role fitted it to the user's words. D-116 lets a clause narrow a question, and this clause narrowed it to a format the app refuses. Three guards now hold the line, which is the D-144 pattern. The ask prompt names the seven formats and forbids them, at prompt version 6. The phrasing guard refuses such a text and falls back to the catalog row, so a bad phrasing can not reach a user even when the prompt fails. The linter already refused the shape, and that is what failed the run. A row that exists to decline a format is exempt from all three, because naming the format is the whole job of that row. Those rows carry `fixed` and never reach the ask role, so the exemption guards a later change. |
| D-151 | 2026-08-26 | A question names no card's color identity (owner, answers OQ-41, corrects D-144) | D-144 refused the shape as a list of prepositions: "colors beyond", "outside the color identity". Gate run 14 wrote "Do you want to use any colors beyond Grist's color identity?", and the rule caught it. Runs 15 and 16 then wrote "within Grist's color identity", and the rule did not. The model kept the shape and changed one word, so the rule now reads the shape and not the wording. The evidence for a rule with no exception is in the catalog. Every row that asks about the colors carries `commander_set: false`, so it fires only when no commander is settled. A card's color identity therefore settles nothing, and the clause presumes that the card leads the deck. Conversation 3 is the case: the user wrote "Build around Grist, the Hunger Tide", which names a card and no role (D-118), and the format was still open in the same turn. Three guards hold it, as in D-150. The ask prompt forbids the shape at version 7. The phrasing guard refuses it and falls back to the catalog row. The linter refuses it as `names_a_card_color_identity`. |
| D-152 | 2026-08-26 | The eval reads the cards a question names (owner, answers OQ-42, extends D-143) | The eval invents facts about a card it does not know. Eval run 14 called Ran and Shaw and Quina, Qu Gourmet unreal. Eval run 16 called Cloak and Dagger, Entwined "not an applicable commander option", and Vincent, Vengeful Atoner and Shadow the Hedgehog "not valid Magic commanders". All five are real and legal in Commander, and all five come from a crossover set: Avatar, Final Fantasy, Marvel, and Sonic. Three of the four card claims of run 16 were false. The one true claim was about color identity, which the eval read from the transcript, so the model reasons well from facts it holds and invents the facts it lacks. The input now carries `cards_named_in_questions`, built from the snapshot for every card the questions offer. Each entry holds the name, the type line, the color identity, and whether the card can lead a deck. The prompt tells the reader that every card there is real, and to read a color identity from that list and never from memory. A string that names no card is left out, so nothing is claimed about it. This is the D-143 move: the eval judged the card-pool question as a presumption because the input did not say a collection was attached. D-149 stays as the backstop, and eval prompt version 3 invalidates the earlier scores. |
| D-153 | 2026-08-26 | An offered commander leaves the table when the colors exclude it (owner, completes D-148) | The pick row keeps the names on the table until the user refuses them (D-80, D-123). Nothing checked them again when the colors arrived later. D-148 filters the pool, and these names were already out. Probe 73 of gate run 16 is the case. Turn 1 named no colors, and the row offered Jaheira, Friend of the Forest, which is mono-green. The user answered "Red and white" on turn 2, and the same three names went out on turns 2 and 3. The eval caught it, and the linter could not: the text alone says nothing about a color identity. A hint source may now implement `IdentityChecker`. When the color slot fills, the agent drops every offered name the colors exclude, retires it so the pool never offers it again, and reopens the pick row. A name that still fits stays, so D-80 and D-123 hold: the agent swaps no name the user could still choose. The resolver tops the list up instead of replacing it, which is the change that keeps the survivors. An unknown name proves nothing and stays, as in D-140. |
| D-154 | 2026-08-26 | The commander pool offers two-commander pairs (owner, answers OQ-43, completes D-148) | D-148 asks one commander to hold every color the user named. A pair carries the union of two color identities, so the single-card test can not express a pair at all. Two defects sat under one label. The color arithmetic was wrong, and the pick row was single-commander shaped: probe 73 asked for "a Background commander pair" and got three single legends, one of them mono-green for a red-white request. The measurement of 2026-08-26 sets the scope. Of 3,384 commander-legal leaders, 177 can pair, and 31 Backgrounds exist. The exact-match pool holds 322 commanders at one color, 124 at two, and 47 at three, so D-148 costs almost nothing there. At four colors it holds one: WUBR is Breya, Etherium Shaper, WBRG is Saskia the Unyielding, and UBRG is Yidris, Maelstrom Wielder. A pair is the only practical way to build those decks. `CommanderPool` now scores a pair as one candidate when the union of the two identities matches, and it offers pairs when the user asks for one and when too few singles fit to fill the three names of the pick row. The pairing rules come from `rules.ValidPair`, which the deck validator already uses, so a pair the agent offers is a pair that passes validation. A pair reads "A + B" in the question, because the user chooses a pair and not half of one. A Background never leads, because it is not a commander on its own. `WantsBackgroundPair` narrows the request when the user names a Background: a Background carries no theme signal, so a pair that holds one loses on score to two themed legends, and the user who asked for a Background got none. |
| D-155 | 2026-08-26 | The app builds three formats (owner) | Commander, Standard, and Modern. Pioneer, Legacy, Vintage, and Pauper are removed. The owner asked for the change and then narrowed it twice inside the session, so read this row and not the earlier drafts. "Anything goes" is not a format a user may select. It stays what D-3 made it: a house-rules layer recorded on top of one of the three, which is what conversations 5 and 21 need when a user writes "any card, no ban list. Call it Modern". The four ids left the proto rather than staying unreachable. Every defect class this repository has found in the question layer was a path that could never fire, so an id no product surface reaches is the same trap. The numbers and the names are reserved. `buf.yaml` moved from FILE to WIRE_JSON, because FILE refuses a deleted enum value even when it is reserved. This was measured and not assumed: FILE reported all four deletions, and WIRE_JSON passes. WIRE_JSON still refuses every change that breaks the wire format or the JSON encoding, and this app has no external consumer that FILE protects. Nothing is deployed and no session is stored, so nothing needed migration. Two things died with Vintage. The restricted-card check is gone, because only Vintage carries a restricted list among the formats the app builds, so the branch could never fire again. The house-rules row lost its second option, which named Vintage rules, and it now confirms the no-ban-list reading alone. The first option keeps its exact words, because the D-119 net closes the key when the user repeats an option and three conversations answer with those words. `sixtyCard` also gained HOUSE, which it never held: no power row could fire for a house format, so such a session would have finished with no power level. Prompt version 8 stops the classify role from resolving a format the app declines. |
| D-156 | 2026-08-26 | No substitute for a removed format (owner, extends D-146) | Pioneer, Legacy, Vintage, Pauper, and Pauper Commander name no nearest format. Pool similarity was measured for each and it was not used. Legacy and Vintage read nearest to Commander at 0.994 Jaccard, because Commander is also an all-sets format of the same size. That number compares two different games, and a Legacy player does not want a 100-card singleton multiplayer deck. Read against the 60-card formats the app keeps, all four read Modern, and only Pioneer carries its whole pool at 100 percent. Legacy and Vintage carry 71 percent, so about three cards in ten would leave the deck. Pauper carries 79 percent and loses the commons-only rule that defines the format, which is the whole reason a person plays it. The owner chose to name no substitute for any of them. Brawl, Oathbreaker, Duel Commander, and Canadian Highlander still name Commander, and Alchemy still names Standard, because those five share a deck shape with the format they name. |
| D-157 | 2026-08-26 | A declined format keeps asking, and it preempts the open format question (assistant finding, extends D-125 and D-126) | The targeted live check before gate run 17 found two dead ends, and both cost nothing to find. Probe 18 answered "I do not build Pauper. Which format should I build instead?" and the user replied "Pauper." again. The row carried no repeat flag, so it never asked again, and the agent sent nothing at all on turns 2 and 3. Probe 21 was worse. The plain format row went out on turn 1, the user wrote "Call it Vintage" on turn 2, and D-126 blocks every other row on a key whose question is still out. The agent could therefore never say that it does not build Vintage, and the session ended with no format. Two changes fix both. The two rows that decline a format now repeat while the format slot is empty, which is the rule the commander pick already uses (D-73). A newly named unsupported format also retires the questions that are out, which is what D-125 does for a changed format: the question that replaces them must not sit behind them. This is the D-130 class, where a user says something reasonable and the agent answers with silence. |
| D-158 (amended by D-210) | 2026-08-26 | The decline row states the limit once, then only while the user insists (assistant finding, corrects D-157) | D-157 made the two rows that decline a format repeat while the format slot is empty. It fixed real silence and it went too far. Eval run 17 refused 14 questions on `format_unsupported_open`, which made it the worst row of the run, and the bad-question ratio rose from 11.3 to 13.3 percent. Probe 18 is the case. The agent said "I do not build Pauper" on turn 1, the user answered "Pauper." on turn 2, and the agent said the same sentence again on turn 3 after the user wrote "Casual power, and 25 dollars is the cap". The user had moved on and the agent had not. The cause is the same one D-125 found: the trigger reads `Ctx.Words`, which is the whole conversation, so a format named once keeps the fact true forever. The repeat now reads the current message alone. The row fires the first time the fact appears, and after that only while the message names the format again. Probe 18 now declines on turn 1, declines again on turn 2 when the user repeats "Pauper", and says nothing on turn 3. Silence there is honest: the question is out, it is recorded as asked with no answer, and the gate reports it. Two identical questions and then a wait is better than three identical questions. The first version of this fix was wrong, and a two-cent check found it. It read the firing fact to decide whether the format was newly named, so the fact oscillated: true on turn 1, false on turn 2, and true again on turn 3. Probes 61 and 97 asked a third time although neither user named a format after turn 1. A sticky reading of the two row ids decides it now. Measured over the eleven conversations that fire the row: 23 decline questions before, 13 after, and the two that remain doubled are the two where the user writes "Pauper." and "Legacy." again on turn 2. |
| D-159 | 2026-08-26 | The fixer needs no dollar cap (owner) | The owner runs Claude Code on a monthly plan, so the fixer agent bills against that plan and not per token. `--max-budget-usd` is dropped from `AUTOTUNE_FIXER_CMD`. The loop budget is a separate thing and it stands: `--budget 3.00` covers the gate and the eval, which run on `gpt-5.6-luna` and are billed per token. Nothing in the scripts couples the two. Every budget reference in `autotune.sh` is the loop's, `autotune-fix.sh` holds none, and a fixer that stops part way is handled: the loop reverts that iteration and continues. What bounds the fixer now is the plan's usage limit and the wall clock. The fixer also runs with `--permission-mode bypassPermissions`, which the owner chose. The revert guards are what contain it: a frozen path, a red build, or a worse run each undo the iteration. |
| D-160 | 2026-08-26 | The baseline carries its report as well as its score (assistant finding) | The first real start of the tuning loop failed in the first second. A supplied `--baseline` skips the `auto-000` run, and the loop then looked for the eval report of the run it had skipped. Iteration 1 read `pr7-question-eval-auto-000.md`, which no supplied baseline ever writes, and the fixer died with "no eval report". The two halves of a scored run have different readers: `tune-check` reads the JSON summary, and the fixer reads the report in prose. Only the summary had a flag. The loop now tracks `PREV_DOC` beside `PREV`. A supplied baseline finds its report by the name of its summary, so `.local/tune/run18.json` pairs with `docs/reference/pr7-question-eval-run18.md`, and `--baseline-doc` names it when the pairing does not hold. A missing report now stops the run with the path it wanted, before any provider call. A kept iteration sets both. The loop cost nothing to find this, because it failed before it called anything. |
| D-161 | 2026-08-26 | The provider keys do not reach the fixer (assistant finding, protects D-159) | The first fixer launch printed a warning: claude.ai connectors are disabled because `ANTHROPIC_API_KEY` is set and takes precedence over the claude.ai login. The warning is cosmetic and its cause is not. `autotune.sh` reads `.env` with `set -a`, so every key in it is exported, and Claude Code reads `ANTHROPIC_API_KEY` in preference to a plan login. The fixer would therefore bill per token to that key. D-159 dropped the dollar cap because the owner runs on a monthly plan, so the result was an uncapped API spend, with `bypassPermissions`, unattended, for a night. Three settings that are each reasonable made one that is not. `autotune-fix.sh` now runs the fixer through `env -u ANTHROPIC_API_KEY -u OPENAI_API_KEY`. The first name is the billing fix. The second stops a fixer from paying for its own gate run, which would spend real money outside the `--budget` accounting the loop keeps. Neither key is needed: the offline tests run against the fake provider, and the loop owns every paid measurement. |
| D-162 (amended by D-201 and D-212) | 2026-08-26 | A confirm row goes out as written (assistant finding, extends D-112) | The confirm row states the inference before it asks: "You asked for a competitive deck, so I will build to tournament level. Is that right?" The ask role dropped that first clause in every conversation of gate run 18. What went out was "Should I build this for tournament-level competition?", which asks a user who wrote "in a tournament" for the value they gave. The eval refused five questions on `power_sixty_confirm`, which ties it for the worst row of the run, and conversations 25, 37, and 63 all show the same rewrite. The row itself is innocent. `Fixed` already keeps a row that names an app limit away from the ask role (D-112), and a confirm row needs the same guard for the same reason: one sentence carries the whole meaning, and the model drops it. `house_format_limits` joins it. That row reads "do the normal limits hold: 60 cards, four copies per name, and a 15-card sideboard?", and the ask role turned it into "should the deck use a 60-card minimum, four copies per name, and a 15-card sideboard?" The eval read three questions in one, because the rewrite lost the words that bundle the three limits into one yes-or-no. Both rows hold no placeholder, so nothing is lost when the ask role never sees them. |
| D-163 | 2026-08-26 | The pick row asks again only with new names (assistant finding, amends D-73 and D-123) | The pick row carries `repeat`, so it fired every turn while the commander was empty. Conversations 1, 77, and 90 of gate run 18 each got the same three names twice, word for word, on a turn where the user answered some other slot. The eval refused four of them as duplicates, and `commander_pick` was one of the two worst rows of the run. D-73 gives the repeat a purpose: a user who says "none" gets three names they have not seen. Nothing else ever earns it. A new `repeat_on_change` flag narrows `repeat`, and the planner reads `Context.OfferChanged`: the names on the table against the names the row sent last. A refusal empties the table (D-73), and the color check of D-153 drops the names the colors exclude. Both give the user a new list. An unrelated answer gives nothing, so the row stays quiet and the question stays out with no answer. That is the D-158 rule for another row: two identical questions are worse than one question and a wait. D-80 and D-123 still hold, because the names on the table do not change. Their tests now read the table instead of a second question. |
| D-164 | 2026-08-26 | cEDH names bracket 5 (assistant finding) | Probe 75 of gate run 18 opens with "A cEDH deck", and the agent asked "Which power bracket should the deck target?" cEDH is competitive Commander, and the corpus bracket table names it bracket 5. The user gave the value in four characters and got the question anyway. Two changes read it. `CEDHRequest` fills the power slot with bracket 5, and "cedh" joins the commander signs, so the word names the format as well. The classify prompt says the same at version 9, and `power()` maps the word to the bracket when the model answers with it. "Competitive Commander" is not on the list. It says the deck is strong, it names no bracket, and bracket 2 and bracket 5 are the same distance from it. |
| D-165 | 2026-08-26 | Colorless closes the color slot (assistant finding, extends D-92) | Probe 73 of gate run 18 opens with "A colorless Commander deck built around big artifacts", and the color question went out. The eval refused it. No model call could have prevented that: the classify schema holds the five colors as an enum, so an empty list means "the user said nothing" and "the user said colorless" at once. This is the D-92 shape, where a schema suppressed the answer. A deterministic rule reads the word and closes the key. The slot takes the skipped state, because the proto documents `COLOR_C` for produced mana alone, and a colorless value in `Slots.colors` would be a new wire contract. The limit is recorded and not hidden: the slot says the user chose no colors, and the theme and the commander carry the colorless plan into PR-8. |
| D-166 | 2026-08-26 | A keep instruction closes the locked row (assistant finding, extends D-70) | Conversation 13 of gate run 18 opens with "Karlov of the Ghost Council lifegain deck, and keep Sanguine Bond in it". The agent asked "Should Sanguine Bond stay in the deck, or may I cut cards that do not fit the plan?" The user had answered it in the message that raised it, which is the D-122 class. `LocksCard` reads a lock verb within three words before the card name, and it closes the locked key. The negation guard runs on both sides of the verb, so "do not keep Sanguine Bond" locks nothing. The card name comes from the classifier, so the rule reads the user's own card and guesses none. The short form is read as well as the full one, which is the D-70 name rule. The slot closes on a value and not on a decline: the card stays in the deck. |
| D-167 | 2026-08-26 | A superlative delegates the commander choice (assistant finding, extends D-147) | Conversation 10 of gate run 18 wrote "Buy the best lifegain commander. Bracket 3, owned-first, and 60 dollars is the cap." The agent answered with three names and asked the user to choose one. The eval called it an assumed answer: the user had asked the agent to select. D-147 reads a delegation from phrases such as "you pick", and no phrase covers a superlative that names no card. `DelegatesCommander` reads "the best", "the strongest", and "the top", and it carries its own guard: the message must name a commander. Without that guard "buy the best lands" would hand over the commander choice whenever a commander question is out. The delegation closes both commander keys, and the generator takes the best commander of the pool, which is what the user asked for. |
| D-168 | 2026-08-26 | A session with no collection carries a buy list (assistant finding, corrects the budget row) | The corpus fires the budget row when the user mentions cost, when a buy list is needed, or when the pool mode is any-card. Only the first two reached the code, and the buy list came from a model fact alone. Gate run 18 asked about the budget in 7 of its 105 conversations, and 61 of the 63 sessions that held no collection were never asked at all. The eval named the budget in 20 of the slots nobody asked about, which is more than every other slot together. A user with no library must buy every card, so the buy list is a fact and not a judgment. The agent now sets it from the session: no collection, or a pool rule that allows a card the user does not own. Owned-only is the one rule that buys nothing. A second rule guards the new question. `NoSpendingLimit` reads "money is no object" and its family and closes the budget slot, so the row asks no user who has answered it. It reads one message and never the whole conversation, which is the D-125 rule. |
| D-169 (extended by D-211) | 2026-08-26 | The meta row offers the general sideboard (assistant finding, extends D-109) | The meta row read "What decks do you expect to play against? I tune the 15 sideboard cards to that." It presumes that the user has a metagame in mind, and probes 37 and 93 of gate run 18 had none. The eval refused both as an assumed answer. D-109 already says a question presumes no table and no event, and this row presumed the knowledge instead of the place. The row now reads "Which decks do you expect to face, or should I keep the sideboard general?", and it offers both answers. The user who knows their shop still names the decks, and the user who knows nothing has a way to say so. The slot closes either way, which is what D-90 asks of every row. |
| D-170 (not applied, D-203) | 2026-08-26 | The decline row offers the three formats (assistant finding, extends D-158) | The row that declines a format with no substitute read "I do not build {bad_format}. Which format should I build instead: Commander, Standard, or Modern?" The eval refused it as an assumed answer: the sentence directs the user to abandon the format they asked for, and it offers no way to say no. The row now reads "I do not build {bad_format}. Would you like Commander, Standard, or Modern instead?" The limit is stated first and the choice is offered second. The row keeps its options and its D-158 repeat rule, so nothing about when it fires has changed. |
| D-171 | 2026-08-26 | A named baseline that can not be read stops the run (assistant finding) | The first real iteration of the tuning loop was accepted and committed, and it should have been rejected. Two faults combined. `autotune.sh` runs `tune-check` from `go/`, and it passed the baseline path as the owner typed it, so `.local/tune/run18.json` resolved to `go/.local/tune/run18.json` and did not exist. `tune-check` then read a missing `-prev` as no previous run at all, printed "first run, so nothing to compare", and accepted. Every guard that needs a comparison was skipped in silence: the floor on questions asked, the floor on questions closed, and the ratio itself. Only the absolute guards ran. Run against the real baseline afterwards, the same iteration rejects twice over: the holdout ratio rose from 10.4 to 11.3 percent, and the checker prints the D-134 warning, because the tune split improved while the holdout did not. Two changes. `autotune.sh` makes the baseline paths absolute before anything reads them. `tune-check` now fails on a `-prev` that was named and can not be read, because a path the owner gave and a path the owner omitted mean different things. An omitted `-prev` is still a legal first run. The lesson is the shape and not the path: a silent fallback turned a guard off, and the loop reported success. |
| D-177 | 2026-08-26 | Every run labels its documents with its own stamp (assistant finding, protects D-65) | The loop labelled each iteration `auto-NNN`, counted from one every time it started. Two runs with `--max 1` therefore both wrote `auto-001`, and the second overwrote the gate document, the eval report, and the summary of the first. The measurements of one whole iteration were lost, and D-65 exists to stop exactly that. The label now carries the run stamp the branch already used, so a document reads `pr7-question-gate-20260826-164931-001.md` and no two runs can collide. `run_gate` and `run_eval` also refuse to write over a file that exists, which is the same guard the `GATE_OUT` target has held since D-65. The stamp is the fix and the refusal is the proof: if a label ever collides again the run stops before it calls a provider. |
| D-178 | 2026-08-26 | The whole judged set is the sanity check under the holdout (assistant finding, extends D-134) | The loop judges on the holdout, which is right: a fixer that rewords what it was shown moves the tune split and leaves the holdout alone (D-134). The holdout is also small. It holds about 120 questions, so two of them move the ratio by more than a point. Iteration 1 of 2026-08-26 shows the cost. The holdout fell from 10.4 to 9.2 percent, which is two bad questions, while the tune split rose by five and the whole judged set rose from 8.7 to 9.6 percent. The agent wrote three more bad questions than before and the loop called it progress. The holdout still decides, and the whole set is now a check under it: an iteration that improves the holdout while every judged question gets worse is rejected. The reverse shape already had a warning, and this one had nothing. Neither reading is a measurement on one sample. Both are guards against a loop that runs with nobody watching. |
| D-179 | 2026-08-26 | The ledger is per run (assistant finding) | The budget ledger was one file for every run ever. Each night counted the spend of the nights before it, so each run had less to spend than the one before. The ledger is now `.local/tune/ledger-<stamp>.txt`, one per run. The whole record is the set of those files. The script cited this number before the row existed. |
| D-180 | 2026-08-26 | A rejected iteration keeps its evidence (assistant finding, extends D-65) | The loop deleted the gate document, the eval report, and the fixer's diff of every rejected iteration. Iteration 1 of run 20260826-191225 cost $0.25, and after the reject nothing said what the fixer had tried. The owner asked that rejected work be preserved for later evaluation. The loop now writes the diff, the commits, the documents, and the verdict to `.local/tune/rejected/<label>/` before it resets the tree. Nothing under `docs/reference/` changes, so the M-5 sheet and the overwrite guards read nothing new. |
| D-181 | 2026-08-26 | The checker judges each change on its own rows, and keeps the ones that helped (owner decision) | The loop was all or nothing. Iteration 1 of run 20260826-191225 fixed three questions and broke three, and the whole set was thrown away. The owner asked for a loop that keeps the good changes and drops the bad ones. The fixer now commits each independent change on its own, with a `Rows` trailer that names the catalog rows it can move and a `Hypothesis` trailer. The checker compares the two runs question by question, keyed on conversation, turn, and row. A verdict that flipped on identical text is the judge's noise and counts for nobody. A verdict that flipped on changed text is charged to the change that declared the row. A change with more questions better than worse is kept. A change with more worse than better is dropped. A run whose undeclared rows got worse past the noise margin is rejected whole. The kept subset was never measured alone, so the loop runs the gate again on the conversations the kept rows touch and nothing else, which the owner asked for. It folds those verdicts into the baseline with `tune-check -merge`. Every summary now carries a count per conversation for that fold, and the holdout keys on the conversation number, so a partial run holds out the same conversations as a full one. `ReadRun` read the heading of the invented-by-row table as a 105th conversation, and every recount was one high. Fixed. |
| D-182 | 2026-08-26 | The loop keeps a lessons file and the fixer reads it (owner decision) | The fixer read the latest eval report and nothing else, and the tree was reset after a reject. Two fixers on two days tried the same confirm-row idea, and both were rejected. The owner asked for a loop that learns from a rejected change set. `tune-check` now writes what each decision taught to `docs/reference/autotune-lessons.md`: the hypothesis, the rows, the verdict, and the questions that got worse. The loop commits that file after every iteration, accepted or not. The fixer prompt carries the file, and it says a dropped hypothesis is not tried again in the same form. The file is frozen, so the fixer can not edit its own memory. The prompt file joins the frozen list, because it told the fixer it was frozen and the script did not hold it. |
| D-183 (amended by D-230) | 2026-08-26 | The checker carries a noise margin of three bad questions (assistant finding, extends D-178) | Run 18 and run 20260826-191225-000 measured identical agent code. The holdout moved from 13 to 10 bad questions and the whole set from 32 to 35, and only 16 of the bad questions were the same in both. The same document scored twice by the same judge differs on 25 of 365 verdicts, and its holdout moved from 10 to 12. The old checker rejected the identical code as a regression, and it accepted noise as progress (D-171). The ratio guards now count bad questions and allow a rise of three before they fail. The paired comparison on changed text is what decides a change. The whole-set message said "although the holdout fell" whatever the holdout did. Fixed. |
| D-184 | 2026-08-26 | Five loop guards from the audit (assistant finding) | The preflight refused tracked changes and let untracked files through, and the loop commits with `git add -A`. It now refuses an untracked file. A `--baseline` the loop could not read bought a new baseline for $0.25. It now stops. A document with no priced cost counted as zero spend, so a model with no price row had no budget. It now stops. The commit result was unread, and a failed push was silent. Both are checked. A gate that failed left an empty document that blocked its label. It is removed. |
| D-185 | 2026-08-26 | Sessions use optimistic concurrency (assistant finding, audit H-7) | Two overlapping Chat calls on one session each loaded the same snapshot, each paid three model calls, and the second Put erased the first turn. The no-repeat rule then repeated a question. The public session document now carries a version that counts the writes. GetState returns it, and Put reads the stored version inside the transaction. Put refuses a stale version with ErrConflict and writes nothing. Chat maps that to CodeAborted with a message to send the message again. The check runs after the turn, so the loser still pays for its model calls. A pre-turn claim is not built, because a lock adds failure modes and the lost turn is the defect. A document from before this change reads as version 0. The Priced flag of a session no longer flips on a turn that made no call. |
| D-186 (amended by D-205) | 2026-08-26 | Theme colors count only theme roles (assistant finding, audit M-1) | ThemeColors read the first 100 cards of the role-ordered list, which were all staples. Aristocrats reported black only and blink reported four colors. The tally now skips the staple roles. Measured after the fix: aristocrats black 88 percent and red 18 percent, dragons red 54 percent. A second color under the 25 percent share is not reported. The share is an open question for the owner. |
| D-187 | 2026-08-26 | A Doctor can pair (assistant finding, audit M-2) | The Doctor's companion card carries the partner kind, and the Doctor carries none. The pair walk admitted only cards with a partner kind, so no Doctor pair was offered. The walk now admits a card with the creature types Time Lord Doctor (CR 702.124m). The snapshot offers six Doctor pairs for a time travel request, from zero before. |
| D-188 | 2026-08-26 | Merged quantity stops at the row cap (assistant finding, audit M-3) | Duplicate rows merged with no cap, so a long file wrapped the count negative. A row that pushes a merged entry over 10,000 is reported as BAD_ROW. A row is then in the collection or in the report, never in part. The card counts saturate at the int32 maximum. The import report counts resolved rows, not merged entries, so resolved plus unresolved equals the input rows. |
| D-189 | 2026-08-26 | A deck entry count under one blocks (assistant finding, audit M-4) | The rules engine summed a negative count, so a 101-card list plus one entry of minus one passed the size check. A count under one is the BLOCK finding bad_count, and no sum reads that entry. |
| D-190 | 2026-08-26 | Game Changer commanders count (verified, audit M-15) | A Game Changer commander counts as one of the three at Bracket 3 and can not play in Brackets 1 and 2. Source: magic.wizards.com, Introducing Commander Brackets Beta, read 2026-08-26. The engine already counted the command zone. The unverified note is removed from the corpus, the code, and brackets.json. |
| D-191 | 2026-08-26 | Corpus facts corrected against Wizards text (assistant finding, audit M-15) | Pauper Commander uses an uncommon creature, Vehicle, or Spacecraft as the commander, and the other 99 are commons, at 30 life (pdhhomebase.com, 2026-08-26). The bracket 3 row put the turn-six qualifier on extra turns. Wizards puts it on two-card combos, and extra turns are not to be chained. The Commander ban list holds 42 named cards, so "38 more" reads "35 more". The Partner text rule is CR 702.124i, not 702.124f, in derive.go and checks.go. Section 2.4 now says seven formats have no substitute and five name a nearest format. |
| D-192 | 2026-08-26 | Brawl is 100 cards and Standard Brawl is 60 (owner decision) | The corpus said Brawl is 100-card with a commander and Standard Brawl is 60-card. words.go said Brawl is 60-card. The owner ruled: Brawl is 100-card with a commander, and Standard Brawl is 60-card with a commander. The comment in words.go now matches. The nearest format stays Commander for both, and the app builds neither. |
| D-193 | 2026-08-26 | The eval summary and the calibration files are never overwritten, and the containers run as nonroot (assistant finding, audit M-7 and M-13) | EVAL_JSON had no overwrite guard, so a rerun could replace the baseline summary the checker compares against. It has one now, and eval-calibrate writes to a named stem that it refuses to overwrite. The Makefile defaults name run 18. The api and worker images run as the distroless nonroot user, on digest-pinned bases. Compose binds every published port to 127.0.0.1, waits on health checks, and keeps the Firestore export in a volume. CI runs shellcheck over scripts. dev.sh kills the process group, so a compiled worker dies with go run. |
| D-194 | 2026-08-26 | The hand-off is a short live file, and the session narratives are a dated log (assistant finding, audit H-8) | SESSION-HANDOFF.md held 819 lines of stacked session narratives, three different baselines, two prompt versions, and a next-steps list already done. A fresh session was sent to a dirty pr-7b at a stale commit. The narratives moved verbatim to docs/reference/session-log-2026-08-23-to-26.md. The hand-off is now about 70 lines: the current numbers, the state, the next steps, the facts that expire, and how to resume. CLAUDE.md, README.md, AGENTS.md, the roadmap markers, and the two question queues were brought to the same date. |
| D-195 | 2026-08-26 | A retired question leaves the asked state (assistant finding, corrects D-125 and D-126, audit H-5) | `RetireOutstanding` cleared `Ctx.Outstanding` alone. The slot kept the asked state, and only an answer or a decline leaves that state. A user who changed the format after the pick row went out never reached ready, and the classifier got the dead key every turn. A retired key now returns to the unspecified state. The row id stays in `Ctx.Asked`, so the same row does not ask twice, and a replacing row may ask on the key. |
| D-196 | 2026-08-26 | Three advisory rows close without the model (assistant finding, extends D-99, D-112, D-129, audit H-6) | The illegal-commander row, the one-deck row, and the out-of-scope row closed only through `closed_keys`. A named commander now closes the illegal row with the other commander rows. A message that names one deck skips the one-deck key. A message that fills a format, theme, color, or commander slot skips the scope key. Each rule reads one message. |
| D-197 (superseded by D-226) | 2026-08-26 | The not-owned row carries its own key (assistant finding, corrects the corpus row, audit M-5) | The row used the commander key, and every named commander filled that key first, so the row could never fire. A skip on it would also have replaced the named commander. The key is now `commander_owned`, the slot stays `commander`, and a skip on the key never touches a filled slot. |
| D-198 | 2026-08-26 | The PR-6 facts are read inside the turn (assistant finding, extends D-124, audit M-6) | `agentsvc` read the thin-theme count, the weak pool, and the missing commander before the turn. A one-message answer filled the format and the theme after that, so the plain pool question went out and the {n} count of D-67 never showed. `RefreshFacts` reads them from the slots as they stand, and the agent calls it after the classify call. |
| D-199 | 2026-08-26 | A sub-format this app does not build names no format (assistant finding, extends D-112, audit M-9) | "Duel Commander" holds the word "commander", and the classifier reported Commander for it. The format filled, and the decline row never fired. A message that names an unsupported format now gives no format, from the classifier or from the word rule, and the decline row asks. |
| D-200 | 2026-08-26 | A missing score keeps the catalog row (assistant finding, extends D-25) | The score call may omit a row. Its zero value entered the M-4 record as a fit of 0, the worst fit ever measured. The row now goes out as written with the threshold as its fit, with a warning in the log. Every fit is clamped to 0 to 1. The color slot is replaced only when at least one new color is valid. |
| D-201 | 2026-08-26 | D-162 holds for the house-limits row alone (assistant finding, amends D-162) | The confirm row is not fixed. Its fixed text asserts a premise the user may not have given, and eval run 20260826-191225-001 refused it in conversations 33, 49, 68, and 71. The house-limits row stays fixed: the two refusals on it in run -000 were rewrites that split one yes-or-no question into three, and run -001 refused none. |
| D-202 | 2026-08-26 | A lock verb after the card name closes the locked row (assistant finding, amends D-166) | Conversation 74 wrote "Sol Ring goes in it" and got the locked question in runs 18, -000, and -001. `LocksCard` now reads "goes" or "stays" directly after the name as well as a verb before it. |
| D-203 | 2026-08-26 | D-163 to D-169 are rebuilt on the evidence, and D-170 is not (owner decision, amends D-171) | The owner asked that each of D-162 to D-170 be judged on its merit and rebuilt when it holds. Each row was judged against the verdicts of run 18 and runs 20260826-191225-000 and -001. D-163, D-164, D-165, D-166, D-167, D-168, and D-169 each name a defect present in all three, and their code is rebuilt on pr-7c. D-170 names a wording fault that appears once in 9 refusals on its row, and the row keeps its text. The eval of the aefb358 run was lost to the D-177 collision, so the cost of D-168 is measured by the gate alone: 374 to 441 questions. The next gate run measures the ratio. |
| D-204 | 2026-08-26 | The wrongly accepted iteration is dropped (owner decision, closes D-171) | Branch `auto-tune/20260826-164931` held the iteration that D-171 judged wrong, and the rows D-172 to D-176 its fixer wrote. The owner dropped the branch. Its ideas were re-judged on the evidence under D-203, so nothing of value was lost. The numbers D-172 to D-176 are retired. |
| D-205 | 2026-08-26 | A theme names its lead color and at most one second color (owner decision, amends D-186) | The flat share of D-186 reported black alone for aristocrats at 25 percent, and five colors for dragons at 15 percent. The owner chose the third option offered: the lead color always goes out, and the strongest second color joins it when its share reaches 15 percent. Measured on the snapshot of 2026-08-24: aristocrats black and red, dragons red and green, lifegain white and black, blink white and blue. The color question never names more than two colors. |
| D-206 | 2026-08-26 | D-168 stays, and the next run measures it (owner decision, confirms D-168) | D-168 fires the budget row for every session that must buy cards, which raised the row from 8 to 86 conversations in the one gate that ran it, and that gate's eval was lost. Every budget question the eval has scored was warranted, and the eval names the budget as the most missed slot. The owner chose to keep the rule and let the next run judge it. The checker charges any new bad budget question to the row, so a bad result drops that change alone (D-181). |
| D-207 (superseded by D-226) | 2026-08-26 | The not-owned row stays dormant until PR-8 (owner decision, holds OQ-36 open) | The M-5 fix of D-197 gave the not-owned row its own key, and D-198 refreshed the not-owned fact inside the turn. Together they made the row fire, which OQ-36 had left undecided. In gate run 20260826-212512-000 it fired 14 times, scored 0.05 each time, and was replaced by an invented question 13 times, which took the catalog-only count from 28 to 24 and failed the gate. The owner chose to keep the key fix and make the row dormant behind a condition that does not hold before PR-8. OQ-36 stays open. |
| D-208 | 2026-08-26 | A declined pick delegates the commander (assistant finding, extends D-147) | Conversation 39 of gate run 20260826-212512-000 went "Make me a good deck", "you pick", "Whatever you think is best", and the session called itself ready with no commander. The classifier declined the pick row, and the skip closed the row's own key and not the commander slot. The D-147 rule could not close it either, because its guard reads the outstanding pick question the skip had just removed. Run 18 escaped only because its classifier did not set wants_suggestion on turn 2, so the base commander row asked instead. A declined pick now skips the commander slot as well: the generator picks (D-147). TestDeclinedPickClosesTheCommanderSlot replays the three messages. |
| D-209 | 2026-08-26 | The power confirm row asks about an inferred step alone (assistant finding, narrows D-107) | The row carries its own key, so a power slot the user filled did not cancel it. Conversation 80 of gate run 20260826-220840-000 answered "TOURNAMENT" and got "Should I build the deck for tournament-level competition?" one turn later. Conversation 71 answered "FNM." and got the same row, which the ask role then replaced with an invented question at fit 0.20. The eval refused both as duplicates. The agent now marks the step it fills in itself, and the row fires on that mark. A step the user named reaches the slot through the classify call, so no inference runs and the row stays silent. |
| D-210 | 2026-08-26 | The format decline rows repeat only on another format (assistant finding, narrows D-158) | D-158 lets the row ask again while the user keeps naming the unsupported format in the current message. Probe 18 named Pauper twice and probe 26 named Legacy twice in gate run 20260826-220840-000, and each heard the same sentence twice. The eval refused the second one as a duplicate. Both rows now carry `repeat_on_change`, and the change they read is the format name the row sent last. A second unsupported format still gets its own sentence. This is the D-163 rule for the format rows: silence beats the same sentence twice. |
| D-211 | 2026-08-26 | The meta row offers the general sideboard first (assistant finding, extends D-169) | "Which decks do you expect to face, or should I keep the sideboard general?" asks for a list and for a yes-or-no answer in one sentence. The eval of gate run 20260826-220840-000 refused it in conversations 44 and 76 as two questions in one, and it read the first clause as a presumed event in conversation 44. The row now leads with the general sideboard, so one choice goes out: "Should I keep the sideboard general, or will you name the decks you expect to face?" The options change place with it. |
| D-212 | 2026-08-26 | The house-limits row names no list of limits (assistant finding, extends D-162 and D-201) | D-162 made the row fixed, so the ask role could not split it into three questions. In gate run 20260826-220840-000 the row went out word for word in conversation 61, and the eval still refused it as two questions in one: the sentence itself named three limits. The text is now "Inside your house format, do the normal 60-card deck limits hold?" The row keeps its fixed flag and its two options, so a user who wants other limits still says so. |
| D-213 | 2026-08-26 | The store row names the formats it builds (assistant finding, extends D-155) | "Which format does your event run: Standard, Modern, or Commander?" reads the list as the formats the event may run. An FNM can run Pioneer, so the eval of gate run 20260826-220840-000 refused the question in conversation 7 for omitted information. The list is true of this app and not of the event, and the row now says so: "I build Standard, Modern, and Commander. Which one does your event run?" The user who answers Pioneer still reaches the decline row (D-146). |
| D-214 | 2026-08-26 | A build-around card answers the locked row (assistant finding, extends D-166) | Conversation 27 of gate run 20260826-220840-000 opened with "Build around Grist, the Hunger Tide, but not as my commander". The locked row then asked on turn 2 whether the deck may cut Grist. `LocksCard` reads a lock verb before the name, and "build around" is not one. The bare phrase must stay outside `LocksCard`. It leaves open whether the card leads the deck, and a commander is never a locked card (D-70). A new rule, `BuildsAround`, reads the phrase. The agent applies it only to a card in `LockedCards`, which holds the cards that sit in the 99. `TestBuildAroundLocksTheCard` replays the message. |
| D-215 | 2026-08-26 | The competitive fact needs the user's own words (assistant finding, extends D-199) | Conversation 33 of gate run 20260826-220840-000 opened with "A Modern deck for an event". The classifier read the word "event" and set `power_competitive`. The agent then filled the tournament step and closed the power slot. The user answered "FNM level" two turns later, so the inference was wrong. The fact now comes from `CompetitiveRequest` alone, as the format comes from the word rule. The sign list carries the paraphrases the classifier caught, such as "as strong as possible". `TestAnEventIsNotACompetitiveRequest` holds the rule. |
| D-216 | 2026-08-26 | The agent states an inferred power step and asks nothing (owner decision, amends D-107, D-90, and D-209) | The eval of gate run 20260826-220840-000 refused the power confirm row six times, always as a duplicate. Two refusals name a step the user gave, and D-209 answers those. The other four name a well-founded inference. Conversations 25, 49, 63, and 68 said "for a Modern tournament", "whatever is winning", and "the strongest deck". The eval asked to remove the row. It offered the wording "I will target tournament-level power unless you want a different level". The owner chose to remove the row. The agent fills the tournament step, closes the slot, and marks the step with `PowerInferred`. PR-8 states the step in the deck plan. CAUTION: D-215 must hold first. Without it a wrong inference reaches the user, and no question goes out. |
| D-217 | 2026-08-26 | The checker rejected every iteration, because its drift guard reads the ask role (assistant finding, extends D-181 and D-183) | `Decide` rejected a whole run when more than three questions got worse on rows no change declared. That threshold came from `DefaultNoise`, which D-183 measured on holdout bad questions. The two quantities are not the same, and the second has a far larger floor. Run 18 and run 20260826-191225-000 ran identical agent code. On rows no change declared, 15 questions got worse and 8 got better. 12 of the 15 moved on changed text, because the ask role rewrites its wording every run. The guard therefore rejects identical code, and it rejected all seven loop runs of 2026-08-26. Iteration 20260826-220840-001 shows the cost: the checker kept all five changes on their own rows, and it then rejected the run on 5 undeclared questions. The same `meta` change scored 2 better and 0 worse in that run, and 1 better and 3 worse in run 19. The guard now reads the harm against the help, and `DefaultDriftNoise` is 8. `TestIdenticalCodeIsAccepted` replays the measured shape and fails at the old threshold. CAUTION: one same-code pair sets the number. Measure a second pair before you lower it. |
| D-218 (amended by D-240, D-247, and D-259) | 2026-08-26 | A built deck keeps 85 percent of a named precon (owner decision, answers OQ-21, extends D-113) | D-113 names the precon in the card-pool question, and no number said how much of it must survive the build. The owner set the share at 85 percent, and called it a start and not a settled figure. PR-8 holds the rule. An upgrade of a named precon keeps at least 85 percent of the precon cards that are legal in the format. CAUTION: the number is a first choice. Measure it against real upgrade requests in PR-8, and report what the share costs the deck. |
| D-219 | 2026-08-26 | An occasion is not a power step (assistant finding, extends D-215) | Conversation 33 of gate run 19 opened with "A Modern deck for an event". D-215 stopped the classifier fact, and the classifier still answered the tournament step itself. A filled step closes the power slot, so no power question went out, and `agent.go` reads a step of FNM or tournament as a competitive request (D-132). The theme row for a competitive deck then fired on a premise the user never gave. The user answered "FNM level" two turns later. `OccasionOnly` now drops a 60-card step when the message names an occasion, names no step, and asks for no strong deck. The open power row asks instead. `TestOccasionIsNotAPowerStep` holds the rule. |
| D-220 | 2026-08-26 | A named card that can not lead a deck has a settled role (assistant finding, extends D-129 and D-70) | Conversation 74 of gate run 19 named Sol Ring, and the agent asked "Should Sol Ring be your commander or one of the 99 cards?". Sol Ring can not lead a deck, and the card index says so. D-129 read the commander list alone, and this card was never on it: the user named a card and gave it no role. The agent now reads the index for every named card. A card that can not lead goes to the locked list, which closes the role question (D-70). `TestNamedCardThatCanNotLeadSettlesItsRole` replays the message. |
| D-221 | 2026-08-27 | A digital default printing is replaced by the newest paper one (assistant finding, extends D-17 and D-6) | The `oracle_cards` file names one printing per card, and for some cards that printing is online only. The first real generate call of PR-8 returned three such cards in one deck: Diamond Valley, Lake of the Dead, and Spawning Pit. All three have paper printings. Diamond Valley read as Masters Edition, an online set, and its paper printing is Arabian Nights of 1993-12-17. A digital default costs the price search, which skips digital printings (D-17). It also shows the wrong image and the wrong artist credit (D-6). The rules engine then reports a paper problem the card does not have. `Printing` now carries the digital flag, the release date, and the display fields. `NewIndex` replaces a digital default with the newest paper printing of the same card, because that is the one a player can buy. Measured on the snapshot of 2026-08-24: 1,062 of 34,890 cards swap, which is 3.0 percent. `Index.PaperSwaps` reports the count, so a snapshot that swaps far more is visible. `TestDigitalDefaultPrintingIsSwappedForPaper` holds the rule, and the three findings are gone from the probe deck. |
| D-222 | 2026-08-27 | The shortlist is the whole contract with the generate role (assistant finding, implements F-13) | F-13 says a model can name a card that exists and is not the card meant, and that a fuzzy match hides it. `generate.Pool` holds every card the model may name, and the normalizer matches an exact name only. The match folds the case and the outer spaces, and nothing else. A near name is a miss, and the code never substitutes one. A card outside the pool is a miss even when the card index knows it. The pool spelling wins, so the model's casing never reaches the deck. Upgrades join the pool only when the session may buy cards, because a deck must not name a card the user can neither own nor buy (D-37). A miss reports the near names to the repair turn as a hint. Measured on 2026-08-27 over two real calls: 198 card names, and every one was copied exactly. |
| D-223 | 2026-08-27 | One repair turn covers a miss and a block finding (assistant finding, implements the PR-8 roadmap) | The roadmap gives the model one repair turn. A name the shortlist does not hold and a block finding from the rules engine both need the same turn: the findings go in, and the whole deck comes back. A name that misses twice never reaches the deck, and the user reads a note that says why the card is absent. The deck reaches the user with its `ValidationResult` attached whatever the verdict, because the user sees the referee's answer. A build failure never loses the turn: the questions are stored and sent before the build starts. |
| D-224 | 2026-08-27 | The summary linter is a net and not a gate (assistant finding, extends F-26) | F-26 says the phrasing role invents claims about the game, and that the deterministic linter found neither of the two false claims that reached a user. The deck summary is prose, so it has more room for this than a question ever had. `LintSummary` reads eight shapes of a rules claim, and it can not read the truth of one. A match is a warning and never a block, because a shape is not a falsehood, and a block would refuse a correct deck over a phrase. CAUTION: a false rule phrased outside these shapes still reaches the user. The judge lane of PR-7B is the real check for F-26. |
| D-225 | 2026-08-27 | The pool holds the basic lands, and a short list is finished with them (assistant finding, extends F-13) | Deck gate run 1 of 2026-08-27 returned 12 decks and not one basic land. `candidates` leaves basic lands out of the shortlist on purpose, because ranking a Plains against a real card means nothing. The generator reads the shortlist and nothing else (D-222), so it could never name one. Two Commander decks came back with 99 cards against a size of 100, and conversation 5 asked for a Plains the pool did not hold. `generate.BasicLands` now adds the basics of the deck's color identity to the pool, and a colorless deck gets none. `padWithBasics` finishes a list that is at most five cards short, and it reports the count as an INFO finding. A larger gap is not a counting slip, and it stays a block finding for the user to see. Measured after the fix: both failing prompts returned 99 cards with no block finding and no repair turn. |
| D-226 | 2026-08-27 | The not-owned commander row is retired, and the weak-pool row gets a probe (owner decision, closes OQ-36, retires part of D-197 and D-207) | OQ-36 asked whether to reorder the two owned-commander rows or let PR-8 handle them. The record said the not-owned row had no owned options to name before the generator existed. That is wrong. Gate run 20260826-212512-000 shows the row resolving real options, for example "Owned options: Abuelo, Ancestral Echo, Oji, the Exquisite Blade". `MissingCommander` is correct as well. The row still scored 0.05 on all 14 firings, and the ask role replaced 13 of them. PR-8 answers the same question without asking it. `checkOwnership` reports ownership per card after the build, with the exact count, as a warning in owned-first and a block in owned-only (D-37). That costs no turn, names the card, and arrives with the deck. Deck gate run 2 shows it: "not_owned: Karlov of the Ghost Council: the deck needs 1, the collection has 0". The owner retired the row. The catalog, the corpus, and the dormant constant of D-207 are gone. The owner then asked that the fact be pulled from every place that held it, so a later reader is not misled by machinery no row uses. `Context.CommanderNotOwned`, `When.CommanderNotOwned`, `FactSource.MissingCommander`, and `CandidateHints.MissingCommander` are all removed. `TestNotOwnedRowIsRetired` asserts the row stays out of the catalog, and `TestNilHintsAreSafe` keeps the nil-source guard the removed test also held. The weak-pool row asks a different question, and it has never fired. Conversation 105 is a probe that forces it: an owned-only Eldrazi request the collection can not lead. |
| D-227 | 2026-08-27 | The prompt cache saves a fifth of a generate call, not nine tenths (assistant finding, measures the PR-8 roadmap claim) | The roadmap says a cache read costs a tenth of a fresh read, and it names two rules that protect the lever: keep the stable text first and the session text last, and hold one cache key per session. Measured on 2026-08-27 with four generate calls on one session id, on `gpt-5.6-terra`: input 7,433 tokens with 7,427 cached, which is 99.9 percent, on every call. The tenth is right for the input alone. Cached input costs $0.20 per million against $2.00 fresh, and the input of one call falls from $0.01487 to $0.00149. A generate call is output-heavy, though: 7,433 input against 4,404 output, and output costs $12.00 per million. The whole call therefore falls from $0.06771 to $0.05435, which is a saving of 19.7 percent and not 90 percent. The lever holds and it is worth keeping. Do not plan a budget on the nine-tenths figure. CAUTION: the four calls were all warm, because an earlier run had written the cache. The cold figure above is computed and not measured. |
| D-228 | 2026-08-27 | The cost accounting is correct, and `input_tokens` counts cached tokens too (assistant finding) | One probe run of 2026-08-27 reported `cached_input_tokens` of 0 while its cost matched the cached rate, which put every spend figure of the session in doubt. The cache measurement settles it. For call 1 the price table gives (7433-7427) at $2.00 per million, plus 7,427 at $0.20, plus 4,404 at $12.00, which is $0.05435. The run reported $0.05435. `input_tokens` is the whole input and `cached_input_tokens` is the part of it the provider served from the cache. The two are not added. The single earlier anomaly is unexplained and it is one data point. Every cost this session reports is trustworthy. |
| D-229 | 2026-08-27 | The judge lane is the check for F-26, and it is cheap (assistant finding, answers F-26 for PR-8) | F-26 says the model invents claims about the game and that no deterministic check caught them. The deck summary is prose, so it has more room for this than a question ever had. `cmd/summary-judge` reads a deck gate document and asks the judge role whether each summary states a rule of the game, and whether that statement is true. The judge runs on `claude-sonnet-5`, another provider than the generator, so it never rates its own work (D-22). Measured on deck gate run 3: 13 summaries, 12 clean, 1 states a rule, and 0 state a false rule. The one flag reads "Karlov leads a ... deck", which the judge rated true and which describes the deck more than it states a rule. The deterministic net of D-224 flagged none of the 13, so the two agree on 12 of 13. Cost: $0.0440 for 13 summaries, which is $0.0034 each. That is cheap enough to run beside every deck gate, and the roadmap gate should adopt it. |
| D-230 | 2026-08-27 | A second same-code pair confirms the drift margin and doubles the ratio margin (assistant finding, extends D-183 and D-217) | Run 21 scored worse than run 20 on the whole set, 18 bad questions against 27, which is 9 and the margin of D-183 is 3. Nothing in the agent changed between them. D-226 retired a row whose fact was hard-coded false, so the row could never enter a plan and the score role never saw it. The one new conversation, 105, contributed 3 judged questions and 0 bad ones. The whole rise came from conversations nobody touched. The pair is therefore a second same-code measurement, which the CAUTION of D-217 asked for. Drift, the D-217 quantity: harm 13 and help 6, so drift is 7. The D-217 pair gave harm 15 and help 8, so drift is 7 as well. Two independent pairs agree, and `DefaultDriftNoise` stays 8. The ratio guard does not survive as well. Two same-code pairs moved the whole-set count by 3 and by 9, so `DefaultNoise` rises from 3 to 9. CAUTION: that guard can no longer tell a nine-question regression from the judge. It is a coarse backstop, and the paired comparison on the declared rows is what decides a change (D-181). Run 21 is not a regression, and the gate held at 29 of 30. |
| D-231 | 2026-08-27 | The price follows the printing the index shows (assistant finding, extends D-221 and D-17) | D-221 replaced a digital default printing with the newest paper one, and it left `PriceUsd` alone. A digital printing carries no USD price at all, only MTGO tickets: the `oracle_cards` row for Diamond Valley reads `usd: null` and `tix: 0.81`. So all 1,062 swapped cards showed a paper printing at a price of zero, which reads as free. Nothing consumes the price today, and the budget slot and `DeckCard.price_usd` are both in the roadmap, so the fault would have reached a budget deck. `Printing` now carries its USD price, and the swap sets `PriceUsd` and `PriceAsOf` from the paper printing. Measured on the snapshot of 2026-08-24: Diamond Valley $693.33, Lake of the Dead $121.33, Spawning Pit $1.58, each $0.00 before the fix. 32,546 paper cards carry a price and 1,348 do not, which are the cards with no market price. |
| D-232 | 2026-08-27 | The weak-pool row is retired, and the generator picks a delegated commander (owner decision, closes the rest of OQ-36, extends D-147 and D-208) | The weak-pool row had never fired in any gate run, and conversation 105 of run 22 shows why. A user who says "you pick" delegates the commander, and D-147 and D-208 skip the slot for the generator. `plan.go` treats a skipped key as filled, and the weak-pool row carries no key of its own, so its key is the commander slot. The delegation therefore silences the row, and the user who delegates is the thin-collection user who needs it. The owner retired the row. PR-8 reports the thin pool with the deck instead, as `thin_commander_pool`, from the library it actually read. Tracing this found a larger fault. No golden prompt named no commander, so PR-8 had never run the delegated path at all. A delegated session reaching the generator would have carried no commander and failed the engine on `no_commander`. `agentsvc` now reads `CommanderPool` and picks the best commander the session delegated, partner included. Prompts 14 and 15 cover the path, one in any-card mode and one owned-first. |
| D-233 | 2026-08-27 | Only Commander carries a command zone, and the gate reports the sideboard (assistant finding) | A probe of 2026-08-27 built a Modern deck and handed it a commander, and the engine refused it: "Karlov of the Ghost Council is not legal in this format". `generate.Request.Commanders` was not guarded by format, so any 60-card session whose classifier reported a commander name would have built an illegal deck. `assemble` now drops the command zone for every format but Commander, and `TestSixtyCardDeckCarriesNoCommander` holds it. The deck gate also could not see the sideboard. It reported the main deck alone, and the engine refuses a sideboard that is too big and never one that is too small, so a 60-card deck with no sideboard would pass every bar. The gate and the probe now report both counts. Measured on the same probe: the model returns 60 main and 15 sideboard, so the sideboard was never missing. The gate was blind to it, not broken. |
| D-234 | 2026-08-27 | Every gate bar is proved to fail (assistant finding) | A bar that has never failed may not be a bar. The block bar was proved by deck gate run 1, which failed on it. The invented-name bar and the F-26 bar had never fired once across four runs. `report_test.go` now fires each one on purpose: a block finding, a note from a name that missed twice, a judge verdict of a false rule, and a claim the judge rated false under a softer verdict. Each fails the run. Two control cases keep the bars from being too strict: a clean run passes, and a rule the judge rated true passes. Run 4 found two such true statements. |
| D-235 | 2026-08-27 | One build is bounded (assistant finding) | The llm client caps each call at three minutes, so a generate and a repair together can hold the stream for six. Nothing capped the build itself. `DefaultBuildLimit` is four minutes, and `WithBuildTimeout` overrides it. A build past its limit ends the turn with a status line that says so, and the turn is still stored, so the questions are not lost. |
| D-236 | 2026-08-27 | The deck carries its prices, and a budget warns (assistant finding, extends D-17 and D-231) | D-231 gave every card the price of the printing the index shows, and the deck still carried none: `DeckCard.price_usd` was never set. Prompt 12 of the deck gate asks for a deck under 100 dollars, and nothing measured or reported the cost. `Normalize` now fills the price per copy. `BuyCost` counts the copies the collection does not cover, and `DeckCost` counts every copy. The gate reports both. A deck over budget gets a warning and never a block: the price is a daily estimate and not a rule of the game, and a deck the user can trim is more use than no deck. CAUTION: `budget_usd` is documented as the cap on the cards the user must buy, so that is what the check reads. The budget-scope row asks whether the cap covers the whole deck instead, and nothing stores that answer. |
| D-237 | 2026-08-27 | The production path has an end-to-end probe (assistant finding) | The deck gate calls `internal/generate` directly, so it never ran `agentsvc`. Every fault found in the build so far lived in that gap: the delegated commander of D-232 survived four deck-gate runs because no gate prompt reached `agentsvc` at all. `cmd/chat-probe` drives the real Chat RPC from a first message to a finished deck, through the whole path, with a real provider and an optional collection. Two sessions of 2026-08-27 both produced a legal deck with no block finding. An any-card session named its commander and cost $372.66 to buy. An owned-only session delegated the commander, the generator picked Denethor, Ruling Steward from the library, and the deck cost $0.00 to buy and $193.91 whole, which is the owned-mode answer. NOTE: a turn that answers some slots and leaves a question outstanding sends no question and no status, because a question already out is never asked again. The user sees nothing come back. The chat UI of PR-12 holds the outstanding question, so this is a UI concern and not a build fault. |
| D-238 | 2026-08-27 | The budget scope is stored and the build reads it (owner decision, completes D-77 and D-236) | The budget-scope row asks "Is that a cap on the cards you buy, or on the whole deck value?", and nothing stored the answer. No proto field, no context field, and no classifier field held it, so the agent asked the question and discarded the reply. That is the fault D-226 and D-232 retired two other rows for, and this row asks rather than sits idle, which is worse. The owner chose the contract change. `Slots.budget_scope` carries a new `BudgetScope` enum, and `buf breaking` passes because a new field is additive. The classify role reports the scope, the agent stores it and closes the row on it, and the build reads it: a cap on the cards to buy checks `BuyCost`, and a cap on the whole deck checks `DeckCost`. An unspecified scope reads as the cards to buy, which is the common meaning and the proto's old wording. CAUTION: the classify prompt is version 10 now. A score taken at version 9 does not carry over (D-66), so runs 19 to 22 do not compare with the next run. |
| D-239 (amended by D-252) | 2026-08-27 | The gate harness can start a conversation after a build (owner decision, closes OQ-38) | OQ-38 asked whether gate conversations 11 and 12 stay, because the variance row and the freeze need a stored deck and neither could work before PR-8. The premise was wrong. `AfterBuild` is set in one place, `agentsvc` reads it from the session's stored decks, and the questions gate never runs `agentsvc`: it drives the questions agent directly with a synthetic state. So the blocker was never PR-8. The harness had no way to say that a conversation starts after a build. In run 22 both conversations were cold starts: each asked format, theme, and colors, and each ended with four or five slots unanswered. They tested nothing they were written for. The conversation file now carries `has_deck` and `frozen`. `has_deck` sets the fact and fills the slots a finished build must have settled, because a stored deck is proof they were answered. `frozen` sets the freeze of D-68. The classify prompt moved to version 10 the same day, so the run series re-baselines anyway (D-66, D-238). That made this the cheapest moment to change a frozen conversation, and the 30-conversation denominator of D-105 is kept. |
| D-240 | 2026-08-27 | The precon share waits on a data source (owner decision, defers D-218, opens OQ-40) | D-218 sets the share at 85 percent, and `checkPreconShare` implements it and is tested. Nothing can reach it. The share needs the precon's card list, and no precon decklist source exists in this repo. The question workflow holds `PreconName`, which is the first card the user named and not a list, and the Scryfall snapshot carries set codes with no per-product decklist. The owner deferred the rule rather than drop it or build an ingester now. D-218 keeps its answer, the code says where it waits, and OQ-40 asks for the source. |
| D-241 | 2026-08-27 | The build-run freeze is retired (owner decision, amends D-68) | D-68 froze every slot when a build run starts, and said a later change starts a new run. `Freeze` was never called outside a test, and calling it would have stranded a session: the frozen guard returned before the classify call, so a frozen session could never detect the change that ends the freeze. Its purpose is already served. The build runs inside the turn, so no message arrives during one, and D-185 rejects an overlapping Chat call with `CodeAborted`. `Freeze`, `Ctx.Frozen`, the `Turn` guard, the `Plan` guard, and both freeze tests are removed. Gate conversation 12 tested the frozen state and now tests a slot change after a build, which is an ordinary turn. The 30-conversation denominator of D-105 is kept. |
| D-242 | 2026-08-27 | A card the user says to keep must reach the deck (assistant finding, completes D-70) | The locked row asks which cards the deck must keep, and `agentsvc` never passed the answer to the build. The generator could not name a locked card that the shortlist did not already rank, and no check noticed one missing, so a deck without it answered the user's own instruction with silence. Deck gate prompt 11 only appeared to work because the prompt text and the pool were built by hand. `agentsvc` now resolves the locked names, adds them to the pool, and passes their oracle ids. The prompt states them as a limit. A locked card the deck does not hold is a BLOCK finding, so the repair turn gets one chance to put it back. A card that became the commander counts as held: it is in the deck, in the command zone (D-70). |
| D-243 | 2026-08-27 | Every slot is read by the build or named as unread (assistant finding) | The question layer collected more than the build consumed, six times over: D-226, D-232, D-238, D-240, D-241, and D-242. Each was found by accident while chasing something else, which is not a method. `TestEverySlotIsReadOrNamed` parses the `Slots` message and checks each field against `build.go`. A field the build reads must not be listed as unread, and a field it does not read must be listed with a reason. Four fields are named unread today: `slot_states` is the question layer's own bookkeeping, `plan_variant` waits for PR-9, and `locked_oracle_ids` and `commander_oracle_ids` are read from the private state instead, which holds the names the user wrote. The test was checked in both directions: it fails when a field has no consumer and no reason, and it fails when a field is read and still claimed unread. |
| D-244 | 2026-08-28 | The model can not budget what it can not see (assistant finding, completes D-236) | Deck gate run 5 built a deck costing $268.37 against a $100.00 cap. The shortlist it was given holds 296 cards whose cheapest 99 cost $25.66, with a median card of $0.70 and only 62 cards over $5. The cap was reachable many times over. No shortlist line carried a price, so the model had no reason to prefer a $0.70 card over a $53.68 one. It was guessing. Three fixes. A shortlist line carries the price of one copy when the session has a budget, and carries none otherwise, so an ordinary build pays no tokens for them. The budget is a limit block of its own rather than a line of the plan prose, and it names what the cap counts and that an owned copy costs nothing. Going over the cap buys the repair turn. The finding stays a warning, because a price is a daily estimate and not a rule of the game, and a deck the user can trim is more use than no deck (D-236). Measured on 2026-08-28: prompt 12 fell from $268.37 to $17.54 against its $100 cap, and a new prompt 16 came to $11.14 to buy against a $25 cap, with the whole deck at $29.82. Neither needed the repair turn. CAUTION: this measures price and not deck quality. Nothing yet says whether a cheaper deck still plays well. That is the M-5 and PR-15 lane. |
| D-245 | 2026-08-28 | A built deck is kept (owner decision, completes PR-8) | PR-8 built a deck, streamed it, and let it go. Nothing held it, so `session.deck_ids` stayed empty, `Context.AfterBuild` was never true, and the variance row was dead for every real user. `GetDeck` and `ListDecks` were declared RPCs with no implementation, `Export` had nothing to read, and the staleness job of PR-3 had nothing to re-validate. The roadmap assumed stored decks and assigned the store to no PR: line 286 says the service reads `AfterBuild` from the stored deck ids, and nothing wrote one. `internal/decks` stores a deck per user, on the shape `internal/sessions` set: gzip protojson so a proto change reads back with no migration, and flat fields for the list query. A deck is written once and never updated, so no version is compared. A re-roll is a new deck with a new id (D-18). The build now stamps `id`, `name`, `created_at`, and `buy_cost_usd`, which `BuyCost` already computed and nothing stored. `agentsvc` reserves the id before the build, keeps the deck, and writes the id to the session in a second Put, because the session is stored before the build runs. A store failure never takes the deck from the user who is already reading it. `decksvc` serves `GetDeck` and `ListDecks`, and both refuse another user's deck. `make store-check` runs both stores against the Firestore emulator, and the deck tests pass there: a round trip, user isolation, an unknown id, newest-first order, and the limit. |
| D-246 | 2026-08-28 | A deck export names its foils, and the parser must read them (assistant finding) | The owner supplied the Avengers Assemble precon on 2026-08-28, as a deck export in the MTG Arena format the corpus names in section 9. `ParseArenaText` read 90 rows and lost one card. `parseArenaLine` looks for the "(SET) 123" pair as the last two fields, and a trailing finish marker sits after the collector number, so the pair could not be found and the whole line became the card name. The lost card was the commander: the deck lost the one card it is built around, and 99 of 100 cards resolved with no error a reader would notice. The unresolved row also carried no raw text, so nothing said which line failed. The parser now reads a trailing `*F*` as foil and `*E*` as etched, and it records the raw line on every row. Measured on the same file: 100 of 100 cards resolve and nothing is unresolved. NOTE: this is the deck-import path of the corpus, not a precon feature. A user who exports a deck from ManaBox with a foil in it hit the same fault. |
| D-247 | 2026-08-28 | The precon decklists live in the repo, and the words name the precon (owner decision, answers OQ-40, unblocks D-218) | The owner supplied nine precon decklists on 2026-08-28, in the deck export format the corpus names in section 9. `internal/precons` embeds them under `decks/`, one file per product, and the file name is the product name. Adding a precon is adding a file. A file may carry a sideboard, and it is not part of the hundred: From Cute to Brute lists five Secret Lair cards there and Tricky Terrain lists an alternate commander. The loader splits the sections, and the shared parser does not, because a 60-card import wants both halves. `Find` reads the user's own words, because the classifier reports a card name for "upgrade my Avengers Assemble precon" and not a product (D-240). All nine resolve to exactly 100 cards with nothing unresolved. Six of them are wholly in the owner's collection, and three are not: From Cute to Brute at 21 percent, Living Energy at 29, and Tricky Terrain at 20. |
| D-248 | 2026-08-28 | The precon share is stated as a count, and the cards must be in the pool (assistant finding, completes D-218) | The share rule ran for the first time on 2026-08-28 and both decks failed it: 68 percent and 29 percent kept. Two faults. The prompt stated a percentage, which asks the model to do arithmetic against a list it is still writing. It states a count now, and how many cards it may drop. The precon's cards also reached the pool after the pool was built, so the model saw 27 of 93 and refused an instruction it could not meet. Its summary said so, and it was right. The resolution now runs before the pool. A short share buys the repair turn, as an over-budget deck does. Measured after both fixes: prompt 18 meets the share, and prompt 17 keeps 54 of the 68 it needs. All 79 of that precon's cards are in its pool, so the shortfall is the model and not the wiring. CAUTION: the rule is enforced and one deck of two meets it. The prompt needs more work before an upgrade request can be trusted. |
| D-249 (amended by D-251) | 2026-08-28 | An upgrade gets no job targets (assistant finding, completes D-218) | The precon share and the job targets were two instructions that fought. The targets prescribe the whole deck and sum to 99 slots, and the share demands 68 of them come from the precon, so the model split the difference and kept 54. A precon is a working deck already. An upgrade now gets no job targets, and the prompt says the precon's own shape holds. Measured: the kept count went from 54 of 68 to 67, and the other prompt met its share with no repair turn at all. |
| D-250 | 2026-08-28 | The builder closes a small precon shortfall (assistant finding, completes D-218) | After D-249 the deck kept 67 of the 68 cards the share needs. The repair turn read a finding that said "keeps 67, asks for 68" and returned 67 again. The model can not count its own list reliably. `swapBackPrecon` trades a card the precon does not hold for one it does, until the share is met, and it reports the count as an INFO finding. It moves at most three cards. A larger gap means the model built a different deck, and that stays a finding for the user to see. It never moves a locked card, a basic land, or an entry of more than one copy, so the mana base and the deck size do not change. Measured on 2026-08-28: both precon prompts meet the share, neither needs the repair turn, and the pair costs $0.1196 against $0.3502 before. |
| D-251 | 2026-08-28 | An upgrade keeps the precon's mana base, from the precon's own count (assistant finding, corrects D-249) | D-249 dropped every job target for an upgrade, and the land count went with them. Deck gate run 7 built both precon decks with 25 lands against a guide of 34 to 38, which the land-count advisory caught. A 25-land Commander deck does not work. Putting the generic target back broke the share instead: 36 lands and 68 precon cards read as two quotas over 99 slots, and the share fell to 61. Neither works, because a generic target is a second quota and the precon's own lands are already precon cards. The upgrade prompt names the precon's own land count and sends no job target. `Precon.Lands` carries the true number from the decklist, copies included, so nothing guesses at how many basics a precon runs. Measured across the attempts on Goblin Storm: no target gave 25 lands, a generic target gave a share of 61, a guessed count gave 30 lands, and the true count gave 39. CAUTION: 39 is one over the guide range. The share holds and the deck is legal, and the mana base is a guide and not a rule. |
| D-252 | 2026-08-28 | A slot the harness fills must be closed, not marked (assistant finding, corrects D-239) | Question gate run 23 failed. Catalog-only reached 30 of 30, the best measured, and conversation 11 called itself complete with every slot "never asked". `has_deck` of D-239 set `Ctx.Filled[k]` by hand. `State.Close` does three things: it marks the planner's view, it sets the proto slot state, and it fills the ask record. The harness did the first alone, so the planner called the slots answered while the record called them never asked, and the premature check read the record. That is the fault this session retired four rows for, committed again: two records of one fact, and one of them updated. The harness calls `Close` now. Run 24 passed at 28 of 30 with no premature session, and the variance row fired for the first time in any gate run, which is what D-239 set out to reach. NOTE: the re-baseline was run to give PR-9 a starting number, and it caught a regression instead. NOTE: conversation 105 was written to force the weak-pool row, and D-232 retired that row. Its note says what it still covers. |
| D-253 | 2026-08-28 | The budget-scope row does not ask what the message already said (assistant finding, extends D-238 and D-111) | The row held two of the twenty bad questions of gate run 24, and both were trigger faults and not storage faults. Conversation 2 said "Build owned-first with a buy list" and was asked which of the two the cap covers. A buy list names the scope, so a budget beside one caps the cards to buy. Probe 92 said "I proxy anything over 20 dollars", which D-111 reads as a proxy rule and not a budget. The budget slot was skipped and the scope row asked anyway: no budget means no scope. A message that names the buy list now fills the scope and closes the row. A skipped budget skips the scope with it. `TestBudgetScopeIsNotAskedWhenTheWordsAnswerIt` holds both, and it checks that a plain budget is not read as a buy list. |
| D-254 | 2026-08-28 | The CI shell and proto checks are green again (assistant finding) | Two jobs of `verify` were red, and neither was caught when PR-7c was opened. `mergeable` names a merge conflict and says nothing about a run. `verify:shell` broke on main with the PR-7c merge: the autotune rewrite left six shellcheck findings, and shellcheck exits non-zero on an info. Fixed one by one, not disabled in bulk: an `A && B || C` became an `if`, an unquoted expansion became `tr`, a `local` was split from its command substitution, and a path list became an array. Two `.env` sources keep a directive, and the autotune one had `disable=SC1091` on a line that held three commands, so it attached to `set -a` and the dot stayed flagged. The commands are split now. `verify:proto` failed on every pull request and passed on main. `buf breaking --against '.git#branch=main'` needs a local main branch. A push to main creates one, and a pull-request checkout does not: it carries `refs/remotes/origin/main` alone. The Makefile resolves `origin/main` when it exists and falls back to `main`. CAUTION: a green tree is not a green run. Read the run before you call a branch ready. |
| D-255 | 2026-08-28 | Every command binary is ignored, and a test says so (assistant finding) | `go build ./cmd/X` writes the binary to the working directory when no `-o` is given, so it lands at `go/X` at 40 to 90 MB. Three reached the repository before anyone read the file list: `candidates-review` in PR-6, and `chat-probe` and `summary-judge` in PR-8, for 216 MB together. The first fix was a list of the two binaries someone happened to see, which is the wrong shape: it covers what was noticed and nothing else. `.gitignore` now holds one line per command in `go/cmd`, and `TestEveryCommandIsIgnored` fails when a command has no line. `TestNoCommandBinaryIsPresent` fails when a built binary sits in the module root, and it found two more the moment it ran: `deck-gate` and `generate-probe`, at 179 MB together, untracked and one forced add away from the same fault. CAUTION: the three binaries leave the working tree and not the history. The pack still carries them, and only a rewrite would remove that. Nothing new is added. |
| D-256 | 2026-08-28 | PR-9 leaves the MVP, and its machinery leaves with it (owner decision, amends D-18) | The owner ruled designed variance out of the MVP. Three reasons hold it. Phase 3 waits on PR-8's gate and not on PR-9's, and that gate held on 2026-08-28. The PR-9 gate asks that two builds differ by 30 percent, a number the roadmap already calls a placeholder until PR-15 measures it, so nobody could hold the work to it. No user has asked: D-18 is a preference recorded before a user existed. The variance row is retired with the item. It asked which kind of variance the user wanted, and `plan_variant` was read nowhere, which is the fault D-226, D-232, D-238, D-240, D-241, and D-242 each corrected. Keeping the row while shelving the work would have recreated it on purpose. `Deck.seed` and `Slots.plan_variant` leave the contract. `buf breaking` allows it because the ruleset is WIRE_JSON and both the number and the name are reserved, which is the precedent D-155 set. Neither field was ever set, and no stored deck or session carries one. The slot name `plan_variant` still keys `slot_states`, because the plan-choice row closes on it. What a user has instead: the deck store keeps every build (D-245), and the generate role is not deterministic, so a second ask gives a different deck. That is not designed variance, and it is not nothing. |
| D-257 | 2026-08-28 | The deployable API builds decks (owner decision, A-1 of the 2026-08-28 audit, corrects the wiring gap L-1 and L-2) | `cmd/api` never passed `agentsvc.WithDecks`, and it read the card index before the first snapshot loaded, so `precons.Load` never ran. Only `cmd/chat-probe` could build a deck, and every deck gate ran on a path the API does not use. The owner ruled both gaps defects. The API now wires the generator and loads the precons after the first snapshot. That unblocks D-245 and D-247 in the API. |
| D-258 | 2026-08-28 | The loop noise default follows the measured value, 9 (owner decision, A-3, applies D-230 to the script) | D-230 raised `tune.DefaultNoise` to 9, and `scripts/autotune.sh` kept its own `--noise 3` from D-183 for two days. The owner chose the newer, better-measured value. The script now reads the constant through `tune-check -print-noise`, so the two can not disagree again. |
| D-259 | 2026-08-28 | The precon share is a ceiling over the nonbasic names (owner decision, A-5, amends D-218 and D-248) | The share is 85 percent of the nonbasic names of the precon. Basic lands swap free. The 15 percent is a maximum and not a target, so an upgrade makes the smallest set of changes that keeps the theme. D-218 named no denominator and D-248 counted every card, which made the basics count against the share. |
| D-260 | 2026-08-28 | Five catalog rows retire: `locked`, `jank`, `meta`, `plan_choice`, `theme_card_named` (owner decision, A-6, amends D-70, D-166, D-202, and D-214 for the locked row, D-103, D-109, D-132, D-169, and D-211 for the meta row, extends D-256 for the plan-choice row) | Each row asked a question and stored the answer nowhere. Section 9 of `docs/audit-2026-08-28.md` holds the check: the reader of each answer, the gate conversations that fire the row, and the decisions that name it. The locked row goes and the card names still flow, from the classifier to `State.LockedNames` to the build. The meta row returns if Phase 5 opens. The retirements touch 9 of the 30 gate conversations, so the next gate run is not comparable with runs 1 to 24 (A-9, D-263). |
| D-261 | 2026-08-28 | A message cap and a concurrency cap, no dollar ceiling (owner decision, A-7) | A message and an answer text hold at most 8 KiB. One process serves at most 8 concurrent `Chat` turns. A refused turn returns `ResourceExhausted`. The owner set no dollar ceiling yet. |
| D-262 | 2026-08-28 | The three binaries leave the history by a force push the owner runs (owner decision, A-8, extends D-255) | D-255 removed `candidates-review`, `chat-probe`, and `summary-judge` from the working tree, and the pack still carries the 216 MB. Only a history rewrite removes them. Section 10 of `docs/audit-2026-08-28.md` holds the procedure. The owner runs the force push, and the branch rule for `main` is lifted for that one step. |
| D-263 | 2026-08-28 | The gate set changes with no run (owner decision, A-9, amends D-105 and D-145) | Conversation 67 was a byte-identical copy of 38, and it is removed. Conversation 30 gains `has_deck`. A conversation with `has_deck` starts with its build slots closed, so it can not invent a question, and it leaves the catalog-only count. The file holds 104 conversations: 30 gate, 3 of them with a deck, and 74 probes. The next approved run is the baseline, and runs 1 to 24 do not compare with it (D-66). |
| D-264 | 2026-08-28 | The STE check runs in `make lint` and CI, prose only (owner decision, A-10, amends D-42) | D-42 kept the STE script as it was. `docs/tools/ste-check.py` now runs under `make ste-check`, which `make lint` and the `verify:shell` job call. It reads prose alone: tables and code blocks are exempt. The 68 findings of 2026-08-28 were fixed the same day. A `.md` file with a finding fails the build. |
| D-265 | 2026-08-28 | The house-rules answer is stored (owner decision, A-6 second half, extends D-3 and D-119) | The `house_rules` and `house_format_limits` rows stay, because D-3 and guardrail 6 require the question. The answer had no store. `Slots.house_rules` is field 12, a typed slot the classifier fills from the user's words, and the build copies it to `Format.house_rules`. `TestEverySlotIsReadOrNamed` holds a row for it. |
| D-266 | 2026-08-28 | `ChatRequest.seed`, `keep_oracle_ids`, and `DeckService.Export` leave the contract (owner decision, A-4, extends D-256) | Nothing read the two fields after D-256, and `Export` answered Unimplemented. Field numbers 5 and 6 and both names are reserved. The `Export` RPC and its messages are removed. `buf breaking` allows it under WIRE_JSON, which is the precedent D-155 set. |
| D-267 | 2026-08-28 | Compose reads `.env` and the fake stays health-only (owner decision, A-11, amends D-9) | D-9 said every role has a fixture fake. Only `health.json` exists, so `LLM_REQUIRE_KEYS=0` can serve no turn. Compose now reads `.env` through `env_file` and no longer sets `LLM_REQUIRE_KEYS=0`. `docker compose up` needs provider keys in `.env`, or the API fails fast with a key error. `make dev` still sets `LLM_REQUIRE_KEYS=0`, and the fake says which roles it can not serve. |
| D-268 | 2026-08-28 | The Firebase ID-token interceptor lands now (owner decision, A-12, extends the C-3 fix of the 2026-08-24 audit) | `go/internal/auth` reads `Authorization: Bearer` and verifies a Firebase ID token. `DEBUG_USER_ID` stays for local work behind `ALLOW_DEBUG_USER=1`. `ALLOWED_ORIGINS` sets CORS. Every caller was `local-dev` before this. |
| D-269 | 2026-08-28 | Grist leads a deck: the engine reads the creature CDA (assistant finding, completes D-140) | D-140 made the commander row quiet for Grist, the Hunger Tide, and the engine still blocked it. The engine now reads the characteristic-defining ability that makes Grist a creature card outside the battlefield (Scryfall ruling, 2021-06-18). The golden set moves Grist to good, at 31 good and 30 bad. |
| D-270 | 2026-08-28 | A name lookup refuses an ambiguous face name, prefers a legal card on a full-name tie, and drops front cards (assistant finding, guardrail 4) | `SkipLayouts` omitted `front_card`, so 291 such objects of the 2026-08-24 snapshot entered the index and won name lookups in file order. "Savage Lands" and "Inferno" resolved to a card with no type and no legality. "Fire" is a face of two cards and resolved to the first. A front card leaves the index. A full-name tie prefers the card that is legal somewhere. An ambiguous face name resolves to nothing, and the miss is counted. |
| D-271 | 2026-08-28 | The loop's accept rule reads the best run of the night (assistant finding, amends D-180, D-217, and the use of D-230) | Each iteration could be one noise margin worse than the last, and the baseline moved every time. The checker now compares against the best accepted run of the night. A commit with no `Rows` trailer is rejected. `questions-gate/main.go`, `metrics.go`, `roles.json`, and `prices.json` join the frozen list, because they are the scorecard. A partial eval carries `partial: true` and the checker refuses it. The fixer reads `.local/tune/<label>.fixer.json`, which holds no holdout verdict. The target stop reads the holdout ratio. |
| D-272 | 2026-08-28 | The Comprehensive Rules file is 2026-08-19 (assistant finding, amends the date of D-50) | The current file is `MagicCompRules 20260819.txt` (https://media.wizards.com/2026/downloads/MagicCompRules%2020260819.txt, verified 2026-08-28). The docs cited 2026-08-07, which was the file of 2026-08-23. Every rules citation was checked against the 2026-08-07 text. The sub-letters are unverified against the new file. |
| D-273 | 2026-08-28 | The live test needs the whole user path (owner decision, scopes PR-11 to PR-13) | The owner wants to test the app live, from the first screen to an exported deck, the way a user would. The UI must be usable and not production quality. The screens are sign-in, collection upload or skip, chat, deck view, and export. `docs/reference/ui-plan-2026-08-28.md` holds the plan. |
| D-274 | 2026-08-28 | The UI uses the roadmap stack from the start (owner decision, confirms PR-11) | Tailwind, TanStack Query, Zustand, react-router, and the import-boundary lint land in PR-11. A minimal stack would have cost a rewrite when the UI hardens. |
| D-275 | 2026-08-28 | The UI signs in for real, over the Auth emulator (owner decision, applies D-11 and D-268) | An email and password form over the Firebase Auth emulator. The ID token goes in the `Authorization` header, and the API checks it through the interceptor of D-268. The debug user of `DEBUG_USER_ID` stays for tools that send no token. |
| D-276 | 2026-08-28 | The live test runs locally under make dev (owner decision) | Emulators, fake GCS, the API, and Vite on the owner's machine, with real provider keys in `.env`. Cloud Run is not planned in this pass. |
| D-277 | 2026-08-28 | `GetCards` holds at most 120 distinct ids and names every miss (assistant finding, implements the ui plan section 4) | One call must cover a Commander deck of 100 cards with two commanders and a sideboard, so the cap is 120. A repeated id counts once, and an empty id is dropped. An id the index does not know goes in `missing_oracle_ids`, so the deck view reports "N cards are not in the card database" and never reads a short list as a full one. The cards come back in request order. `buf breaking` against `main` is clean. |
| D-278 | 2026-08-28 | The chat holds an open question until the user answers it, and the live panel survives the first navigate (assistant finding, applies D-237) | D-237 recorded that a turn which fills some slots and asks nothing sends no question, so the user saw nothing come back. The chat keeps every question of the last turn that asked any, and removes one when the user answers it. A reload rebuilds the same list from the stored turns. The first `session_started` event moves the route from `/session/new` to `/session/<id>`, and the panel stays mounted across that move, so the stream is not cut and `GetSession` does not run on a session that is still in its first turn. |
| D-279 | 2026-08-28 | Every card image carries "Illustrated by <artist>. © Wizards of the Coast, LLC" in a figcaption, and the deck view carries the Fan Content line (assistant finding, applies D-6 and guardrail 7) | The Scryfall API docs, read 2026-08-28, say: do not cover, crop, or clip off the copyright or the artist name, do not distort, skew, or stretch, and let the user identify the artist and the source. The face artist comes from `CardFace.artist`, and a card with no face data uses the default printing. The image scales in width only, so the aspect ratio holds. The normal image falls back to the small one on a load error (ui plan, section 8). A test asserts the caption on every image. |
| D-280 | 2026-08-28 | The word rules read the user's own words, and an echoed question sits on a `Q:` line (owner finding in the browser gate, amends D-112) | The owner wrote "Build an Angel deck that makes lots of counters", clicked Modern, and got "I build one deck at a time". `withAnswers` joined the question text "Commander, Standard, or Modern?" in front of the click, and `oneDeckRequest` counted three format names. `chat-probe` never sent a structured answer, so no gate saw it. The echo now goes on a `Q:` line with the answer on an `A:` line, the classifier reads both, and `questions.UserWords` strips the `Q:` line before the word rules, `closeByOption`, and the plan. The chat also replaced its open questions with the new turn's questions, so the color question left the screen when the one-deck question arrived (D-278 was incomplete). The open list now merges: an unanswered question stays, and a new question for the same slot replaces the old one. CAUTION: the classify input changed shape for structured answers, and the next question gate run measures it. |
| D-281 | 2026-08-28 | The message box hides while a question waits, and the UI audit fixes land (owner decision, and assistant findings from three review passes) | The owner saw the message box with its example text under an open question and asked for it to go. The box shows when no question is open, and Enter sends it. The audit found 40 more defects, and every one that a user can reach is fixed: an aborted stream no longer clears the busy flag of the live one, the session id and the collection id live in refs so a fast second send can not open a second session, the collection goes with the first message only, a reload merges the open questions the same way the live stream does and shows a clicked option as the user's line, a failed send gives the draft back, a Stop button aborts a stalled turn, sign-out clears the stored ids and the query cache, a second upload click can not import the same file twice, the import result clears when the user picks another collection, a click on the active collection clears it, a GetDeck failure shows an alert instead of "no deck", a dead stored session id offers "Start a new chat", the page commits to a light color scheme so a dark browser does not paint dark on dark, the header wraps on a phone, the file input looks like a button, the curve bar scales to its cell, the card grid reads its own column width, the Oracle text tooltip is gone, every list keeps its role in Safari, and no error announces twice. Not done: Markdown in the agent prose, and a tab control that reaches the deck on a phone. |
| D-282 | 2026-08-28 | The answers go out in one request, and the message box hides on Send (owner decision, amends D-281) | An option click selected and sent at once, so a user with two open questions could answer only one per round trip. An option click now selects, a second click on it clears it, free text replaces it, and one "Submit answers" button sends every answer in one `Chat` request. The button waits until every open question has an answer. The message box hides the moment a send starts, not when the first event lands, and it returns when the turn ends with no question open. |
| D-283 | 2026-08-28 | A message after a build revises the latest deck (owner decision, opens PR-12B, finding F-27) | The owner's first live session wrote "Replace some lands with better options if possible. Also tune the mana curve lower - no 6 or 7 mana cards needed" after the deck. The agent said nothing, and `plan()` built a second deck from turn 1 and the slots, with the same 24 Plains and the same four 6- and 7-mana cards. The owner's rule: after a build, every message is a change request against the deck the user sees. The agent answers in prose, then asks a question when the request is unclear, or builds the revised deck when it is clear. A slot change still means a full rebuild (D-241), with a note that says so. The roadmap entry PR-12B holds the design. Collection delete stays off the roadmap for now (owner, same day). |
| D-284 | 2026-08-28 | A revision request is clarified, done, or declined with a reason (owner decision, refines D-283) | The assistant asked what "better lands" means in a mono-white Modern deck. The owner's rule: when a user asks for a land upgrade in any form, the agent first finds out what they mean and how it can help. When no change would help, the agent says so, for example "for a casual mono-white deck, all basic lands is fine". The revision brief of PR-12B carries a declined list with a reason per item, and the reply names every decline. Silence is never an outcome. |
| D-285 | 2026-08-28 | The revision turn: a `revise` role writes the brief, the pool enforces it, and the reply comes from the diff (assistant finding, implements PR-12B) | After a build, a turn that changes no deck setting runs the revise role over the message and the deck the user read. The brief carries the changes, the cards to remove, the cards to keep, a mana cap, one question, and the declines with a reason. The pool of the revised build drops every removed card and every nonland over the cap, so the model can not name one (D-222). The base deck's cards join the pool, so the model can keep them. `CheckRevision` adds a block finding for a removed card that stayed or a kept card that left, and a warning that buys the repair turn for a card over the cap. The reply is built from the diff of the two decks plus the declines, never from the model, and it lands in `Turn.agent_message`. A slot change after a build rebuilds from the start and says so. A revise question is an invented question with slot `revision`, so the answer flows back through the same `Answer` path, and the revise call reads the earlier message too. `make revise-gate` is the paid gate, unrun as of this row. |
| D-286 | 2026-08-28 | The verify workflow runs on pull requests only, skips unchanged jobs, and cancels a superseded run (owner directive, applies the Actions quota) | The owner had used 90 percent of the monthly GitHub Actions minutes after six days. The history showed why: 114 runs, 25 billed minutes each, a second run on every merge to `main`, no cancel on a new push, and twelve Dependabot pull requests in one day that each ran again after every merge. The workflow now runs on pull requests only, cancels the run in progress per branch, and reads the diff in a first job so that the Go, web, proto, emulator, docker, and shell jobs run only when their inputs changed. `govulncheck` runs when `go.sum` changed. The fan-in job is gone: the repo is private on the free plan, and GitHub refuses a ruleset there, so no required check can exist. Dependabot moves to one grouped monthly pull request per ecosystem with no automatic rebase. A code pull request costs about 12 billed minutes now, a docs pull request 2, and a merge 0. |
| D-287 | 2026-08-28 | A commander offer shows each card with its art and its rules text (owner directive, applies D-6) | The commander-pick question offered three names as plain buttons, and the user had nothing to choose by. `Question.option_oracle_ids` is a new additive field, parallel to `options`. `agentsvc` fills it for every option that is an exact card name in the index, and leaves an empty string for an option that is not a card, for example "None, name three more". The UI reads the ids through `GetCards` and shows each card option as a tile: the art with the artist and the copyright line, the type line, the mana cost, the rules text, then the pick button. A question with no card option keeps its plain buttons. The rule is general: any question whose option is a card name gets the tile, not only the commander row. |
| D-288 | 2026-08-28 | A number that answers "Is there a budget for cards to buy?" sets the scope to the cards to buy, and a question with no options labels its field "Your answer" (owner directives) | The owner answered the budget row with "$100" and was asked "Is that a cap on the cards you buy, or on the whole deck value?". The budget row names the scope in its own text, so the answer carries it. The word rule `ruleBudgetScope` reads the keys that were open when the turn began, and a budget number that answers the budget key closes `budget_scope` as the cards to buy. A user who names the whole deck in the same message still gets the whole-deck scope, because that case reads the words first. The UI change: a question with no options showed "Or answer in your own words" over its field, which made no sense with nothing to answer otherwise. The label reads "Your answer" then. |
| D-289 | 2026-08-28 | The shortlist follows the commander's color identity, and the deck view shows the commander from its own field (owner finding in the browser) | Session `zXDG0swGvB0Oak92xrEg`: the user chose Ghalta, Primal Hunger and said any colors are fine. The classifier filled every color, `buildDeck` passed the slot colors to the candidate builder, and the shortlist held nine white and red Dinosaurs. The engine refused each one as off-color, and the repair turn could not fix what the pool forced. The candidate request now carries `deckColors`, which is the union of the commanders' color identities when a commander is chosen and the slot colors otherwise. The deck gate did this already, and the product path did not. A test proves a green card stays out of a white-black commander's pool. The second fault was the deck view: it drew the command zone from the card list, and the generator never lists the commander there. The view now builds the command zone from `commander_oracle_ids`, loads those cards too, shows no owned mark for them, and counts "99 cards + 1 commander". The owner also asked that code comments carry no session ids, dates, or owner quotes. They cite the decision id and state the rule. |
| D-290 | 2026-08-28 | The commander row asks for a name or a suggestion in one step (owner decision, amends D-71) | The row asked "Do you have a commander in mind, or should I suggest one?" with the options "I have one in mind" and "Suggest one". The first option answered nothing: the user then had to type the name in a second turn. The row reads "Which commander do you want? Name one, or I suggest three." with the one option "Suggest one", and the free-text field takes the name. The classifier already maps a typed name to `commander_names` and "Suggest one" to `wants_suggestion`. The ask prompt's example cited the old row, and it moves to version 13 with the new text. The owner also cut the type line and the rules text from a card option tile: the card image carries them, so the tile shows the image, the attribution, and the button (amends D-287). CAUTION: a catalog text change and an ask prompt change re-baseline the question gate (D-66), and the conversation set still holds the messages that answer the row in words. |
| D-291 | 2026-08-28 | A card tile shows the full card image and no caption (owner decision, amends D-279 and the PR-12 gate) | The tile under each image repeated the artist and the copyright line that the card prints in its own frame. The owner cut it: the image is enough. The Scryfall guidelines, read 2026-08-28, ask for a separate artist and copyright line only beside an `art_crop`, and the app never shows one. The full image is shown uncropped and unscaled in its aspect ratio, so the printed credit and copyright stay visible, which is what the guideline asks. The one Fan Content notice of the deck view stays. A double-faced card keeps a "face 1 of 2" caption, because two images need a label. The owner then cut the "Oracle text" dropdown under each card for the same reason: the image shows the rules text (2026-08-29). The PR-12 gate line "a test asserts the artist and the copyright on every image" reads "a test asserts the full image, uncropped, on every card" now. |
| D-292 | 2026-08-28 | The conversation words hold the user's words only, never a quoted question (assistant finding, extends D-280) | Session `azQUnSDL3EdrIkRkYMil` got the precon question with no precon named. The power row's option "2 core, near a precon" came back on the `Q:` line of a structured answer, `AddMessage` folded the whole message into `Ctx.Words`, and `preconRequest` read "precon" as the user's word. D-280 kept the quoted question out of the turn's word rules and not out of the stored words. `AddMessage` stores the full message for the classifier's prior messages and `UserWords` for the conversation words. A test holds the split. |
| D-293 | 2026-08-28 | A commander offer before the pool question ranks on quality alone (owner finding, amends the owned-first rule of the commander pool) | Session `azQUnSDL3EdrIkRkYMil` asked for "a really good commander deck that has lots of removal" and was offered Glissa Sunslayer, Pashalik Mons, and Witch-king of Angmar. The owner called them terrible. The snapshot ranking for that theme is Braids, Arisen Nightmare, Loran of the Third Path, and Sheoldred, Whispering One. The offer went out before the pool question, `CommanderPool` read an unset pool rule with a collection as owned-first, and the owned-first reorder put the removal-tagged legends the user owns ahead of every ranked one. An unset pool rule ranks as any card now. A user who chose owned-first or owned-only still sees the owned commanders first, because that is the choice they made. The Dinosaur offer of session `zXDG0swGvB0Oak92xrEg` came from the same reorder. |
| D-294 | 2026-08-28 | The commander rows come after the power, the colors, the pool, and the budget (owner decision, amends the catalog order of D-71) | Session `uyiIW2YqMt42e3knGQ5I` asked for a commander before the power and the colors. The owner's rule: the power and the colors decide which commander fits, so the commander choice is the last question before the build, as a matter of order and not as a hard rule. The commander row moves from order 31 to 80 and the pick row from 32 to 81, after the budget rows. Nothing else in the row changes, and a message that names a commander still fills the slot at once. A `requires` list on the two rows was tried and dropped: it held the offer back even when the user had answered everything in one message, and it broke the conversation set. With three questions a turn, the commander row now sits in the batch after the power and the colors in every scripted conversation. Two scripts moved the commander one turn later, and one of them became shorter. The question gate re-baselines with D-290. |
| D-295 | 2026-08-28 | A closed question offers no free-text field (owner decision) | The format question showed "Or answer in your own words" under three options, and the app builds those three formats and no other. A catalog row carries `closed: true` when its options are the whole answer space, and the question carries `Question.closed`, an additive field. The UI shows the buttons and no field. The closed rows: the three format rows with format names, the two power rows, the three pool rows, the budget-scope row, and the named-card-role row. A yes-or-no row whose "no" invites a name stays open, for example the illegal-commander row, and so do the theme and commander rows. |
| D-296 | 2026-08-28 | Revise gate run 1: the model held every bar, and the gate's own bar was wrong on a mixed message (assistant finding, PR-12B) | `docs/reference/pr12b-revise-gate.md`, $0.33, 3 minutes 36 seconds, 13 calls. Five of six revisions passed. Revision 1 was the owner's own message, "Replace some lands with better options if possible. Also tune the mana curve lower - no 6 or 7 mana cards needed". The brief asked "For better lands, do you want faster white mana, utility lands, or lands that support adding more colors?" and carried the cap at 5 with the change line, which is the outcome D-284 asks for. The gate marked the prompt clear and refused the question. The prompt file marks it unclear now, and the gate checks the cap in the brief even when a question goes out. The cap revisions kept 97 and 100 percent of the untouched cards, removed every card over the cap, and passed the engine. "Better lands please" earned a question. "Add two more cards that draw cards, and cut two of the weakest creatures" cut Blood Baron of Vizkopa and Epicure of Blood and added Convalescent Care and The Gaffer. Run 2 the same day, with the bar fixed: PASS 6 of 6, $0.29 (`docs/reference/pr12b-revise-gate-run2.md`). The mixed message earned its question and carried the cap, and every other revision held. |
| D-297 | 2026-08-29 | The commander offer ranks on quality in owned-first too, the commander row is fixed, and the pick row says "Which commander" (owner finding, amends D-293 and D-290) | Session `z5uIOD3LZvhfG17BbtW9` asked for a bracket 5 Dinosaur deck, chose "use the cards I own first", and was offered Ghalta, Primal Hunger, Moon Girl and Devil Dinosaur, and Reptil, Dinomorpher, the Dinosaur legends the user owns. D-293 kept the owned-first reorder for an explicit choice, and the owner called the result horrible. The commander is one card, and the buy list exists for it, so the offer ranks on quality in owned-first as in any-card. Owned-only still needs an owned commander, because there nothing is bought. The owned mark stays on the candidate. The same session showed the ask role rewriting the D-290 row back to "Do you have a Commander in mind, or should I suggest one?", so the row is fixed now and its text goes out as written. The pick row reads "Which commander do you want" instead of "Which one do you want". Both text changes re-baseline the question gate (D-66). |
| D-298 | 2026-08-29 | The mana sources table shows the deck's own colors (owner finding) | The table counted every card that can make each color, so a mono-green deck full of rocks and lands that make any color reported ten white, twenty-two blue, ten black, and ten red sources. The owner could not read it. The table now lists the colors the deck's cards are, and colorless alone for a colorless deck, under the caption "Mana sources, cards that make each of the deck's colors". The count itself is unchanged. |
| D-299 | 2026-08-29 | An owned card shows the priciest printing the user holds (owner decision) | The deck view showed the snapshot's default printing for every card. The owner wants an owned card to show the copy they own, and the most valuable one when they own several. The card index keeps the display fields and the USD price of every playable printing now, `Index.Printing` answers a printing id, and the collection repo lists the printing ids per Oracle id. After a build, `agentsvc` sets `DeckCard.owned_printing` on each owned card to the owned printing with the highest price that has an image. `Printing.price_usd` is a new field. The tile shows that image in place of the default one on a single-faced card. A double-faced card keeps its face images, because a printing row carries no face images. The price used is the nonfoil USD price of the printing, so a foil copy ranks by its nonfoil price. The index grows by one small record per printing. |
| D-300 | 2026-08-29 | The deck view drops the not_owned warnings (owner decision) | An owned-first deck carried one `not_owned` warning per card to buy, twenty-five in the owner's Dinosaur deck, and each tile already says "To buy" with the price. The view filters a `not_owned` finding of warning severity out of its Findings list. A `not_owned` block, which owned-only produces, still shows. The rules engine is unchanged: the deck gate documents and the owned-only check read the findings as before. |
| D-301 | 2026-08-29 | A card the revision brief removes is unlocked, and a missing locked card is named (assistant finding from the owner's session) | Session `kQqFj9xeRXJRiPz9SaTp` wrote "Replace Arena of Glory with a card I do not own - don't replace anything else." The classify call, which runs on a revision turn to catch a setting change, read the named card as a card to keep. The brief removed it, the pool dropped it, and the locked-card check blocked the revised deck for a card the user had asked to remove, naming it by Oracle id because the pool no longer held it. Two fixes. `State.Unlock` drops the lock for every card the brief removes, before the build and in the stored state, so a later turn does not block on it either. `missingLocked` names the card from the card index when the pool does not hold it. The revision itself was right: Arena of Glory left, Armored Kincaller came in, nothing else moved. |
| D-302 | 2026-08-29 | The quality audit of 2026-08-29 lands its prompt-affecting fixes now, and the owner re-baselines later (owner decision) | Three fixes change a prompt or the classify schema: the dead `two_plans` and `owned_mode` facts leave the schema, the shortlist text omits the commander, and the house format gets a sideboard sentence. Each bumps its prompt version (D-66). The owner runs the question gate and the deck gate when they choose. Until then the baselines of run 27 and deck run 8 do not compare with the code on `main`. |
| D-303 | 2026-08-29 | A turn during a build is refused, and a build finishes after a disconnect (owner decision) | A second message while a build runs started a second billed build, and the first build's deck id write then failed on the version conflict. A page change mid-build cancelled the paid model call. Now a session marks a build in flight, a turn during it gets `CodeAborted` with "a build is in progress", the build and its store writes run detached from the client context, and the deck shows on reload. The web app warns before it leaves a page mid-build. |
| D-304 | 2026-08-29 | The STE checker flags passive voice and modal verbs, and the live docs are fixed (owner decision) | `docs/tools/ste-check.py` did not check passive voice (3.6), modal and helper verbs (3.2, 3.4), sentence-initial -ing forms (3.5), or the 20-word procedural limit (5.1). The checker now flags them. The live docs are rewritten to pass. Dated records are exempt: gate documents, audits, session logs, and the eval documents. The Makefile names the exemption. |
| D-305 | 2026-08-29 | The go job fetches the full history, and a weekly scheduled govulncheck runs (owner decision) | `make llm-defaults-check` found no merge base under `fetch-depth: 1` and skipped on every run, so guardrail 3 never ran in CI. The go job checks out with `fetch-depth: 0`. A vulnerability in a pinned module was scanned only when `go.sum` changed. A weekly schedule runs govulncheck alone, at about 2 minutes a week. |
| D-306 | 2026-08-29 | The house format offers paper cards only (owner decision, refines D-3) | With the legality filter off, the shortlist could offer Alchemy rebalances, playtest cards, and cards with no paper printing. A house deck may hold any paper card, banned or not. A card with no paper printing never reaches the shortlist. |
| D-307 | 2026-08-29 | The export line names the owned printing when the user owns the card, else the default paper printing (owner decision, PR-13, applies D-15 and D-299) | The Arena text line is "4 Lightning Bolt (STA) 42". An owned card names the printing of `DeckCard.owned_printing`, so ManaBox matches the copy the user holds. A card to buy names the default paper printing of the snapshot. |
| D-308 | 2026-08-29 | The buy list holds the shortfall of the main deck, the sideboard, and the commander, and each row links to its printing on Scryfall (owner decision, PR-13) | A row is the count minus the owned count. The link opens `https://scryfall.com/card/<set>/<number>` for the default paper printing. The deck's upgrades show under their own heading. |
| D-309 | 2026-08-29 | The buy list exports as text through a second format (owner decision, PR-13) | `ExportDeck` accepts `EXPORT_FORMAT_BUY_LIST_TEXT` and returns one "count name" line per row, for a shop's mass-entry form. A second button copies and downloads it. |
| D-310 | 2026-08-29 | The product UI serves the owner locally and invited users on GCP (owner decision, opens Phase 3B) | The live-test UI of PR-11 to PR-13 is not a product. The next phase builds one. It runs under `make dev` and, at the end of the phase, on GCP for a list of invited users. No public sign-up. |
| D-311 | 2026-08-29 | The UI uses shadcn/Radix primitives, a light and a dark theme, and a card-art-forward look (owner decision, applies D-274) | Accessible primitives, a theme toggle that follows the system by default, and a neutral palette that lets the card art carry the color. Responsive from a phone to a desktop. |
| D-312 | 2026-08-29 | Phase 3B covers four flows: the deck library, the collection management, the chat and build experience, and the deck view with card detail (owner decision) | The owner chose all four. The order is the design system first, then the deck library, the collection management, the chat and build experience, the deck view, the share link, and the deploy. |
| D-313 | 2026-08-29 | The UI test bar is Vitest with axe per pull request, and one Playwright smoke flow on a manual trigger (owner decision) | Storybook, visual tests, and knip stay out. The smoke flow signs in, builds a deck, and exports it, at about 5 minutes per run, and it runs on `workflow_dispatch` only. |
| D-314 | 2026-08-29 | An invited user is an email on an allowlist the API reads (owner decision) | Sign-up stays open in Firebase Auth. The API refuses every uid whose email is not on the allowlist, with `CodePermissionDenied`. The list lives in one env var or one Firestore document. Local mode allows every emulator user. |
| D-315 | 2026-08-29 | A deck share link opens for anyone who holds it, read-only (owner decision) | A public read-only page at `/d/<token>`. The token is unguessable and revocable. The API gains one unauthenticated read, rate-limited, and the page carries no user data. |
| D-316 | 2026-08-29 | Phase 3B runs before Phase 4, and the paid re-baseline runs when the owner chooses (owner decision) | PR-14 and PR-15 follow the UI phase. The question gate and the deck gate re-baseline (D-302) in parallel, on the owner's word. |
| D-317 | 2026-08-29 | The first slice of Phase 3B is the design system and the app shell (owner decision) | Every existing screen moves onto the new primitives with no new feature. The features follow. |
| D-318 | 2026-08-29 | The sample hand leaves the parking lot, and the goldfish simulator stays parked (owner decision, amends D-20) | Draw seven, mulligan, draw one, from the exact deck list. No turn simulation. PR-20 holds it. |
| D-319 | 2026-08-29 | The Phase 3B roadmap is written before any of it is built (owner directive) | The owner asked for the complete roadmap first and no implementation. `docs/reference/ui-phase-plan-2026-08-29.md` holds the detail behind the roadmap entries. |
| D-320 | 2026-08-29 | The PR-16 bundle gate counts the raw bytes of the first-paint chunks, not the gzipped bytes (owner decision, PR-16) | The build of 2026-08-29 is one chunk of 584.07 kB raw and 179.81 kB gzipped. A gzip gate of 200 kB passes before the slice starts, so it measures nothing. The gate reads the raw bytes of the chunks that the first paint loads, from the Vite build report, and the roadmap records both numbers. |
| D-321 | 2026-08-29 | PR-16 adds only the shadcn primitives that the shell and the moved screens use (owner decision, amends the plan of D-317) | The plan listed 19 primitives for the first slice. A survey of the four screens found the button, the input, the checkbox, and the label. Each later slice adds the primitives that it needs, and no unused primitive enters the repo. |
| D-322 | 2026-08-29 | The session pushes the Phase 3B roadmap branch and opens the pull request, and the owner merges it (owner instruction) | The house rule says the owner commits and pushes. For this branch the owner asked the session to push and to open the pull request. PR-16 starts on a branch off `main` after the merge. |
| D-323 | 2026-08-29 | The PR-16 bundle gate reads the gzipped bytes of the first-paint chunks, the bar is 130 kB, and the shell menus and the toast host load on the first use (owner decision, amends D-320) | A measurement on 2026-08-29 found the floor. React, the router, and TanStack Query are 101.51 kB gzipped together, so a raw bar of 200 kB and a gzipped bar of 120 kB were both out of reach. The account menu, the theme menu, and the toast host now load on the first use, and Radix stays the implementation. The result is 116.31 kB gzipped and 363.25 kB raw, from 179.81 kB gzipped in one chunk. |
| D-324 | 2026-08-29 | The deck grid filters by power, and the filter runs on the server (owner decision, answers OQ-47, PR-17) | `PowerLevel` is a oneof, so `ListDecksRequest` gains two fields: `power_bracket`, 1 to 5, and `power_sixty_step`. Zero and UNSPECIFIED keep every deck. A filter in the grid alone would read one page of 24 and hide a matching deck on page two. |
| D-325 | 2026-08-29 | The message box leaves the screen while the agent works (owner report, PR-16B) | The box stayed and went disabled beside "the agent is working". A control that stays and does nothing reads as a dead control. The working row and its Stop now hold that space, and the box returns with focus when the turn ends. |
| D-326 | 2026-08-29 | The product UI takes a visual pass, because the screens of PR-16 still read as a test bench (owner direction, PR-16B) | PR-16 moved every screen onto the primitives and kept each composition, per D-317. So the design system changed and the screens did not. The pass gives the app a type face, an elevation scale, a docked composer, a reading measure, and a thread with real hierarchy. It moves the session id, the pool line, and the spend line out of the reading column. |
| D-327 | 2026-08-29 | The five colors of the game are the app's palette, and dark leads (owner decision, amends D-311) | D-311 kept every surface neutral and let the card art carry the color. The result had one accent and grey everywhere, and it read as boring. Now a deck carries its own color identity, and the page it owns takes that identity as its accent. A card role carries its own hue. Dark is the base theme, and light carries a class. |
| D-328 | 2026-08-30 | The shell is one top bar, and the sidebar and the phone tab bar go (owner decision, amends D-311 and D-327) | The reference design carries a header with the mark and the tagline on the left, and the navigation on the right. The header gives every page its full width, and one shell serves a phone and a desktop. |
| D-329 | 2026-08-30 | Gold and purple dress every deck, and the color of the game appears in a mana pip and a rarity dot alone (owner decision, amends D-327) | The identity wash left. A deck reads as one gold frame over a hatched ground, and its colors show as the lettered pips the game prints. The palette is the app's, not the deck's. |
| D-330 | 2026-08-30 | Dark is the only theme, and the light theme and its toggle go (owner decision, amends D-311 and D-327) | Parchment on navy with gold is a dark-native look, and a light version needs its own design rather than a token swap. The theme store, the toggle, the no-flash script, and half the axe matrix leave with it. |
| D-331 | 2026-08-30 | A deck owns the whole page, and the conversation docks at the corner (owner decision) | A deck beside a chat gave the deck half a screen and two cards a row, and it made the reader scroll far. Now the deck fills the page. The composer floats at the bottom left, always in reach, and a History control opens the thread over it. Before a deck exists, the conversation is the page. |
| D-332 | 2026-08-30 | Build in the header asks which cards the deck may use, and then opens a chat (owner decision) | The entry is a menu, not a link. It lists "Any card" and every collection with its count. A collection sets the owned pool, and any card clears it (D-37). The menu loads on the first open, so the first paint carries none of it. |
| D-333 | 2026-08-30 | The binder head sits under the controls, the buy list stays shut, and a page holds about 80 percent of a wide screen (owner decision) | The binder head over the form moved every control down when the reader picked an upload. The buy list of a hundred-card deck filled the panel, so it opens on request. A page capped at 75 rem keeps the docked chat off the content. |
| D-334 | 2026-08-30 | Build is where a signed-in reader lands, and its menu offers to add a collection when none exists (owner decision, amends D-37 and D-332) | The root path and the sign-in return both go to `/session/new`. Build is what the app is for, and the collection is a step on the way. With no collection the menu says the agent builds from any card, and it offers the way to the upload screen. |
| D-335 | 2026-08-30 | A deck has one screen and one address, `/decks/<deck id>` (owner decision) | The deck page carried the actions and no chat, and the session page carried the chat and no actions, for the same deck. The deck address now shows the deck, the actions the user owns, and the conversation that built it. `/session/<id>` carries a build with no deck yet, and it hands the reader to the deck address the moment a deck exists. The "Open the chat" button went, because the chat is on the screen. |

| D-336 | 2026-08-30 | The chat screen carries its own collection picker (owner report) | The pool lived in the header menu of D-332 alone, and the owner did not find it. A "Build from" control now sits under the title of a new chat. It lists any card, every collection with its count, and the way to add one. The owned-only checkbox moved into it. The picker leaves once the session starts, because the server holds the pool of a started session. |
| D-337 | 2026-08-30 | No screen shows a decision id or a Firestore id to a reader (owner report, D-336) | The pool line read "Pool: any card (D-37)", and the collection line read "Pool: your collection (c1)". A decision id belongs in the documents, and an opaque id means nothing to a reader. The line names the collection now. |
| D-338 | 2026-08-30 | No chunk of this app loads behind a Suspense boundary (measurement, 2026-08-30) | React holds a committed Suspense fallback for 300 ms, and it holds every later reveal with it. The two `Suspense fallback={null}` boundaries of the shell committed at 19 ms and put every route behind that hold. The measured result: every file in the browser by 42 ms, and content at 347 ms. `app/deferred.tsx` replaces `React.lazy`. It starts a download, it mounts the component the moment the code is here, and nothing suspends. Content now shows at 44 ms. |
| D-339 | 2026-08-30 | Every deferred chunk warms in the idle time after the first paint (measurement, 2026-08-30) | A menu that mounts on the click cost 323 ms of that click, with the chunk already in the browser. The bytes of the first paint do not change, so the D-323 bar holds at 116.13 kB gzipped. The first open of the Build menu now costs 16 ms. |
| D-340 | 2026-08-30 | Server state stays fresh for 30 seconds (measurement, 2026-08-30) | Every hop between pages refired its list call. A mutation still invalidates its key, so a change shows at once. A return to a page it already read now needs no call. |
| D-341 | 2026-08-30 | An open question shows in its card alone, not also in the thread (owner report) | A question enters the thread and the open list at the same time, so the screen asked it twice. The thread now holds the line for the history, and it shows the question after the answer. A user line hugs its text, because a two-word message filled 85 percent of the row. |
| D-342 | 2026-08-30 | "Add a collection" opens the file dialog on arrival, and it clears the active collection first (owner decision) | The reader asked for a new collection, so the old one is not the answer. A cancelled file dialog therefore leaves no collection active. The history entry drops its mark before the dialog opens, so a reload opens no dialog. The pool picker and the header menu both do this. |
| D-343 | 2026-08-30 | The decks of one chat are the version history of one deck, and any two of them compare (PR-17) | A revision turn writes a new deck in the same session, and `revised_from_deck_id` names the one it revised. `ListDecks` takes a `session_id`, so one call reads the whole history with no walk of the chain. Every version keeps its own address. The compare reads the two decks whole and shows the diff the revision note already used. |
| D-344 | 2026-08-30 | PR-17 ships whole, with all eight of its concerns on one branch (owner decision) | The session recommended a split into the contract, the look, and the speed work, because guardrail 10 asks for one concern. The owner chose to ship it whole and take the history cost. |
| D-345 | 2026-08-31 | The pool is a choice of one chat, not a setting of the app (owner decision, amends D-37) | The picker opened on the collection the reader last used, because the store kept it in localStorage. Only the session id survives a reload now, so every load opens at any card. A reader who builds from a collection names it each time. |
| D-346 | 2026-08-31 | The chat is a pinned column at the left of a deck, not a panel over it (owner decision, amends D-331) | The floating dock covered the deck. The chat is now a column of its own, full height, and the deck sits beside it. The gap between the two equals the gap between the deck and the right edge of the window. The History control left, because the whole thread is in view. |
| D-347 | 2026-08-31 | A collection can be deleted, and a deck built from it then builds from the whole card database (owner decision) | `CollectionService.DeleteCollection` removes one collection for good. A deck keeps every card it holds. Its chat reads the collection on the next turn, finds it gone, forgets it, sets the pool to any card, and says so in the thread. |
| D-348 | 2026-08-31 | The export buttons name what the reader gets, not the text shape (owner decision) | "Copy Arena text" is now "Copy deck list". The shape is still the Arena text that ManaBox and MTG Arena read, and the panel says so. The deck list holds every card, the commander first. |
