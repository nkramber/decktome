# Go
.bin/
go/bin/
go/coverage.out
# Node
node_modules/
web/apps/web/dist/
# The Playwright smoke flow writes its traces and its report here (PR-23)
web/apps/web/test-results/
web/apps/web/playwright-report/
# Local stack state
.local/
# OS / editor
.DS_Store
.vscode/
# Secrets
.env
.env.*
!.env.example
*.tsbuildinfo
*-debug.log
firebase-export-*/
coverage/
.firebase/

# Binaries that `go build ./cmd/X` drops in the module root, because it
# writes to the working directory when no -o is given. Three reached the
# repo before this list did: candidates-review in PR-6, and chat-probe and
# summary-judge in PR-8, for 216 MB together (D-255).
#
# One line per command in go/cmd. A new command needs a new line, which
# the test in go/cmd/cmdignore_test.go enforces.
/go/api
/go/candidates-review
/go/chat-probe
/go/allow
/go/feedback
/go/feedback-harvest
/go/users-backfill
/go/deck-gate
/go/eval
/go/bracket-gate
/go/quality-gate
/go/revise-gate
/go/generate-probe
/go/m5-report
/go/m5-sheet
/go/questions-eval
/go/questions-gate
/go/summary-judge
/go/tune-check
/go/worker

# The feedback harvest holds what a reader wrote (D-559, D-640). The
# repository is public, so no harvest file belongs in it. The tuning loop
# reads these files where they are written, and git never sees them.
docs/reference/feedback/
