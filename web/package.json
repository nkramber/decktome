{
  "name": "mtg-deck-builder-web",
  "private": true,
  "packageManager": "pnpm@9.2.0",
  "engines": {
    "node": ">=22.12.0 <23"
  },
  "scripts": {
    "dev": "pnpm --filter @mtg/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@bufbuild/protoc-gen-es": "2.14.0",
    "typescript": "7.0.2"
  }
}
