rules_version = '2';
// Default deny. The Go services use the Admin SDK, which bypasses rules.
// Client SDKs get no direct Firestore access. The API is the only door.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
